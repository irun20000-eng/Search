# 수학사 개념 지도 — 무엇을 어떤 순서로 쓸 것인가

`math/` 개념 문서의 집필 계획. **무엇을 쓸지**만 정한다.
**어떻게 쓸지**의 정본은 `MATH_PIPELINE.md` 이고, **사실의 정본**은 각 노트다.

여기 적힌 연도·인물·저작은 집필 시 M1 수집으로 **다시 검증한다.** 이 문서를 출처로 삼지 않는다.

---

## 0. 이 지도는 추측이 아니라 리포에서 뽑았다

계획 문서가 흔히 실패하는 방식은 "쓰면 좋을 것 같은 주제"를 나열하는 것이다.
그러면 본문과 무관한 목록이 되고 아무도 보지 않는다.

이 지도의 항목은 셋 중 하나에서 나왔다.

1. **frontmatter 가 이미 가리키는 슬러그** — `선행개념`·`후속개념`·`기여개념`에 적혀 있으나 문서가 없는 것. 이미 걸어 둔 약속이다.
2. **세기 개관이 약속한 내용** — `century-18c` 본문이 "이 세기에 이런 게 열렸다"고 써 놓고 문서가 없는 것.
3. **인물 노트의 `기여개념` 이 비어 있어 생긴 구멍** — 인물은 있는데 그가 기여한 개념 문서가 없는 것.

---

## 1. 왜 지금 지도가 필요한가 — 관측된 결함 3건

### (1) 18세기 인물이 전부 17세기만 가리킨다

| 인물 | `기여개념` 현재 값 | 귀속 세기 |
|---|---|---|
| 레온하르트 오일러 | `concept-fundamental-theorem`, `concept-integration` | 둘 다 **17세기** |
| 조제프루이 라그랑주 | `concept-differentiation`, `-advanced` | **17세기** |
| 피에르시몽 라플라스 | `concept-integration` | **17세기** |

18세기 개념 문서가 0편이라 벌어진 일이다. **오일러의 기여개념에 무한급수가 없다.**
세기 개관 §3 이 "바젤 문제", §4 가 "무한급수를 자유롭게 더하고 자르고 순서를 바꿨다"고
써 놓고도 가리킬 문서가 없어 링크가 안 걸렸다.

### (2) 참조되지만 없는 슬러그 5종

`선행개념`·`후속개념`·`기여개념`에 적혀 있으나 문서가 없는 것 — dangling 이다.

| 슬러그 | 예약 | 예약한 문서 |
|---|---|---|
| `concept-analysis-rigor` | **4회** | `concept-analytic-geometry-advanced`, `concept-fundamental-theorem`, `concept-integration`, `concept-integration-advanced` |
| `concept-algebra-notation` | 1회 | `concept-analytic-geometry`(선행개념) |
| `person-huygens` · `person-jakob-bernoulli` | 각 1회 | `concept-probability`(기여인물) — **인물 노트 예약** |
| `concept-real-analysis` | **2회** | `concept-differentiation-advanced`(후속개념), `concept-probability-advanced`(후속개념) |
| `concept-logarithm` | 1회 | `person-napier`(기여개념) |

이 5종은 **위키링크 백로그에 잡히지 않는다.** 본문 `[[…]]` 가 아니라 frontmatter 필드라
`verify_math.py --backlog` 의 시야 밖이다. 그래서 여기 적어 둔다.

### (3) 백로그 3종은 새 문서 대상이 아니다

`--backlog` 가 뽑아 주는 3종을 참조 문맥까지 읽어 보면 셋 다 **문서를 쓸 일이 아니라
링크를 고칠 일**이었다. §7 에서 다룬다.

---

## 2. 현재 개념 문서 (13편)

| 슬러그 | 트랙 | 짝문서 | 세기 |
|---|---|---|---|
| `concept-analytic-geometry` | 이해편 | 있음 | 17 |
| `concept-analytic-geometry-advanced` | 확장편 | 있음 | 17, 19, 20 |
| `concept-differentiation` | 이해편 | 있음 | 17 |
| `concept-differentiation-advanced` | 확장편 | 있음 | 17, 18, 19, 20 |
| `concept-integration` | 이해편 | 있음 | 17 |
| `concept-integration-advanced` | 확장편 | 있음 | 19, 20 |
| `concept-fundamental-theorem` | 이해편 | **없음** | 17 |
| `concept-series` | 이해편 | 있음 | 14, 17, 18, 19 |
| `concept-series-advanced` | 확장편 | 있음 | 18, 19, 20 |
| `concept-probability` | 이해편 | 있음 | 17, 18 |
| `concept-probability-advanced` | 확장편 | 있음 | 17, 18, 19, 20 |
| `concept-differential-equation` | 이해편 | 있음 | 17, 18 |
| `concept-differential-equation-advanced` | 확장편 | 있음 | 18, 19, 20 |

처음 7편은 전부 17세기에서 출발했다. **18세기 단계에서 4편(무한급수·확률론 투트랙)이 더해지면서
14세기 밴드가 새로 생겼고**(마다바·오렘), 18세기 인물들이 자기 세기의 개념을 가리키게 됐다.

`concept-fundamental-theorem` 이 **단일 이해편의 선례**다 — 짝문서 없이 게이트를 통과한다.

---

## 3. 개념 계보 — 세기별 배치

| 세기 | 개념 문서 | 상태 |
|---|---|---|
| 16 | `concept-algebra-notation` 대수 기호법 | 예약(1회) · 미착수 |
| 17 | 해석기하 · 미분법 · 적분법 · 미적분학의 기본정리 | **완료** |
| 17 | `concept-logarithm` 로그 | 예약(1회) · 미착수 |
| **18** | ~~무한급수~~ · ~~확률론~~ · ~~미분방정식~~ · ~~복소수~~ · ~~변분법~~ · ~~그래프이론~~ · 함수 개념 | **이번 단계 (6/7 완료)** |
| 19 | `concept-analysis-rigor` 해석학의 엄밀화 | 예약(**4회**) · 18세기 다음 |
| 19~20 | `concept-real-analysis` 실해석 | 예약(**2회**) · 엄밀화 다음 |

18세기 다음은 논쟁의 여지가 없다 — **`concept-analysis-rigor` 가 4회 예약으로 가장 무겁고**,
세기 개관 §12 가 "무한소를 무엇으로 대체할 것인가 — 코시와 바이어슈트라스가 답한다"로
숙제를 명시적으로 넘겨 놓았다.

---

## 4. 18세기 집필 순서

7주제 · 문서 11편(투트랙 4 × 2 + 단일 3).

### 1 — 무한급수 `concept-series` · 투트랙

- **세기** 17, 18, 19 · **교과** 미적분(수열의 극한·급수) · **난이도** 표준
- **기여인물** `person-gregory`, `person-leibniz`, `person-euler`
- **선행** `concept-integration` · **후속** `concept-analysis-rigor`
- **왜 1번인가** 18세기의 심장이다. 세기 개관 §3(바젤 문제)·§4(자유로운 조작)·연표
  1735·1748 이 전부 이 문서를 가리켜야 하는데 가리킬 것이 없다. 오일러의 `기여개념`
  구멍을 메우는 것도 이 문서다.
- **트레이드오프** dangling 압력만 보면 확률론(3회 예약)이 먼저다. 그런데 확률론은
  뿌리가 17세기라 "18세기를 채운다"는 이번 단계의 목적에는 무한급수가 맞다. 확률론을
  2번에 붙여 곧바로 해소한다.
- **역할 분담** 이해편 = 무한히 더한다는 것이 왜 말이 되는가 · 기하급수 · 바젤 문제 ·
  수렴의 직관. 확장편 = 발산급수에 값을 매긴 오일러의 형식적 조작 · 오일러 곱셈공식과
  제타 · **해석적 정수론을 여기에 흡수한다**(세기 개관 §6 표의 항목이지만 별도 문서로
  세우면 무한급수 확장편과 내용이 겹친다) · 수렴 판정의 부재가 19세기에 어떻게 청구되는가.

### 2 — 확률론 `concept-probability` · 투트랙

- **세기** 17, 18, 19 · **교과** 확률과 통계 · **난이도** 표준
- **기여인물** `person-pascal`, `person-fermat`, `person-laplace`
- **왜 2번인가** **예약 3회를 한 번에 해소한다.** 파스칼·페르마·라플라스 세 인물의
  `기여개념` 과 `episode-pascal-fermat-letters` 의 `관련개념` 이 모두 이 슬러그를 기다린다.
- **역할 분담** 이해편 = 도박판의 배당 문제에서 출발해 기댓값까지(17세기 중심).
  확장편 = 세기 개관 §6 이 서술한 이행 — 보험·연금, 관측 오차, 인구 통계, 라플라스의
  종합. "확률은 무지의 척도"라는 그의 입장까지.

### 3 — 미분방정식 `concept-differential-equation` · 투트랙

- **세기** 17, 18, 19 · **교과** — · **난이도** 심화
- **기여인물** `person-euler`, `person-lagrange`, `person-laplace`
- **왜 3번인가** 세기 개관 §5(라그랑주 『해석 역학』)와 §11("자연은 미분방정식으로 쓴다는
  관행")이 이 문서를 전제로 쓰여 있다. 백로그의 `[[미분의 응용]]` 도 상당 부분 여기로 흡수된다.
- **역할 분담** 이해편 = 변화율로 쓴 식을 푼다는 것이 무슨 뜻인가 · 변수분리 · 현진동.
  확장편 = 현진동 논쟁(달랑베르·오일러·베르누이)이 함수 개념을 어떻게 흔들었는가 ·
  오일러-라그랑주 방정식 · 라플라스 변환의 원형.

### 4 — 복소수 `concept-complex-number` · 투트랙 ✅

- **세기** 16, 18, 19 · **교과** 고등학교 수학(복소수) · **난이도** 표준
- **기여인물** `person-euler`
- **왜 4번인가** 무한급수 이해편이 깔린 뒤라야 오일러 공식을 급수로 설명할 수 있다.
  고교 교과 연결이 가장 강한 편이라 "내일 수업" 필터에서 값이 크다.
- **역할 분담** 이해편 = 없는 수를 왜 만들었나(3차방정식의 우회로) · 복소평면 · 오일러 공식.
  확장편 = 대수학의 기본정리 · 복소해석의 문턱까지.

### 5 — 변분법 `concept-calculus-of-variations` · 단일 이해편 ✅

- **세기** 18 · **교과** — · **난이도** 심화 · **기여인물** `person-euler`, `person-lagrange`
- **왜 단일인가** §6 판정 기준에 따라 — 고교 교과 접점이 없어 이해편의 "쉬운 진입" 층이
  얇고, 확장편으로 나눌 만큼 두 층의 독자가 갈리지 않는다.
- **내용** 최단 강하선 문제 · 오일러-라그랑주 방정식 · 최소작용 원리로 이어지는 길.

### 6 — 그래프이론의 탄생 `concept-graph-theory` · 단일 이해편 ✅

- **세기** 18 · **교과** — · **난이도** 개관 · **기여인물** `person-euler`
- **왜 6번인가** 세기 개관 §13 이 **수업에서 쓸 만한 대목**으로 직접 지목한 주제다
  ("종이와 연필만으로 되고, '버려서 얻는다'를 체감시킨다"). 활용도는 높지만 개념
  계보상 다른 문서의 선행이 아니라 순서를 뒤로 둔다.
- **주의** 쾨니히스베르크 다리를 **일화로 쓰고 싶은 유혹**이 있다. 하지만 세기 개관 §6 은
  이것을 "분야가 열린 사건"으로 분류했고, 일화 상한 2,500자로는 "재는 것을 그만두자 새
  수학이 나왔다"를 설명할 수 없다. **개념으로 쓰고, 원한다면 일화를 따로 붙인다.**

### 7 — 함수 개념 `concept-function` · 단일 이해편

- **세기** 17, 18, 19 · **교과** 공통수학(함수) · **난이도** 표준 · **기여인물** `person-euler`
- **왜 마지막인가** 세기 개관 §4 의 핵심 논거 — "18세기가 다룬 함수가 얌전했기 때문에
  형식적 조작이 통했고, **19세기에 함수 개념이 넓어지고 나서** 문제가 터졌다" — 를
  담는 문서다. 미분방정식(3번)의 현진동 논쟁이 깔려야 이 이야기가 성립한다.
- **다리 역할** 이 문서의 후속개념이 `concept-analysis-rigor` 다. 18세기를 닫고 19세기를 연다.

---

## 5. 착수 순서에 따른 누적 효과

| 편 | 해소되는 dangling | 복구되는 `기여개념` |
|---|---|---|
| 1 무한급수 ✅ | — (새 링크를 심었다) | 오일러 · 그레고리 · 라이프니츠 |
| 2 확률론 ✅ | **`concept-probability` 3회 — 해소됨** | 파스칼 · 페르마 · 라플라스 |
| 3 미분방정식 ✅ | **`[[미분의 응용]]` 2회 — 해소됨** | 오일러 · 라그랑주 · 라플라스 · 뉴턴 · 라이프니츠 |
| 4 복소수 ✅ | — | 오일러 |
| 5 변분법 ✅ | — | 오일러 · 라그랑주 |
| 6 그래프이론 ✅ | — | 오일러 |
| 7 함수 개념 | — | 오일러 |

7편을 마치면 18세기 인물 3명 전원이 **자기 세기의 개념 문서**를 가리키게 된다.

---

## 6. 트랙 판정 기준 — 투트랙인가 단일 이해편인가

원칙은 투트랙이다(`LESSONS.md` 2026-06-21 확정). 단일은 예외이고, 예외에는 기준이 필요하다.

`concept-fundamental-theorem` 이 단일로 간 이유를 일반화하면 이렇다.

**단일 이해편으로 쓴다** — 아래를 **모두** 만족할 때.
- 고교 교과 접점이 없거나 얇아, 이해편의 "쉬운 진입" 층이 두꺼워지지 않는다.
- 확장편에 넣을 내용이 다른 문서의 확장편과 겹친다.
- 주제 자체가 단일 사건·단일 정리에 가까워 두 문서로 나누면 양쪽 다 게이트에 미달한다.

**투트랙으로 쓴다** — 하나라도 걸리면.
- 교과 단원이 있다(= 중·고 독자가 실재한다).
- 18세기의 사용법과 19세기의 근거가 갈린다(= 시대별로 할 말이 다르다).
- 인물이 셋 이상 기여했다(= 서사가 하나로 안 묶인다).

판정을 뒤집을 때는 **여기에 근거를 남긴다.** 게이트를 피하려고 단일로 내리는 것을 막는 장치다.

---

## 7. 백로그 3종 처리 방침 — 셋 다 새 문서가 아니다

`verify_math.py --backlog` 가 뽑는 3종을 **참조 문맥까지 읽고** 판정했다.

| 대상 | 참조 문맥 | 판정 | 조치 |
|---|---|---|---|
| **무한소** (1회) | `concept-differentiation` — "페르마 자신은 [[무한소]]가 무엇인지 정의하지 않았다 … 이 미해결 상태가 이후 150년을 간다" | **이미 다뤄지고 있다.** 세기 개관 §4·§12 가 이 주제를 `[[미분법 확장편]]` 으로 명시적으로 넘긴다. 별도 문서는 중복이다 | `link-aliases.json` 에 `"무한소": "concept-differentiation-advanced"` |
| **부분적분** (2회) | `concept-integration`/`-advanced` — "[[부분적분]]과 치환적분은 새로운 발명이 아니라 미분 규칙을 거꾸로 읽은 것" | **그 문장이 이미 설명을 끝냈다.** 문서 한 편을 세울 사적(史的) 사건이 없다 | 위키링크를 걷어내고 평문으로 두거나, `concept-integration-advanced` 로 alias |
| ~~**미분의 응용** (2회)~~ | `concept-differentiation` — "최적화, 근사, 미분방정식 — 는 [[미분의 응용]] 쪽에서 다룬다" | **교과 단원명이지 수학사 주제가 아니다** | **완료(2026-08-24)** — 두 문장을 고쳐 미분방정식은 `[[미분방정식]]`, 최적화·근사는 `[[미분법 확장편]]` 으로 갈랐다 |

**교훈**: 백로그는 "쓸 거리"의 후보일 뿐 지시가 아니다. 참조 문맥을 읽지 않고 백로그를
그대로 집필 목록으로 삼으면, **이미 다른 문서가 다룬 주제를 한 편 더 쓰게 된다.**

---

## 8. 착수할 때마다 하는 부수작업

개념 문서 한 편은 자기 파일만 쓰고 끝나지 않는다. 빠뜨리면 링크가 한쪽으로만 걸린다.

- [ ] **세기 개관에 위키링크 심기** — `century-18c` 본문에서 그 개념을 언급한 자리에 `[[…]]` 를 건다. 지금은 링크할 곳이 없어 평문으로 적혀 있다.
- [ ] **인물 노트 `기여개념` 갱신** — 해당 인물 frontmatter 에 새 슬러그를 넣는다. §5 표 참조.
- [ ] **선행/후속개념 양방향 갱신** — 새 문서의 `선행개념` 에 A 를 적었으면 A 의 `후속개념` 에도 새 슬러그를 적는다. 한쪽만 적으면 역링크 패널이 비대칭이 된다.
- [ ] **짝문서 상호 지정** — 투트랙이면 양쪽 `짝문서` 를 서로 가리키게 한다.
- [ ] `python3 tools/verify_math.py` — 스키마·분량 게이트
- [ ] `python3 tools/build_math_manifest.py` → `python3 tools/build_link_index.py` (**순서 고정**)
- [ ] `python3 tools/sync_math_obsidian.py --dry-run` → 실행
- [ ] 검수는 **별도 패스**. 작성한 컨텍스트에서 self-approve 하지 않는다.
- [ ] PR 본문에 **측정값 표** 첨부(자수·줄·절·시각화·출처·게이트 통과 여부)

---

## 8-1. 집필하며 지도에서 달라진 것 (2026-08-25)

지도가 §9 에서 요구한 대로, 벗어난 지점을 적어 둔다.

**4 복소수** — 지도는 이해편 내용으로 "3차방정식의 우회로 · 복소평면 · 오일러 공식"을
적었는데, 집필 중 **극형식·드무아브르·단위근**이 빠져 있다는 것이 드러나 확장편 §2-1 로
넣었다. 이해편의 "곱셈은 회전"을 대수로 잇는 고리라 없으면 두 편이 끊긴다.

**5 변분법** — `concept-differential-equation-advanced` 가 오일러–라그랑주 방정식을
이미 깊이 다루고 있어(1755년 편지·1766년 명명·라그랑주 역학), 이 노트는 **방정식이 아니라
문제 유형과 그 이야기**를 맡도록 범위를 좁혔다. 도구는 저쪽, 문제는 이쪽이다.

**6 그래프이론** — 지도의 "일화로 쓰지 말 것" 경고가 정확했다. 다만 집필 중
**라이프니츠가 '위치의 기하학'이라는 이름을 먼저 지어 두었다**는 사실과
**충분조건이 1873년 히어홀처에게서야 왔다**(137년 뒤)는 사실이 나와, 지도가 예상한
분량보다 내용이 두꺼워졌다.

**세 편 공통** — 인용을 쓰기 전에 원문 확인이 안 되면 넣지 않았다. 뺀 것 두 가지는
라플라스 일화의 허셜 목격 기록(복소수 아님, `person-laplace`)과 변분법의
"ex ungue leonem" 이다. 국내 자료의 연도 오기(변분법 1689 → 1696)는 본문에 밝혔다.

---

## 9. 이 지도를 고칠 때

집필하다 보면 지도가 틀렸다는 것을 알게 된다. 그때 **조용히 벗어나지 말고 이 파일을 고친다.**
특히 다음은 반드시 기록한다.

- 트랙 판정을 뒤집었을 때(투트랙 → 단일, 또는 반대) — §6 기준의 어느 항목이 갈랐는지
- 주제를 합치거나 쪼갰을 때 — 왜 겹쳤는지/왜 한 편에 안 들어갔는지
- 순서를 바꿨을 때 — 어떤 의존을 놓쳤는지

이력이 남아야 다음 세기 지도를 짤 때 같은 판단을 다시 하지 않는다.

---

## 10. 이력

지도에서 벗어나거나 지도가 틀린 것을 알게 된 경우를 여기 적는다(§9의 규칙).

### 2026-08-24 · 1번 무한급수 완료

- **해석적 정수론을 별도 문서로 세우지 않고 확장편에 흡수했다** — 계획대로다(§4 1번).
- 세기 필드가 계획(17·18·19)과 달라졌다. 발전단계에 오렘·마다바(14세기)가 들어가면서
  이해편이 `[14, 17, 18, 19]` 가 됐다. **세기 인덱스가 합집합이라는 설계가 의도대로 작동한 결과**이므로
  계획을 고친 것이 아니라 계획이 미처 계산하지 않은 것이다.
- 백로그 `해석학의 엄밀화` 가 2회로 새로 잡혔다. §3이 예약한 `concept-analysis-rigor` 와 같은 대상이다.

### 2026-08-24 · 2번 확률론 완료

- **예약 3회가 실제로 해소됐다.** 파스칼·페르마의 `기여개념` 과 파스칼–페르마 서신의 `관련개념` 이
  이미 이 슬러그를 가리키고 있었고, 문서가 생기자 링크가 그대로 살아났다. §1(2)의 진단이 맞았다.
- **인물 노트 예약이 둘 늘었다** — `person-huygens`·`person-jakob-bernoulli`.
  이해편 §4·§6과 발전단계의 절반이 두 사람이라 `기여인물` 에 넣었고, 문서는 아직 없다.
  17세기 인물 서가를 보강할 때 우선 대상이다.
- 확장편이 `후속개념: [concept-real-analysis]` 를 걸어 그 슬러그의 예약이 1회 → 2회가 됐다.
- **부채 하나를 적어 둔다 — 18~19세기 도해가 없다.** `math/assets/figures/` 의 3개는 전부 17세기 미적분 것이다.
  특히 확장편 §7의 논지("띄엄띄엄한 것을 세는데 답이 연속 곡선으로 나온다")는 문장보다 그림이 빠른 종류다.
  이항→정규 근사 SVG 한 장, 최소제곱 잔차 그림 한 장이 다음 회차의 대상이다(M3).
- **계획과 달라진 것**: §4 2번은 이해편 세기를 17·18·19로 잡았으나 실제로는 `[17, 18]` 이다.
  19세기 내용(최소제곱·정규분포·역확률의 확산)이 전부 확장편으로 갔기 때문이며,
  역할 분담이 계획보다 선명해진 결과라 그대로 둔다.

### 2026-08-24 · 3번 미분방정식 완료

- **백로그 `[[미분의 응용]]` 2회가 계획대로 해소됐다.** §7의 방침(새 문서를 쓰지 않고 문장을 고쳐
  실제 대상으로 링크)을 그대로 실행했다.
  **개념 백로그**에 남은 것은 `부분적분`·`무한소`·`해석학의 엄밀화` 셋이고,
  확률론·미분방정식 회차가 심은 **인물 백로그**가 따로 있다. 합쳐서 세는 수치는 `--backlog` 로 확인할 것.
- **`person-euler` 에 미분방정식 언급이 0건이었다.** 이 분야의 중심 인물인데 인물 노트가 비어 있었고,
  이번에 `기여개념` 으로 연결했다. 인물 노트 본문 보강은 하지 않았다 — 다음 인물 회차 대상이다.
- **집필 중 무인용 절을 스스로 잡았다.** LESSONS 2026-08-24(확률론) 규칙대로 절별 `[n]` 개수를 세었더니
  확장편 §6(다니엘 베르누이)이 0이었다. 확인해 보니 "달랑베르와 오일러가 그 주장을 받아들이지 않았다"는
  서술에 근거가 없어, 출처가 실제로 말하는 범위로 다시 썼다. **검수 전에 작성자가 잡은 첫 사례다.**
- **계획과 달라진 것**: §4 3번은 확장편에 "라플라스 변환의 원형"을 넣기로 했으나, 원형의 역사적 경위를
  뒷받침할 출처를 찾지 못해 **변환이 무엇을 하는지**(선형 ODE를 대수방정식으로)로 한정했다.
  역사 서술 대신 방법 서술로 바꾼 것이며, 출처가 말하는 범위를 넘지 않기 위한 선택이다.
- **계획과 달라진 것 (2)**: §4-3 세기는 17·18·19로 잡았으나 확장편이 **20**을 더했다
  (로렌츠의 나비효과와 약해 개념). 확장편만의 확장이고 이해편은 계획대로 17·18이다.
- **5번 변분법과 겹친다.** 확장편 §10이 오일러-라그랑주·등시곡선·최소작용을 이미 담았고,
  별칭 `오일러-라그랑주 방정식` 도 이 문서가 가져갔다(link-index 확인).
  **5번을 쓸 때 범위를 다시 잡거나 별칭을 넘겨야 한다.** 지금 정하지 않고 남겨 둔다.
- 본문에 인물 위키링크를 심어 **인물 백로그가 늘었다**. 도해 부채는 그대로다(18~19세기 0장).

---

## 11. 인계 — 다른 자리에서 이어받을 때

이 절은 **이 대화 기록 없이, 다른 PC에서도** 바로 이어받기 위한 것이다.
§1~§9가 *무엇을 쓸지*를, §10이 *무엇을 했는지*를 맡고, 여기는 **지금 어디에 서 있는가**와 **어떻게 시작하는가**를 맡는다.

### 지금 상태

- **18세기 3/7 완료** — 무한급수 · 확률론 · 미분방정식(각 투트랙, 6편)
- `math/` 전체 **30편**, `verify_math.py` **0편 실패**
- 라이브: https://irun20000-eng.github.io/Search/math/
- **리포가 정본이다.** 옵시디언 볼트는 사본이고 `sync_math_obsidian.py` 가 만든다.

### 다른 PC에서 시작하기

```bash
git clone https://github.com/irun20000-eng/Search.git
cd Search
```

그다음 네 가지를 맞춰야 한다. **이걸 안 하면 반드시 막힌다.**

| 항목 | 명령 / 값 | 안 하면 |
|---|---|---|
| **git identity** | `git config user.name "Jang Unkyu"`<br>`git config user.email "252165118+irun20000-eng@users.noreply.github.com"` | 푸시가 **email privacy 로 거부**된다(GH007) |
| **gh 인증** | `gh auth login` (scope: repo, workflow) | PR·머지·Pages 조회 불가 |
| **파이썬 도구** | `pip install pyyaml pypdf` | 검증기·PDF 출처 대조 불가 |
| **콘솔 인코딩** | 모든 python 실행 앞에 `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` | 윈도우 cp949 에서 **UnicodeEncodeError** |

볼트 경로는 로컬마다 다르다. `G:\내 드라이브\00_Obsidian_Second Brain\...` 가 없으면
`sync_math_obsidian.py` 는 건너뛰고 나머지 절차만 돌리면 된다 — **리포가 정본이므로 볼트는 나중에 맞춰도 된다.**

### 다음에 할 것 — 4번 복소수

§4의 4번 항목이 그대로 다음 대상이다.

- **선행 조건이 충족됐다.** 4번은 "무한급수 이해편이 깔린 뒤라야 오일러 공식을 급수로 설명할 수 있다"는
  이유로 이 자리에 놓였고, 1번이 완료돼 그 조건이 섰다.
- **교과 연결이 이번 단계에서 가장 강하다**(고등학교 수학, 복소수).
- 세기 개관 §3의 $e^{i\theta}=\cos\theta+i\sin\theta$ 가 아직 아무 문서도 가리키지 않는다. 링크를 심을 자리다.

### 착수 전 필수 (preflight)

1. `MATH_PIPELINE.md` — 절차 M1~M6
2. **`LESSONS.md` 의 2026-08-24 항목 셋** — 최근 세 회차에서 실제로 걸린 것들이다
   - 무한급수: **인용이 출처와 어긋남**(검색 요약을 원문으로 착각)
   - 확률론: **인용이 아예 없음**(게이트가 못 잡는다)
   - 미분방정식: **출처가 유보한 자리를 단정으로 옮김**
3. 이 절과 §4의 해당 항목

### 자주 쓰는 명령

```bash
# 게이트 (환경변수 필수)
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python tools/verify_math.py
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python tools/verify_math.py --backlog

# 무인용 절 찾기 — 게이트가 못 잡는 것. ### 소절까지 내려서 센다
awk 'c==2{b=1} /^---$/{c++} b' math/notes/<슬러그>/note.md \
 | awk '/^#{2,3} /{if(s!="")printf "%3d  %s\n",n,s; s=$0; n=0; next} \
        {n+=gsub(/\[[0-9]+\]/,"&")} END{if(s!="")printf "%3d  %s\n",n,s}'

# 빌드 (순서 고정: manifest → link-index)
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python tools/build_math_manifest.py
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python tools/build_link_index.py

# 볼트 동기화 (dry-run 먼저)
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python tools/sync_math_obsidian.py --dry-run

# 네이버 지식백과 원문 열기 — -L 과 브라우저 UA 가 둘 다 필요하다
curl -s -L -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
  "https://terms.naver.com/entry.naver?docId=<ID>"
#  UA 없으면 403 · UA 만 있고 -L 없으면 301(0 bytes) · 둘 다면 200
#  본문은 id="size_ct" 이후를 태그 제거해 읽는다. 맨 위 'AI 요약' 블록은 본문이 아니다

# PDF 출처 대조 (WebFetch 는 PDF 를 못 읽는다)
python -c "import pypdf;print('\n'.join((p.extract_text() or '') for p in pypdf.PdfReader('x.pdf').pages))"

# 배포 확인
gh api repos/irun20000-eng/Search/pages/builds/latest --jq '{status, commit:.commit[0:7], error:.error.message}'
curl -s https://irun20000-eng.github.io/Search/math/manifest.json | python -c "import json,sys;print(len(json.load(sys.stdin)['notes']))"
```

### 갚아야 할 부채

| 부채 | 내용 |
|---|---|
| **도해 0장** | `math/assets/figures/` 의 3개가 전부 17세기 미적분 것이다. 18~19세기가 비어 있다. 이항→정규 근사, 최소제곱 잔차, 파동방정식이 대상(M3) |
| **인물 노트 예약** | 확률론·미분방정식 회차가 본문에 심은 인물 링크들. `--backlog` 가 참조 횟수순으로 뽑아 준다 |
| **`person-euler` 본문** | 미분방정식 언급이 0건이다. `기여개념` 으로만 연결했고 본문 보강은 안 했다 |
| **`person-laplace` 무인용** | "확률은 무지의 척도"류 문장이 무인용으로 남아 있다. 확률론 회차에서 발견했으나 손대지 않았다 |
| **5번 변분법 범위** | 미분방정식 확장편 §10과 겹치고 별칭도 그쪽이 가져갔다(§10 이력 참조) |
| **개념 백로그 3종** | `해석학의 엄밀화`(문서를 써야 함) · `부분적분`·`무한소`(§7 방침대로 링크 처리) |

### 18세기 다음

`concept-analysis-rigor` 가 **frontmatter 4회 + 본문 2회**로 가장 무겁게 예약돼 있다.
18세기 7편을 마치면 이것이 다음이고, 세기 개관 §12가
"무한소를 무엇으로 대체할 것인가 — 코시와 바이어슈트라스가 답한다"로 숙제를 명시해 뒀다.
