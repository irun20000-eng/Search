---
title: n8n으로 첫 자동화 워크플로우 만들기
date: 2026-08-08
level: 입문
minutes: 40
tags: [n8n, 자동화, 노코드, 워크플로우, 트리거, 노드, JSON, 스케줄]
prereq: [이메일 계정, 웹 브라우저]
videos: [JJTowW0N-fo, 4cQWJViybAQ, y9u1IdDYHZQ, q6iuH_oxqHs]
---

# n8n으로 첫 자동화 워크플로우 만들기

> 이 가이드는 n8n을 한 번도 안 써 본 사람이 계정 만들기부터 **직접 돌아가는 워크플로우 하나**를 완성하기까지를 따라가게 만든 것이다. 약 40분.

## 이 가이드로 만들 것

**"정해진 시간마다 자동으로 실행돼서, 데이터를 가져와 원하는 모양으로 바꾸는"** 워크플로우 하나를 만든다.

구조는 세 덩어리다. 이 세 덩어리는 앞으로 만들 거의 모든 자동화의 뼈대가 된다.

```
[트리거]  →  [가공]  →  [출력]
언제 돌지    데이터를    어디에
정한다       손본다      내보낼지
```

이 가이드를 끝내면 다음을 할 수 있다.

- 트리거를 골라 워크플로우를 **원하는 시점에** 돌린다
- 노드 사이를 흐르는 **데이터 모양(JSON)** 을 읽는다
- 실행 결과를 보고 **어디서 틀어졌는지** 짚는다

반대로 이 가이드가 다루지 않는 것: AI 에이전트 노드, 외부 서비스 인증(OAuth) 연결, 자체 서버 설치. 그건 아래 "다음으로 할 것"에서 이어진다.

## 사전 준비물

| 항목 | 설명 |
|---|---|
| 이메일 계정 | n8n Cloud 가입용 |
| 웹 브라우저 | 크롬·엣지·사파리 아무거나 |
| 시간 | 약 40분 |

설치할 것은 없다. **n8n Cloud로 시작하는 것을 권한다.** n8n Cloud는 n8n 쪽에서 인프라를 전부 관리해 주는 완전 관리형 서비스이고, 공식 빠른 시작 문서도 처음에는 Cloud를 권장한다[1]. 신규 사용자에게는 무료 체험이 제공되며, Starter·Pro 플랜 기준 14일이다[1].

자체 서버(셀프 호스팅) 설치는 도커 등을 다뤄야 해서 첫 단추로는 부담이 크다[5]. 지금은 **자동화 개념을 익히는 게 목적**이니 설치는 나중으로 미룬다.

## 1단계 — 계정 만들고 캔버스 열기

**할 일**
1. n8n Cloud에 가입한다. 이메일 인증을 마치면 작업 공간이 만들어진다.
2. 새 워크플로우를 만든다. 빈 캔버스가 열린다.

**화면에서 확인할 것**
가운데가 텅 빈 격자무늬 캔버스이고, 노드를 추가하는 **+ 버튼**이 캔버스 중앙 또는 왼쪽 상단에 보인다[1]. 이 + 가 모든 것의 시작점이다.

**막히면**
가입 직후 작업 공간 준비에 몇십 초 걸릴 수 있다. 화면이 비어 보이면 새로고침한다.

> **버전에 따라 버튼 위치·이름이 다를 수 있다.** n8n은 업데이트가 잦다. 이 가이드는 "무엇을 하는 버튼인지"를 기준으로 썼으니, 라벨이 조금 달라도 역할이 같은 것을 찾으면 된다.

## 2단계 — ★트리거 고르기 (가장 중요한 선택)★

**할 일**
+ 를 눌러 첫 노드를 추가한다. 첫 노드는 반드시 **트리거**여야 한다.

트리거는 워크플로우의 시작 노드로, **언제 그리고 어떻게** 워크플로우가 실행될지를 정의한다[1]. 종류는 크게 셋이다[1][2].

| 트리거 | 언제 도는가 | 첫 연습에 |
|---|---|---|
| **수동 실행** | 내가 버튼을 누를 때 | ◎ 가장 적합 |
| **스케줄** | 매일 09:00처럼 정해진 시각 | ○ 2단계로 |
| **앱 이벤트** | 예: Airtable에 행이 추가될 때[1] | △ 인증 필요 |

**지금은 수동 실행 트리거를 고른다.** 이유는 명확하다 — 스케줄로 걸어 두면 결과를 보려고 기다려야 하지만, 수동은 누를 때마다 즉시 돈다. **배우는 동안에는 피드백이 빠른 쪽이 압도적으로 유리하다.**

**화면에서 확인할 것**
캔버스에 노드 하나가 놓이고, 오른쪽에 실행 버튼이 생긴다.

**막히면**
트리거가 아닌 일반 노드를 먼저 놓으면 워크플로우가 실행되지 않는다. 첫 노드가 트리거인지부터 확인한다.

## 3단계 — ★데이터의 모양을 눈으로 보기★

여기가 이 가이드에서 가장 중요한 단계다. **대부분의 초보자가 여기를 건너뛰고 나중에 막힌다.**

**할 일**
1. 트리거 뒤에 데이터를 만들어 내는 노드를 하나 붙인다. 값을 직접 지정하는 노드(Set/Edit Fields 계열)가 연습에 좋다.
2. 필드를 두어 개 만든다. 예: `name` = `홍길동`, `score` = `90`.
3. 워크플로우를 실행한다.

**화면에서 확인할 것**
노드를 클릭하면 **입력(왼쪽) / 출력(오른쪽)** 패널이 보인다. 출력 쪽에 방금 만든 값이 나타나고, 표 보기와 JSON 보기를 전환할 수 있다.

**JSON 보기로 한 번 바꿔서 꼭 확인해라.** 대략 이런 모양이다.

```json
[
  { "name": "홍길동", "score": 90 }
]
```

바깥이 **대괄호(배열)** 라는 점이 핵심이다. n8n에서 노드끼리 주고받는 단위는 값 하나가 아니라 **아이템의 배열**이다. 아이템이 3개면 뒤 노드가 3번 도는 식이다.

**왜 이걸 먼저 보게 하는가** — 나중에 만나는 에러 대부분은 AI나 복잡한 기능 때문이 아니라 **데이터 모양이 안 맞아서** 생긴다. 실제로 n8n을 오래 다룬 쪽에서도 시간을 잡아먹는 진짜 문제는 AI가 아니라 데이터를 넣고 꺼내고 모양을 바꾸는 "주변부"라고 짚는다(아래 관련 영상 3번).

**막히면**
출력 패널이 비어 있으면 아직 실행하지 않은 것이다. 실행부터 한다.

## 4단계 — 노드 이어 붙이고 값 가져다 쓰기

**할 일**
1. 노드를 하나 더 붙인다.
2. 새 노드의 입력값에 **앞 노드의 결과를 끌어다 쓴다.** 왼쪽 입력 패널의 필드를 오른쪽 입력칸으로 드래그하면 참조 식이 자동으로 들어간다.
3. 실행한다.

**화면에서 확인할 것**
직접 타이핑한 고정값이 아니라 **앞 노드에서 온 값**이 결과에 찍히는지 본다. 이게 되면 노드 연결의 기본을 익힌 것이다.

**막히면**
값이 안 나오고 참조식이 글자 그대로 보이면, 그 칸이 "고정값 모드"일 수 있다. 칸 옆의 고정/표현식 전환을 확인한다.

## 5단계 — 실행 기록으로 디버깅하기

**할 일**
일부러 틀리게 만들어 본다. 없는 필드 이름을 참조하도록 바꾸고 실행한다.

**화면에서 확인할 것**
- 실패한 노드에 **빨간 표시**가 뜬다
- 그 노드를 열면 입력에는 무엇이 들어왔고 출력이 왜 비었는지 보인다
- 실행 기록(Executions)에 이번 실행이 남는다

**이 습관 하나가 앞으로 시간을 가장 많이 아껴 준다.** 워크플로우가 안 돌 때 처음부터 다시 만들지 말고, **실패한 노드의 입력 패널부터 열어 본다.** 열에 아홉은 "들어온 데이터 모양이 예상과 다름"이다.

**막히면**
실행 기록을 지우지 마라. 실패한 실행은 나중에 다시 열어 볼 수 있다.

## 6단계 — 스케줄로 바꿔서 진짜 자동화 만들기

**할 일**
1. 트리거를 수동 실행에서 **스케줄**로 바꾼다.
2. 주기를 정한다. 연습이면 짧게(예: 5분마다) 두고 확인 후 늘린다.
3. 워크플로우를 **활성화(Active)** 한다.

**화면에서 확인할 것**
활성화 스위치가 켜져야 스케줄이 실제로 돈다. **꺼져 있으면 시간이 돼도 돌지 않는다.** 잠시 뒤 실행 기록에 자동 실행이 쌓이는지 확인한다.

**막히면**
- 활성화했는데 안 돈다 → 저장했는지 확인한다. 저장하지 않은 변경은 반영되지 않는다.
- 너무 자주 돈다 → 연습용 짧은 주기를 그대로 두면 실행 횟수를 금방 소진한다. 확인했으면 **바로 늘려라.**

여기까지 오면 세 덩어리(트리거 → 가공 → 출력)를 다 만져 본 것이다.

## 흔히 막히는 지점

**1. 첫 노드를 트리거로 안 놓음**
가장 흔하다. 워크플로우는 트리거에서 시작한다[1]. 일반 노드를 먼저 놓으면 실행 자체가 안 된다.

**2. 아이템이 여러 개인 걸 모르고 하나로 취급**
노드 간 전달 단위는 배열이다. 아이템이 3개면 뒤 노드가 3번 돈다. **"왜 3번 실행되지?"의 답은 대부분 이것이다.** 실행 전에 아이템 개수부터 확인하는 습관을 들인다.

**3. 활성화를 안 해서 스케줄이 안 돎**
저장 ≠ 활성화다. 둘 다 해야 한다.

**4. 템플릿을 그대로 가져와 쓰다 고치지 못함**
템플릿은 동작은 하지만 **왜 그렇게 생겼는지 모르면 고칠 수가 없다.** 예제로 시작하는 건 좋지만[6], 최소 한 번은 빈 캔버스에서 직접 조립해 봐야 한다.

**5. 연습용 짧은 주기를 방치**
5분마다 도는 워크플로우를 켜 두고 잊으면 실행 횟수가 순식간에 사라진다. 확인 즉시 주기를 늘리거나 비활성화한다.

## 다음으로 할 것

1. **외부 서비스 하나 연결하기** — 구글 시트나 슬랙 같은 걸 붙여 본다. 여기서 인증(자격 증명) 개념을 처음 만난다.
2. **분기 넣기** — 조건에 따라 다른 길로 보내는 IF 노드를 써 본다.
3. **AI 노드 붙이기** — 데이터 흐름이 손에 익은 다음에 붙여야 한다. 순서를 바꾸면 두 배로 헤맨다.
4. **자체 서버 설치 검토** — 데이터를 내부에 둬야 하거나 실행량이 많아지면 그때 고민한다[5].

## 관련 영상

아래 영상은 전부 YouTube API로 **실재·공개 여부를 확인**한 것이다. 제목·채널·길이는 확인 시점의 실제 값이다.

1. **초보자를 위한 n8n 튜토리얼 (2026) – 완전 가이드** — 첫 워크플로우를 단계별로 만드는 과정. 이 가이드와 짝으로 보기 좋다.
2. **n8n Quick Start Tutorial: Build Your First Workflow** — n8n 공식 채널. 영어지만 자동 자막으로 볼 만하고, 개념 뼈대가 정확하다.
3. **n8n 빨리 배우려면 가장 중요한데 다들 놓치는 기초만 눌러담았습니다** — 3단계(데이터 모양)를 더 파고들 때. "진짜 문제는 AI가 아니라 주변부"라는 관점.
4. **초보자도 쉽게 배우는 n8n 뉴스피드 자동화** — 6분짜리. 실제로 쓸모 있는 워크플로우를 짧게 끝까지 만들어 본다.

## 출처

**국내**
- [3] n8n 처음 시작하기 — n8nkorea · https://slashpage.com/n8n-korea/qrx6zk25zq8j4mv314y5
- [5] n8n 사용법 기초부터 완벽 설치까지 — ProfBear · https://profbear.kr/n8n-getting-started/
- [6] 2026년 n8n 워크플로우 예제로 배우는 노코드 자동화 A to Z — Blocktalker · https://blocktalker.co.kr/n8n-workflow-examples-2026-guide/
- [4] n8n 시작하기와 기본 사용법 — TILNOTE · https://tilnote.io/en/pages/691edb16109022b7785e6280

**해외·공식**
- [1] n8n 공식 기술 문서 한글판 — 빠른 시작 · https://n8n-docs.infograb.net/try-it-out/quickstart/ (트리거 정의·종류, Cloud 권장·무료 체험, 캔버스 + 버튼)
- [2] What is n8n? — Hostinger 튜토리얼 · https://www.hostinger.com/kr/tutorials/what-is-n8n (n8n의 정의와 노드 기반 자동화 개념)

> **확인 조건** — 이 가이드는 2026-08-08 기준 위 자료들의 교차 확인으로 작성했다. n8n은 업데이트가 잦아 **버튼 이름과 위치가 달라질 수 있다.** 화면이 다르면 "무엇을 하는 기능인지"를 기준으로 찾으면 된다. 셀프 호스팅 설치 절차는 이 가이드 범위 밖이라 다루지 않았다.
