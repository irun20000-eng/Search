# MATH_PIPELINE — 수학사 아카이브 운영 런북

`math/` 섹션의 정본 절차. 보고서는 `PIPELINE.md`, 영상·가이드는 `VIDEO_PIPELINE.md`를 따르고,
수학사는 이 문서를 따른다. 공통 규율(무환각·국내+해외 이중 출처·검수 별도 패스)은 그대로 승계한다.

---

## 0. 왜 별도 섹션인가

기존 세 갤러리는 전부 같은 데이터 모델이다 — *태그 달린 문서의 평면 목록*.
수학사는 **세기(시간축) × 개념(발전 계보) × 인물(엔티티)** 이 교차 참조되는 관계형 구조라
manifest에 카드를 얹는 것으로 성립하지 않는다. 그래서 `math/` 를 독립 섹션으로 둔다.
SPA 통합은 하지 않는다 — 공통 셸은 소비자가 넷이 된 뒤에 추출해야 맞는 모양이 나온다.

---

## 1. 문서 원자 4종

담기(장바구니) 기능이 성립하려면 조각이 작아야 한다.

| 유형 | 역할 | 트랙 |
|---|---|---|
| **세기** | 그 세기의 시대 이슈·역사적 사건과 수학의 접점. 해당 세기 원자들의 관문 | 단일 |
| **개념** | 개념 하나의 발생과 발전. `발전단계`를 품는다 | 이해편 + 확장편 |
| **인물** | 수학자 일대기 | 단일 |
| **일화** | 에피소드. **의도적으로 짧다(800~2,500자)** — 수업 도입 한 꼭지이자 재사용 단위 | 단일 |

일화에 상한을 두는 이유: 8,000자 일화는 아무도 담지 않는다. 짧아야 재사용된다.
다만 짧다고 검증이 느슨해지지는 않는다 — 출처 3개와 사실성 판정은 필수다.

---

## 2. 스키마

정본은 `tools/verify_math.py` 다. 문서로만 적힌 규칙은 지켜지지 않으므로 검증기가 막는다.

### 공통 (전부 필수는 아님 — ★가 필수)

```yaml
유형: 개념 | 인물 | 일화 | 세기      # ★
제목: 미분법                          # ★
슬러그: concept-differentiation       # ★ 폴더명과 같아야 한다
별칭: [미분, 도함수, 유율법]           # 위키링크 해석용
볼트파일명: 개념_미분법                # ★
세기: [17]                            # ★ 정수 배열
분야: [해석학]
교과: [미적분Ⅰ]                       # 교육과정 연결 — "내일 미적분 수업" 필터
난이도: 개관 | 표준 | 심화
요약: 한 줄                           # ★ 카드·담기 요약에 쓰인다
태그: []
이미지: [{파일:, 출처:, 라이선스:, 설명:}]   # 네 항목 모두 필수 (라이선스 미상은 게시 불가)
출처: [{번호:, 제목:, URL:, 유형: 1차|2차}]  # ★
갤러리URL: "#n=<슬러그>"
```

### 유형별

| 유형 | 추가 필드 |
|---|---|
| 개념 | `트랙`★ · `짝문서` · `발전단계[]`★ · `선행개념[]` · `후속개념[]` · `기여인물[]`★ |
| 인물 | `생몰{출생,사망,출생지}`★ · `기여개념[]` · `관련일화[]` |
| 일화 | `사실성`★ · `사실성근거`★ · `관련인물[]` · `관련개념[]` |
| 세기 | `연대{시작,끝}`★ · `시대이슈[]`★ · `세계사사건[{연도,사건,수학과의연결}]` |

### 발전단계 — 개념과 세기를 잇는 다리

```yaml
발전단계:
  - 세기: 17
    인물: 페르마
    변화: 미소량 e로 극대·극소와 접선을 함께 처리했다
    출처: 5          # 출처 목록의 번호. 없는 번호를 쓰면 검증 실패
```

자유 서술로 두면 세기 뷰가 긁어갈 수 없다. 구조화하면 개념 문서 하나가
여러 세기에 자동으로 나타난다. **세기 인덱스는 `세기` 필드와 `발전단계[].세기` 의 합집합**이다.

### 사실성 — 이 아카이브의 차별점

수학사 일화의 상당수가 후대 창작이다(가우스의 1~100 합산, 뉴턴의 사과, 데카르트와 파리).

```yaml
사실성: 확실 | 전승 | 창작의심
사실성근거: 판정 이유 1~2문장 (출처 번호 포함)
```

UI에 배지로 노출된다. 수업에서 잘못된 일화를 사실처럼 말하는 것을 막는 장치다.

---

## 3. 절차 M1~M6

### M1 수집
1차 사료 우선. 주장↔URL fact-ledger를 만든다. **국내(네이버 MCP) + 해외(WebSearch) 이중 출처.**
상충하면 한쪽을 고르지 말고 양쪽을 병기한다. 단일 출처 사실은 "한 자료에 따르면"으로 한정한다.

### M2 작성
frontmatter → 본문. 본문에 위키링크를 적극적으로 건다 —
**해석되지 않는 링크는 오류가 아니라 콘텐츠 백로그다.**

`[^라벨]` = 용어 주석, `[n]` = 출처. 별개 체계이며 섞지 않는다(기존 규약 승계).

### M3 시각화
개념 도해·그래프는 SVG로 직접 제작해 `math/assets/figures/` 에 둔다.
PD 초상은 위키미디어 커먼즈에서 확보해 `math/assets/portraits/` 에 두고
`이미지` 필드에 출처·라이선스를 반드시 적는다. 장변 1200px 상한, WebP 우선.

본문 마크다운의 이미지 경로는 **노트 폴더 기준**(`../../assets/figures/x.svg`),
frontmatter `이미지.파일`은 **math/ 기준**(`assets/figures/x.svg`)이다.

### M4 검증 — 별도 패스, self-approve 금지

```bash
python3 tools/verify_math.py                # 전체
python3 tools/verify_math.py <슬러그>        # 일부
python3 tools/verify_math.py --backlog      # 미해결 위키링크만
```

검증기가 막는 것: 스키마, 유형별 분량 게이트, 출처 번호 중복,
**본문 인용이 출처에 없음(dangling)**, 발전단계의 출처 번호 존재,
이미지 라이선스 누락, 일화 사실성 누락, 파일 부재.

여기에 더해 사람이 확인할 것: 연도·인물의 사실성, 일화 진위 판정,
표의 각 행이 실제로 그 출처로 뒷받침되는지(표는 인용 누락을 감춘다).

### M5 볼트 동기화

```bash
python3 tools/sync_math_obsidian.py --dry-run   # 먼저 미리보기
python3 tools/sync_math_obsidian.py
```

`G:\내 드라이브\00_Obsidian_Second Brain\Insight Miner\000-수집\002-수학사\` 에
`01-세기 / 02-개념 / 03-인물 / 04-일화` 로 나눠 쓰고, `_MOC/_MOC_수학사.md` 를 갱신한다.
참조된 자산은 `002-수학사/_assets/` 로 복사하고 본문 링크를 `../_assets/` 로 고친다.

**파일명에 유형 접두어를 붙이는 이유**: `Concepts/` 에 원자 노트가 1,598편 있어
`[[미분법]]`이 모호해질 수 있으나 `[[개념_미분법]]`은 충돌하지 않는다.

### M6 배포

```bash
python3 tools/build_math_manifest.py    # math/manifest.json
python3 tools/build_link_index.py       # 루트 link-index.json (4개 갤러리 통합)
```

`build_link_index.py` 는 반드시 manifest 뒤에 돌린다 — math 항목을 manifest에서 읽는다.
그 다음 커밋 → 푸시 → PR → main 머지 → Pages 자동 배포.

---

## 4. 상호 링크

`[[위키링크]]`와 인용 `[n]` 을 실제 링크로 만드는 층이다.
`math/index.html` 과 루트 `index.html` 이 같은 규약을 쓴다.

### 조회표

`link-index.json` (루트) = 4개 갤러리 manifest에서 뽑은 `별칭 → 항목` 표.
`tools/build_link_index.py` 가 만든다. 각 페이지는 이 파일 하나만 더 받으면
갤러리 경계를 넘는 링크가 성립한다.

**추측 매칭(부분일치·유사도)은 쓰지 않는다. 틀린 링크가 미해결 링크보다 나쁘다.**
자동 정규화(부제·`(DEEP)`·영문 병기 제거)로 안 잡히면 `link-aliases.json` 에 손으로 적는다.

### 해석 순서

1. 현재 문서의 `짝문서` (`[[확장편]]` 처리용)
2. `볼트파일명` → 3. `제목` → 4. `별칭` → 5. 실패 → **미해결(흐린 점선)**

미해결은 죽은 링크가 아니라 아직 쓰지 않은 문서다. `--backlog` 가 참조 횟수순으로 뽑아준다.

### URL 규약 (각 갤러리 index.html 실측)

| 섹션 | 해시 |
|---|---|
| reports | `./#r=<slug>` |
| guides | `guides/#g-<slug>` |
| videos | `videos/#post-<id>` |
| math | `math/#n=<slug>` |

### 인용

출처 목록에 **실제로 존재하는 번호만** 링크화한다 — 수식이나 배열 인덱스의 `[1]`을
오탐하지 않기 위한 가드다. 호버하면 제목·도메인·1차/2차 배지가 뜬다.

기존 `reports/` 는 출처 형식이 세 가지로 섞여 있어 셋 다 받는다:
`- [1] 제목 — URL` / `- [K1] 제목 — URL` / `1. (국내) 제목 … URL`.
`**국내**` `**해외**` 소제목이 목록을 쪼개므로 다음 대절까지 전부 훑는다.

---

## 5. 화면

| 뷰 | 내용 |
|---|---|
| 연표 | 세기 밴드 + 칩. 세기 개관으로 진입. "이 세기 전체 담기" |
| 개념 | 카드 + 발전단계 세로 타임라인 |
| 인물 | 생몰 막대를 겹친 **생애 겹침 뷰** + 카드 |
| 일화 | 사실성 배지가 붙은 카드 |
| 검색 | 제목·요약·태그·별칭·분야·교과·발전단계.변화·기여인물 |

읽기 화면: 같은 세기 내 순회 · 담기 · 링크 복사 · 인쇄 · 원문 .md ·
개념 하이라이트 토글 · 글자 크기 · 고정 목차 · 역링크 패널 · 이어서 읽을 것.

**담기함**(`localStorage`): 드래그 정렬 · 항목별 전문/요약 · 예상 분량 ·
합본 .md / 인쇄 / 복사 · **코스 저장**(이름 붙여 저장, `#cart=a,b,c` 로 공유).

---

## 6. 자주 걸리는 것

- **frontmatter 값이 따옴표로 시작하면 YAML이 깨진다.** `제목: "Barrow and …" (arXiv:…)` 처럼
  인용 스칼라 뒤에 내용이 더 오는 형태. 통째로 작은따옴표로 감싼다.
- **자수는 본문 기준**이다(frontmatter 제외). 근거는 `verify_math.py` 머리말 참조.
- **`build_link_index.py` 를 manifest보다 먼저 돌리면** math 항목이 빠진다.
- **일화가 2,500자를 넘으면 실패한다.** 길어졌다면 개념이나 인물로 옮길 내용이 섞인 것이다.
