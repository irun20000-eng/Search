# LESSONS.md — 검수 학습 원장 (compounding QA ledger)

이 파일은 **검수 에이전트**가 매 리서치 작업의 결함·수정·방지규칙을 누적하는 곳이다.
작성·만화 에이전트는 **매 작업 시작 시 아래 "방지 규칙 체크리스트"를 프리플라이트로 로드**하여 같은 류의 실수를 사전 차단한다(품질 복리).

## 방지 규칙 체크리스트 (작업 시작 시 로드)
- [ ] 출처 충돌(같은 사실에 다른 수치/명령)이 있으면 단정하지 말고 출처별로 병기한다.
- [ ] 단일 검색 요약에만 나오는 수치는 1차 출처로 교차검증되기 전엔 "한 자료에 따르면"으로 한정한다.
- [ ] 설치 명령/버전/스타수처럼 시간에 민감한 값은 조회 날짜를 함께 적는다.
- [ ] 국내·해외 출처를 모두 인용하고, 해외 인용엔 한글 요약/번역을 붙인다.
- [ ] 만화 대사는 본문 fact-ledger 밖의 새 사실을 만들지 않는다(각색만).
- [ ] **DEEP는 개요+목록이 아니다.** why/how/비교표/트레이드오프/실전 워크스루/비용·보안까지. QUICK 확장본이면 FAIL. 소스 10+개·병렬 리서치.
- [ ] **"학습만화 = 캐릭터툰" 가정 금지.** 사용자 디자인 스펙(프리미엄 인포그래픽 학습카드: 웜화이트·라운드서체·여백·모티프) 우선. 임베드는 작게(기본 1장 4분할 요약카드).
- [ ] **비주얼 톤을 멋대로 정하지 말 것.** 갤러리/카드는 사용자 톤(라이트·웜화이트) 먼저 확인. 다크 기본 가정 금지. 큰 변경 전 Figma 시안으로 확인.
- [ ] **개념 설명서 = 투트랙(이해편+확장편)(사용자 확정 2026-06-21, 최종).** 개념·학습형은 목적이 다른 두 문서로: **이해편**(slug=`<concept>`, 입문·딥, 비유·예제·"왜 자연스러운가"·대표 응용1) + **확장편**(slug=`<concept>-advanced`, 학부+대학원, 정의·유도·여러 응용·개념 발전 지도·최전선, 이해편 전제). **둘 다 DEEP, 역할 분담으로 중복 금지**, 문체 평어, 끝맺음 상호 링크. 골격·분량 테이블은 PIPELINE.md "개념 설명서=투트랙" 절. 교훈 누적: ①3계층 한 파일=얕음 ②레벨 4분할=얕고 파편화 ③단일 딥=입문 독자엔 인트로 부족 → 목적축 2분할이 최선.
- [ ] **시리즈 모드(사용자 트리거 "시리즈로 작성", 2026-06-23).** 단일 문서 강박 금지 — 담을 게 많으면 여러 파일로. "시리즈" 지시 시 **메인 1편 + 내가 선정·사전보고하는 파생 주제 3편 = 4편 DEEP**, 각 편 독립 게이트 통과, frontmatter `시리즈/시리즈순서` + 본문 끝 내비게이션(◀이전/▶다음/목차), 공통 slug 접두사, 각 편 파이프라인 완주. 상세는 PIPELINE.md "시리즈 모드" 절.
- [ ] **개념 설명서 분량·요소 객관 카운트 게이트(2026-08-16 단위 정정).** 커밋 전 측정: **★정본 글자수 `LC_ALL=C.UTF-8 wc -m`(이해편 ≥8,000자 / 확장편 ≥11,000자)** — ⚠️ **로케일 없는 `wc -m`은 바이트를 센다**(한글 1자=3바이트). 종전 15,000/20,000은 바이트 기준이었고, gold standard도 바이트로 쟀기에 비교는 공정했다(라벨만 틀림). 같은 비율로 문자 단위 환산한 값이 8,000/11,000이다 · 줄수 `wc -l`(≥140 / ≥165) · 섹션 `^## `(≥8 / ≥10) · 시각화(≥3 / ≥2) · 출처(≥7 / ≥12). 하한 미달이면 **머지 금지·보강**, PR 본문에 측정값 표 첨부. **단어수 `wc -w`는 참고만**(한국어+수식은 30~40% 낮게 찍혀 부당 탈락 — gold standard 이란-미국 25,104자 대비 비율로 판정). 주관 판단 금지, 자문: "이란-미국만큼 읽을 게 있나?"
- [ ] **Drive base64 페이로드를 세션이 직접 재입력(Write)할 때, 눈으로 본 head/tail 정상·UTF-8 strict 디코드 성공·구조 게이트 통과도 중간 손상을 못 잡는다(2026-08-17).** 5~7KB급 base64 문자열을 통째로 옮기면 드물게 몇 바이트가 조용히 다른 유효 바이트로 치환돼, UTF-8 디코드는 성공하지만 **엉뚱하지만 그럴듯한 한글 단어로 둔갑**한다("제시한다"→"구글하다" 등) — 예외도 안 뜨고 챕터/인용/TL;DR 개수도 그대로라 게이트를 다 통과한 채 조용히 병합될 뻔했다. → **규칙: MCP `download_file_content`로 받은 base64를 옮길 때는 (a) 디코드 후 바이트 길이를 Drive `search_files`가 보고한 원본 `fileSize`와 대조하고(20바이트 안팎 오차는 줄바꿈 방식 차이로 무해, 수백 바이트 이상 차이는 손상), (b) 결과 파일을 처음부터 끝까지 한 번은 실제로 통독해 의미가 통하는지 확인한다(머리·꼬리만 보지 말 것). 그래도 못 미더우면 그 건은 중단하고 다음 회차로 넘긴다 — 억지로 살리지 않는다.
- [ ] **2회 다운로드 대조도 결정적 오독은 못 잡는다(2026-08-19).** 같은 base64를 두 번 옮겨 적으면 **같은 자리에서 똑같이 틀릴 수 있고**(관측: `코드`→`코듘` `c`→`Y`, `짧게`→`쇧게` `K`→`I`, `안팎`→`안팍` `O`→`N`), 결과가 정상 한글이라 `fileSize` 대조·사설영역 검사·구조 게이트를 전부 통과한다. → **규칙: 인제스트 후 `python3 tools/check_encoding.py <파일>`을 돌려 '기존 코퍼스에 없는 음절' 자문 출력까지 확인**하고, 각 건이 정상 희귀어(뺄·닛)인지 손상(듘·쇧·팍)인지 문맥으로 가른다. 손상이 **1건이라도 확정되면 그 파일은 폐기**한다 — 흔한 음절로 떨어져 검출을 피한 같은 유형의 손상이 더 있을 수 있으므로 보이는 것만 고쳐 발행하지 않는다.
- [ ] **본문을 다른 갤러리로 옮길 때 색·폰트 정의가 따라왔는지 확인한다(2026-08-30).** 옮긴 HTML 이 `var(--…)` 로 색을 부르면 그 정의는 **원천 스타일시트**에 있다. 블로그 서가는 본문·이미지만 옮겨 와 `--green` 등 5종이 미정의였고, **미정의 `var()` 는 SVG 에서 검정으로 떨어져** 도해 19/22편이 검은 덩어리로 두 달 넘게 발행돼 있었다. 콘솔 오류·깨진 이미지·PNG 무결성·모바일 잘림 검사는 **전부 통과했다** — 파일 무결성은 화면 정상을 뜻하지 않는다. → **이관·갤러리 변경 뒤에는 (a) 속성이 `var(` 인데 계산값이 `rgb(0, 0, 0)` 인 자리를 세고, (b) 실제 브라우저로 한 편 열어 눈으로 본다.** 대비를 잴 때는 알파를 합성하고 KaTeX 내부 SVG(`viewBox` 폭 40만)는 제외한다 — 안 그러면 오탐이 진짜를 덮는다.
- [ ] **본문에 숫자를 쓰면 그 자리에서 스크립트로 재계산한다(2026-08-31).** 미분 보고서의 오일러 표 마지막 행이 손계산과 0.01 어긋난 채로 커밋 직전까지 갔다(0.55 vs 0.54, 비율 2.04 vs 2.07). 분량·섹션·인용 게이트는 **전부 통과했다** — 세는 게이트는 값이 맞는지 보지 않는다. → **규칙: worked example·표의 수치는 커밋 전 `python3` 로 전부 재계산하고, 본문 값과 대조한다.** 특히 "비율이 2로 수렴한다" 처럼 **추세를 주장하는 표**는 마지막 행이 가장 틀리기 쉽다(반올림이 누적되는 자리).
- [ ] **"A이면 B" 를 교육적으로 단순화할 때, 그 등가가 실제로 성립하는지 먼저 따져 본다(2026-08-31).** "물가 상승률 둔화 = `f″ < 0`" 은 직관적이고 수업에 좋지만 **엄밀히 거짓**이다 — 뉴스의 상승률은 전년동월비라 로그미분 `P′/P` 에 가깝고, 그것이 감소할 조건은 `P″ < (P′)²/P` 이므로 `P″` 가 양수여도 둔화가 가능하다. → **규칙: 단순화를 버리지 말고 각주로 정확한 조건을 병기한다.** 대비가 유효하면 본문은 살리되, 무엇이 근사인지 독자가 알 수 있게 한다. 단순화를 몰래 하는 것과 밝히고 하는 것은 다르다.
- [ ] **정의만 있고 참조가 없는 각주·출처는 조용히 사라지거나 죽은 무게가 된다(2026-08-31).** 확장편에서 출처 [3][6][17] 이 목록에만 있고 본문에서 안 불렸고, 각주 정의 2건은 참조가 없어 **갤러리 렌더에서 아예 빠질 상태**였다. 사람 눈으로는 "출처가 17개나 있네" 로 보인다. → **규칙: 커밋 전 `본문 [n] 집합 == 출처 정의 [n] 집합`, `[^라벨] 참조 집합 == 정의 집합` 을 기계로 대조한다.** frontmatter `소스수` 도 실제 출처 수와 대조할 것(이번에 6/5로 적혀 있었으나 실제는 7/7이었다).
- [ ] **위키링크는 빌더가 정규화한 정본 이름에 맞춘다 — 별칭 파일을 먼저 늘리지 말 것(2026-08-31).** `[[변화율로 예측하기 (확장편)]]` 이 별칭구멍을 냈다. 정본 이름은 부제·`(확장편)`·`(DEEP)` 이 떨어진 `변화율로 예측하기` 였다. `link-aliases.json` 에 한 줄 적어도 되지만, **글을 정본 이름에 맞추는 쪽이 유지비가 0이다.** → **규칙: `build_link_index.py` 를 돌려 「별칭구멍」이 0인지 확인하고, 구멍이 나면 먼저 글의 링크 표기를 정본 이름으로 고친다.** 별칭 파일은 본문이 자연스럽게 다른 이름으로 부를 때만 쓴다.
- [ ] **산출물이 "언제 만들었나" 를 콘텐츠의 "언제 썼나" 로 쓰지 않는다(2026-08-31).** math 노트에만 날짜 필드가 없어 `build_link_index.py` 가 매니페스트의 `generated`(빌드한 날)를 72편 전부에 복사했다. 결과는 둘이었다 — (a) 허브 「최근」 12칸 중 5칸을 math 가 차지하는데 **그 다섯이 실제 최신이 아니었다**(적분법·무한급수·복소수는 8/21에 쓴 것이고 8/29에 쓴 로그·대수 기호법이 밀려 있었다), (b) 날이 바뀔 때마다 `link-index.json` 73줄이 바뀌어 **진짜 변경을 덮었다**. 갤러리는 정상으로 보였고 검사기도 조용했다 — 날짜가 "그럴듯한 값"이라 아무도 안 봤다. → **규칙: 매니페스트의 `generated`·파일 mtime·`date.today()` 를 항목의 날짜로 쓰지 않는다. 정본은 노트가 들고 있어야 한다**(git 최초 커밋일로 뽑는 방법은 러너가 `fetch-depth: 1` 이라 그 자리에서 빈 값이 된다). 폴백을 둘 때는 폴백이 쓰였는지 보이게 할 것. 그리고 **빌더는 두 번 돌려 산출물이 같은지 확인한다** — 같지 않으면 그 자리에 시계가 섞여 있다.
- [ ] **YAML 날짜는 따옴표를 씌운다(2026-08-31).** `날짜: 2026-08-21` 은 문자열이 아니라 `datetime.date` 로 읽혀 JSON 직렬화에서 터진다. 게이트에 모양 검사(`\d{4}-\d{2}-\d{2}` 문자열)를 넣어 두면 그 자리에서 잡힌다 — 실제로 일부러 따옴표를 빼 확인했다.
- [ ] **세는 게이트는 "표기 이탈"을 원리적으로 못 잡는다(2026-08-25).** 제3자 분석본이 태그를 `Claude Code`로 붙여도 `verify_video.py`는 개수(7~14)만 세어 통과시키지만, 갤러리 33편이 쓰는 `클로드코드`와 어긋나 그 노트만 **태그 필터에서 영원히 분리**된다. → **규칙: 인제스트 후 새 태그가 기존 manifest에 없는 표기(같은 개념의 영문/공백/붙여쓰기 변형)면 갤러리 다수 표기로 정규화**하고 manifest를 재생성한다.

---

## 기록

<!-- 형식:
## YYYY-MM-DD · <주제>
- 결함유형: <무환각위반|인용누락|국내편향|만화-본문불일치|스타일이탈|기타>
- 무엇이 틀렸나: ...
- 수정: ...
- 다음 방지 규칙: ...
-->

## 2026-06-21 · 헤르메스 에이전트 (사용자 피드백)
- 결함유형: 깊이부족 / 스타일이탈(방향오인) / 비주얼톤 오인
- 무엇이 틀렸나:
  1. DEEP가 전혀 deep하지 않고 QUICK 확장 수준(개요+설치+목록). why/how/비교/실전이 없음.
  2. 코믹을 캐릭터툰(로봇·사람 말풍선)으로 만듦. 사용자가 원한 건 "프리미엄 인포그래픽 학습카드"(웜화이트·라운드서체·여백). 품질 "최악, 없는 게 나을 정도".
  3. 임베드 컷이 920px로 과대. "한 컷에 네 내용 담을 사이즈면 족함".
  4. 갤러리를 다크로 만듦. 사용자는 밝은 톤 선호.
- 수정: DEEP 풀 심층 재작성(병렬 리서치·소스10+·비교표·워크스루·Slack·비용/보안). 코믹=클린 학습카드(auto) 1장 4분할로 축소. 갤러리=라이트/웜화이트 재설계. 모두 Figma 시안 먼저 확인 후 코드.
- 다음 방지 규칙: 위 체크리스트의 마지막 3줄(DEEP 기준·캐릭터툰 금지·비주얼톤 사전확인) 상시 적용. 산출 전 "이게 정말 DEEP/사용자 톤인가" 자문.

## 2026-06-21 · 이란-미국 분쟁 타임라인 (자가검수)
- 결함유형: 환경제약(WebFetch 차단) / 시의성 사안 검증
- 무엇이 관찰됐나:
  1. 클라우드 세션에서 **WebFetch가 다수 사이트(naver terms, aljazeera, npr, wikipedia, 의회도서관)에서 403** 반환 → 1차 문서 정독 실패. WebSearch 요약 + Naver MCP 스니펫으로 교차검증해 대체.
  2. 2026년 사안에서 국내 보도가 **2025년 '12일 전쟁'과 2026년 '106일 전쟁'을 혼용**하는 경우 발견 → 두 라운드를 명시적으로 분리 서술하고 본문에 '출처 충돌·주의' 박스로 한정.
- 수정: 최신·유동 구간은 (a) 조회 날짜(2026-06-21) 병기, (b) "진행 중 사안" 라벨, (c) 보도 간 미세차(서명일·조항 수)는 골자 위주로 한정, (d) 재검증 시점(MOU 60일 후) 명시.
- 다음 방지 규칙:
  - [ ] 클라우드 세션은 **WebFetch 403을 전제**로 설계 — WebSearch 요약 + Naver MCP 스니펫 다중교차로 사실 확정, 단일 스니펫 단정 금지.
  - [ ] 동일 명칭의 사건이 **연도만 다른 별개 사건**일 수 있음(예: 12일 전쟁 vs 106일 전쟁) — 연도·기간으로 구분 검증 후 서술.

## 2026-06-21 · 학습자 수준 표준 (사용자 지시)
- 결함유형: 학습자 눈높이 미스매치
- 무엇이 관찰됐나: 야코비안 보고서는 학부 수준으로 작성됨. 사용자는 **개념 설명·학습이 강한 보고서는 중·고등학생 눈높이**를 원함.
- 수정: 위 체크리스트에 "학습자 수준 게이트" 추가. 학습형 보고서는 frontmatter `대상수준` 명시 + 비유·예제·말로 푸는 수식.
- 다음 방지 규칙:
  - [ ] 학습형 보고서 작성 전 **대상 학년대(중/고)를 먼저 정하고** 그 눈높이로 일관 작성. 용어 즉시 풀이, 한 단락=한 개념.
  - [ ] 시사/산업형(DEEP)과 학습형(쉬운 설명)은 **목적이 다른 모드** — 섞지 말 것.

## 2026-06-21 · 개념 설명서 분량 미달 (포맷 4차 반복 끝 확정)
- 결함유형: 분량·깊이 부족(QUICK을 DEEP으로 오판) / 포맷 과다 실험
- 무엇이 관찰됐나:
  1. 개념 학습형 포맷을 4번 바꿈 — ①3계층 한 파일(레벨당 얕음) → ②레벨 4분할(얕고 파편화) → ③단일 딥(입문 인트로 부족) → ④**투트랙(이해편+확장편)** 확정.
  2. 그런데 투트랙으로 써도 **이해편 419단어·확장편 558단어**에 그쳐, 사용자가 "DEEP가 아니라 QUICK 같다"고 평가하고 전부 삭제. **기준점: 이란-미국 보고서 1158단어/15섹션**.
  3. 근본 원인: "이 정도면 깊다"는 **주관 판단**에 의존 → 매번 얕게 나옴.
- 수정: (a) PIPELINE에 **분량·구조 보장 테이블**(이해편 ≥900/확장편 ≥1300단어, 섹션·시각화·worked·FAQ·출처 하한) 명문화, (b) **객관 카운트 게이트**(커밋 전 wc/grep 측정→하한 미달 시 머지 금지, PR 본문에 측정값 표), (c) 이해편 필수 4요소(동기 서사·시각화 3+·worked 1·FAQ 3+) + 골격 고정, (d) 문체 평어.
- 다음 방지 규칙:
  - [ ] 개념 설명서는 **커밋 전 단어·섹션·시각화·출처를 숫자로 측정**하고 하한 미달이면 절대 머지하지 않는다(판단 금지, 측정만).
  - [ ] 포맷을 또 바꾸기 전에 "분량·요소 게이트를 지켰는데도 부족한가?"를 먼저 확인 — 포맷 문제와 분량 문제를 혼동하지 말 것.

## 2026-06-22 · 벡터 보고서 + 게이트 지표 보정 (첫 투트랙 적용)
- 결함유형: 측정 지표 오보정(`wc -w`가 한국어+수식 과소측정)
- 무엇이 관찰됐나:
  1. 벡터 이해편/확장편을 새 표준(투트랙+게이트)으로 작성. 첫 측정에서 단어수 게이트(900/1300)에 미달(643/709) → **머지 보류하고 실질 내용 보강 반복**(worked 예제·부분공간·차원정리·노름·힐베르트 내적 등 추가).
  2. 그래도 단어수가 더디게 차올라(이해편 877, 확장편 1064) 검증 차 gold standard와 직접 비교 → **이미 DEEP으로 인정된 hermes-deep가 742단어**로 벡터 확장편(1064)보다 짧음을 발견. 글자수로 보면 확장편 22,191 vs 이란-미국 25,104(88%)로 동급.
  3. 결론: `wc -w`가 한국어 조사·수식 줄 때문에 수학 DEEP을 부당 탈락시킴. **내용은 충분, 지표가 틀림.**
- 수정: 게이트 정본 지표를 **글자수 `wc -m`·줄수**로 전환(이해편 ≥15,000자/140줄, 확장편 ≥20,000자/165줄), 단어수는 참고로 강등. 근거 수치를 PIPELINE에 명기(게이트를 몰래 낮춘 게 아니라 지표 보정임을 투명화). 보강 후 벡터 이해편 18,191자·확장편 22,191자로 **정본 게이트 통과**.
- 다음 방지 규칙:
  - [ ] 한국어·수식 문서의 분량은 **글자수(`wc -m`)로 측정**한다(단어수 맹신 금지).
  - [ ] 지표가 내용과 어긋나면 **gold standard와 직접 비교**해 보정하고 근거를 남긴다 — 단, 보정과 '게이트 회피'를 구분(비교 수치 공개).

---

## 2026-08-08 — 영상 갤러리 모바일화 이관

### 12. 대량 이관에는 반드시 기계적 동일성 대조를 붙인다
73편을 1MB HTML에서 추출할 때, 눈으로 본 첫 샘플은 완벽했다. 그런데 파리티 검사를 붙이자
**120건 불일치**가 나왔다. 원인은 모놀리스에 **마크업 방언이 2종 공존**한 것이었다.

- 방언1: `<ul><li><strong>3줄 요약</strong></li><li>b1</li>…</ul>`
- 방언2: `<p><strong>3줄 요약</strong></p><ul><li>b1</li>…</ul><p><strong>핵심 인용</strong>…</p>`

한쪽만 처리하는 코드는 **에러 없이 조용히 항목을 버린다.** 샘플 검토로는 절대 못 잡는다.
→ **규칙: 기존 산출물을 다른 구조로 옮길 때는 전수 필드 대조를 먼저 만들고, 그게 통과해야 머지한다.**
   대조 대상은 개수만이 아니라 **순서와 내용**까지여야 한다(실제로 `<li>`/`<p>`를 각각 따로
   수집해 순서가 어긋난 검사기 자체의 버그도 이때 드러났다).

### 13. 게이트 지표는 '잣대'를 통일해야 한다
코퍼스 보정은 **공백 포함** 글자수로 재서 하한 2,300을 뽑았는데, 검증기는 **공백 제외**로
재고 있었다. 그 결과 이미 합격한 노트 3편이 새 게이트에 걸렸다. 게이트가 틀린 게 아니라
**두 지표를 섞은 것**이 틀렸다.
→ **규칙: 하한을 정할 때와 검사할 때가 같은 계산식인지 먼저 확인한다.** 코드 주석에 어느
   지표인지 못 박아 둔다. (공백 포함 min 2,675 / 공백 제외 min 2,091 — 30% 가까이 벌어진다.)

### 14. 근거 없는 게이트 숫자를 계획서에 적지 말 것
가이드 분량 게이트를 계획 단계에서 '6,000자'로 적었다. **아무 근거가 없었다** — 영상 노트는
73편 코퍼스가 있어 실측했지만 가이드는 코퍼스가 없었고, 그냥 그럴듯해 보이는 숫자였다.
실제 가이드는 4,295자로 나왔고, 이때 선택지는 셋이었다: ① 억지로 늘려 6,000을 맞춘다
② 조용히 4,000으로 낮춘다 ③ 근거 없음을 밝히고 근거 있는 값으로 교체한다.
①은 독자에게 나쁜 글을 만들고, ②는 "게이트를 몰래 낮추지 말 것" 위반이다.
→ **규칙: 실측 대상이 없으면 '잠정'이라고 못 박고 앵커를 밝힌다.**(영상 노트 중앙값 3,065자에
   앵커링 → 3,000자 잠정) 그리고 **언제 재보정할지**를 함께 적는다(가이드 3건 후).
   근거를 못 대는 숫자는 게이트가 아니라 그냥 취향이다.

### 15. 개선이 기존 성질을 깨뜨리지 않는지 본다
1MB 모놀리스를 21KB 셸로 줄이면서 마크다운 렌더링을 CDN(`marked`)에 맡겼다. 크기는 98%
줄었지만 **원본이 갖고 있던 "외부 의존성 0"이라는 성질을 잃었다** — CDN이 막히면 노트를
하나도 못 읽는다. 실제로 이 세션 환경에서 jsdelivr가 차단돼 있어서 드러났다.
→ **규칙: 리팩터링 전에 원본이 가진 좋은 성질을 목록으로 적어 두고, 이후에도 유지되는지 대조한다.**
   여기서는 렌더러를 자체 구현(약 60줄)해 복원했다. 노트가 쓰는 문법이 좁아 비용이 작았다.

### 16. 원본 데이터도 틀릴 수 있다 — 권위 있는 출처로 교차확인
`XzU81FfDXLs`는 `data-dur="58"`(58초), 라벨은 `58:59+`였다. YouTube API로 확인하니 실제
**1:04:25(3,865초)**. 58초짜리로 취급돼 '길이순' 정렬이 조용히 잘못 동작하고 있었다.
→ **규칙: 이관은 "원본 그대로"가 원칙이지만, 권위 있는 출처로 검증 가능한 필드는 검증한다.**
   교정할 때는 파리티에서 빼되 **검사 출력에 매번 표시**해 숨기지 않는다.

### 17. 모델에게 사실 메타데이터를 묻지 않는 설계
제미나이 프롬프트에서 제목·채널·길이·게시일을 **아예 요구하지 않도록** 했다. API가 채우기
때문이다. 무환각 규칙을 "지어내지 마라"라고 부탁하는 대신 **지어낼 경로를 없앴다.**
비슷하게 `get_video_details`는 없는 ID를 응답에서 조용히 빼므로, "응답에 없으면 폐기"라는
결정적 판정이 성립한다.
→ **규칙: 환각 방지는 지시보다 구조로 푼다.** 모델이 만들 수 있는 것과 검증 가능한 사실을
   분리하고, 사실 쪽은 권위 있는 출처가 채우게 한다.

### 18. 자동 게이트는 "인용이 실제로 그 말을 하는지"를 못 본다
OpenRouter 가이드가 `verify_guide.py` 를 **통과한 상태로** 두 가지 사실 오류를 품고 있었다.

- "크레딧은 최소 구매액 없고 만료되지 않는다[3]" → 실제로는 **최소 $5·약관상 만료 조항 있음**.
  게다가 출처 [3](요금 한도 문서)은 크레딧 만료를 아예 다루지 않는다. **인용이 하지 않은 말을 업고 있었다.**
- "`404` = 모델 ID 오타" → 실제로는 잘못된 ID는 **400**이고, `404`는 보낼 엔드포인트가 없다는 뜻이다.

게이트는 `[n]` 이 출처 목록에 있는지(구조)는 검사하지만 **그 출처가 그 주장을 지지하는지(내용)는 검사할 수 없다.**
→ **규칙: 구조 게이트 통과를 품질 통과로 착각하지 말 것.** 작성 컨텍스트 없는 리뷰어 패스는
   생략 가능한 절차가 아니라 **게이트가 원리상 못 보는 층을 담당하는 유일한 장치**다.
   특히 숫자(요금·한도·버전)와 오류 코드는 리뷰어가 반드시 원출처 기준으로 다시 확인해야 한다.

### 19. "무료"를 팔 때는 무엇으로 지불하는지 함께 밝힌다
OpenRouter 무료 모델을 "돈 한 푼 안 든다"로 소개했는데, 정작 **대가가 프라이버시**라는 걸 빼먹었다.
무료 엔드포인트 대부분은 프롬프트를 학습에 쓰거나 공개할 수 있고, 그 권한을 켜야만 쓸 수 있다.
게다가 그 설정을 안 켜면 **모든 무료 모델이 404** 로 실패해 독자가 막다른 길에 갇힌다 —
"공짜"라는 홍보 문구와 "왜 다 404지?"라는 좌절이 같은 원인에서 나온 것이다.
→ **규칙: 무료·할인·크레딧을 안내할 때는 반드시 그 대가(데이터 정책·한도·수수료·만료)를 같은 화면에서 밝힌다.**
   그리고 **그 조건을 켜는 단계 자체를 절차에 넣는다.** 안 그러면 가이드가 독자를 막다른 길로 보낸다.

### 20. 소액 결제가 수수료 면에서 가장 불리할 수 있다
"일단 $5만 넣어 보자"가 상식처럼 보이지만, 최소 수수료($0.80)가 걸리면 **실효 수수료율이 16%** 가 된다.
→ **규칙: 결제를 권할 때는 금액대별 실효 비용을 표로 보여 준다.** 퍼센트 표기만 옮기면 소액 사용자를 오도한다.

## 2026-08-19 · 보고서 날짜가 전부 같은 값이었다 (사용자 지적)
- 결함유형: 메타데이터 복붙(날짜 고정) / 갤러리 정렬 왜곡
- 무엇이 틀렸나: 새 보고서를 쓸 때 frontmatter `날짜:`와 manifest `date`에 **직전 보고서의 값을 그대로 복사**해 왔다. 그 결과 8/1~8/16에 걸쳐 만든 **20편이 전부 `2026-08-01`**로 기록돼, 갤러리 카드의 작성일이 의미를 잃고 날짜 정렬·필터가 무력화됐다. 사용자가 갤러리를 보고 발견했다.
- 수정: `git log --diff-filter=A`로 각 report.md의 **최초 커밋일**을 뽑아 19편의 manifest·frontmatter를 실제 작성일로 교정(8/2·8/6·8/8·8/12·8/13·8/14·8/15·8/16로 분산). manifest↔frontmatter 불일치 0건 확인.
- 다음 방지 규칙:
  - [ ] **새 보고서의 날짜는 반드시 `date +%F`로 그날 날짜를 넣는다.** 이전 보고서에서 frontmatter를 복사할 때 `날짜:`·`date`는 **가장 먼저 갈아끼울 항목**이다.
  - [ ] 커밋 전 점검에 **날짜 확인**을 포함한다: `grep '^날짜:' reports/<slug>/report.md` 가 오늘 날짜인지, manifest의 `date`와 같은지.
  - [ ] 메타데이터(날짜·소스수·slug·짝문서)는 본문과 달리 **검수 에이전트가 잡아주지 않는 영역**이다(내용 검증에 집중하므로). 작성자가 직접 확인한다.

## 2026-08-16 · 게이트 단위 오류: `wc -m`이 바이트를 세고 있었다 (검수 발견)
- 결함유형: 측정 도구 오용(로케일) / 지표 라벨 오기 / 출처-주장 불일치(재발)
- 무엇이 틀렸나:
  1. **이 컨테이너는 `LANG=`가 비어 있어 `wc -m`이 문자가 아니라 바이트를 센다.** 한글 1자 = UTF-8 3바이트라, 모든 보고서가 실제보다 **약 1.8배 길어 보였다**. 평균값 정리 이해편은 "19,615자"로 기록됐지만 실제는 **11,130자**였다. 검수 에이전트가 잡아냈다.
  2. **다만 게이트가 부당했던 것은 아니다.** gold standard(이란-미국)도 **같은 바이트 방식**으로 25,104로 기록됐고(실제 13,564자), 하한 15,000/20,000이 그 바이트 값에서 도출됐다. 즉 **계산과 검사의 잣대가 일치**해 비교는 공정했다 — 틀린 것은 숫자가 아니라 **라벨('자'라고 쓴 것)**이다.
  3. 부수 왜곡: 바이트/문자 비율이 문서마다 1.76~1.85로 달라(ASCII 수식이 많을수록 낮음) **수식이 많은 수학 문서가 상대적으로 불리**했다.
  4. 같은 세션 확장편에서 **출처-주장 불일치가 재발**: 네이버 "이계도함수" 항목을 **벡터값 함수의 평균값 등식 실패**라는 전혀 다른 주장에 5회 인용. (2026-08-13에 추가한 방지 규칙이 지켜지지 않았다.)
- 수정: 측정 명령을 **`LC_ALL=C.UTF-8 wc -m`**으로 교체(CLAUDE.md·PIPELINE.md·LESSONS.md 전부). 하한은 **gold standard 대비 같은 비율을 유지**한 채 문자 단위로 환산 — 이해편 **≥8,000자**, 확장편 **≥11,000자**(59.8%·79.7% × 13,564). 환산 근거를 PIPELINE에 공개(게이트를 낮춘 것이 아니라 단위 정정임을 명시). 확장편의 [5]는 실제로 뒷받침하는 자리(테일러 2차항)로 옮기고, 벡터값 주장에는 Wikipedia의 해당 절을 [13]으로 신설.
- 다음 방지 규칙:
  - [ ] **분량 측정은 반드시 `LC_ALL=C.UTF-8 wc -m`.** 로케일 없는 `wc -m`은 `wc -c`(바이트)와 같은 값을 낸다 — 한 번이라도 두 값이 같으면 바이트를 세고 있다는 신호다.
  - [ ] **새 측정 도구를 도입할 때 "이 숫자의 단위가 정말 내가 생각하는 단위인가"를 한 번 검증**한다(같은 파일을 두 방법으로 재서 대조).
  - [ ] 지표를 정정할 때는 **gold standard를 같은 방식으로 다시 재고 비율을 보존**한다. 그래야 '몰래 낮추기'와 구분된다.
  - [ ] **출처-주장 일치 점검을 검수 체크리스트의 고정 항목으로** 둔다(이번이 재발이다). 특히 국내 백과 항목은 제목만 보고 갖다 붙이기 쉽다.

## 2026-08-15 · 전단변환 2편 (검수)
- 결함유형: 과잉 일반화(그럴듯한 대칭 주장) / 자유도 계산 오류 / 부호 조건 누락
- 무엇이 틀렸나:
  1. **CRITICAL — "사차함수와 직선의 네 교점은 대칭 배치"는 거짓.** 근의 합이 −b/a로 고정되므로 **평균(무게중심)만 고정**될 뿐이다. 검수가 반례를 계산해 제시: `y=x⁴−15x²` 와 `y=10x−24`는 x=−3,−2,1,4에서 만나 평균은 0이지만 배치는 비대칭. "합이 일정하다"에서 "대칭이다"로 넘어간 것이 논리 비약.
  2. **CRITICAL — 자유도 뺄셈이 일차함수에서만 깨진다.** 평행이동 2개를 걸어도 직선은 `a(x+s)+b+t`가 되어 **상수항 하나만** 움직인다(기울기 불변). "계수 개수 − 2"가 보편 공식이 아님을 몰라 표에 0을 적었다.
  3. **CRITICAL — 부호 조건 누락.** 삼차함수 극점 존재 조건을 `p<0`으로 적었으나 정확히는 **`p/a<0`**(a<0이면 반대). 최고차 계수 부호를 상수처럼 취급한 전형적 실수.
- 수정: 반례 박스 신설 + "무게중심" 용어로 전면 통일(TL;DR·핵심포인트·장 제목·위키링크 라벨까지), 자유도 표에 주의 박스, a 부호 단서 추가. 그 외 |det| 절댓값 표기, 이중접선 존재조건 p/a<0, 사차 축대칭 기준(일차항 소멸) 등 MINOR 반영.
- 다음 방지 규칙:
  - [ ] **"합/평균이 일정" ≠ "대칭"**. 근과 계수로 얻는 것은 대칭함수 값뿐이다. 대칭을 주장하려면 **짝을 이루는 근 구조**를 따로 보여야 한다. 대칭·등간격·비율 주장은 반드시 **반례를 한 번 찾아본 뒤** 쓴다.
  - [ ] **최고차 계수의 부호를 잊지 말 것.** 극값·볼록성·부등식 방향 조건은 `a>0` 가정을 명시하거나 `p/a` 꼴로 쓴다.
  - [ ] **용어를 정정할 때는 전 문서를 grep**한다(본문만 고치고 제목·요약표·링크 라벨에 옛 표현이 남아 두 번 더 커밋했다).
  - [ ] 영상 기반 보고서라도 **영상 메타데이터는 `get_video_details`로만** 확정하고, 수학 내용은 영상 요약이 아니라 **직접 유도**로 세운다(자막 수집 불가 환경에서 특히).

## 2026-08-13 · 수식 렌더 깨짐 (사용자 피드백)
- 결함유형: 스타일이탈(렌더러 미지원 문법 사용) / 표 파괴
- 무엇이 틀렸나: 보고서에 LaTeX `$$…$$`·`\frac`·`\lim`·`\int`를 썼는데 **갤러리에는 수식 렌더러(KaTeX/MathJax)가 없다**(index.html은 marked@12.0.2만 로드). 그래서 `$`·백슬래시 명령이 **그대로 노출**되고 `\frac{a}{b}`의 중괄호·밑줄이 마크다운을 깨뜨렸다. 게다가 **표 셀 안의 `|a|`(절댓값) 파이프가 열 구분자로 먹혀 표 전체가 붕괴**. 다항함수 2편에서 사용자가 발견.
- 수정: 모든 수식을 **유니코드 평문**으로 전환 — 분수 `a/b`, 지수 `xⁿ⁺¹`, 적분 `∫[α, β] … dx`, 그리스문자 α/β/γ/Δ 직접. 디스플레이 수식은 **인라인 코드스팬(백틱)**으로 감싸 밑줄·별표 간섭 차단. **표 안 절댓값은 `\|a\|`로 이스케이프**(marked@12.0.2에서 `|`로 정상 렌더됨을 실제 설치·렌더로 검증). 렌더 후 HTML에 `$`·잔여 LaTeX 0, 표 칸부족 0 확인.
- 다음 방지 규칙:
  - [ ] **보고서 수식은 LaTeX(`$…$`, `\frac` 등) 금지 — 유니코드 평문으로 쓴다.** 갤러리 렌더러는 marked뿐, 수식 미지원. 디스플레이 수식은 코드스팬으로 감싼다.
  - [ ] **표 셀 안 파이프(절댓값 `|a|` 등)는 `\|`로 이스케이프.** 안 하면 표가 조용히 깨진다.
  - [ ] 수식·표가 많은 보고서는 커밋 전 `grep -c '\$'`(0이어야)와 marked 렌더 스팟체크로 확인.
  - [ ] ※ 이 규칙은 **소급 적용 대상**: 기존 `$$`를 쓴 수학 보고서(로피탈 2편·벡터·함수용어 등)도 동일 결함 가능 — 재작업 시 점검.

## 2026-08-13 · 다항함수 확장편 (검수)
- 결함유형: 인용-주장 주제 불일치(citation-target mismatch)
- 무엇이 틀렸나: 사차함수(quartic) 실전 절의 세 주장이 출처 [10] "Wikipedia, Cubic function"에 걸려 있었다. 수식·수치는 전부 정확했고 `[n]`도 출처 목록에 존재해 **구조 게이트는 통과**했지만, **삼차(cubic) 페이지가 사차 내용을 뒷받침할 수 없다**. 게이트는 "[n]이 목록에 있나"만 보고 "그 출처가 그 주제를 다루나"는 못 본다(LESSON 18의 재확인).
- 수정: [10]을 "Wikipedia, Quartic function"으로 재지정. 아울러 §2 변곡점 표준형의 1차계수를 c→c′=c−b²/3a로 정확화(리뷰어 지적, 대칭 결론은 불변).
- 다음 방지 규칙:
  - [ ] 인용은 `[n]`이 존재하는지뿐 아니라 **그 출처의 주제가 문장의 주제와 일치**하는지 검수 패스에서 확인한다. 특히 "삼차→사차", "이차→삼차"처럼 인접 대상으로 주장이 넘어갈 때 출처가 따라왔는지 본다.
  - [ ] 수학 꿀팁·공식류 보고서는 **모든 공식을 리뷰어가 손유도로 재검증**(단순 그럴듯함 금지). 이번 두 편은 넓이 4공식·마스터 항등식·16:1·|a|D³/2·비에트를 전부 유도 확인해 CRITICAL 0으로 통과.

## 2026-08-17 · 스파크 영상노트 인제스트 (자가검수 — Drive base64 손상 발견)
- 결함유형: 콘텐츠 손상(무결성 실패), 게이트가 못 보는 층
- 무엇이 관찰됐나: Drive 폴더(구글 스파크 산출물)에서 신규 영상노트 5건을 골라 `download_file_content`로 base64를 받고 세션이 직접 파일에 옮겨 적었다.
  1. 첫 시도(kdt5J2bpchM, _PdvWiuJF-M)는 head/tail 육안 확인 + 백슬래시 0개 + `===VIDEO-NOTE v1===`/`===END===` 마커로 "검증 통과"했고, `ingest_video.py`도 UTF-8 strict 디코드 성공 + 챕터 5·인용 5·TL;DR 5 게이트를 모두 통과해 커밋 직전까지 갔다.
  2. 그런데 두 번째 파일(`_PdvWiuJF-M`)을 인제스트하는 과정에서 `UnicodeDecodeError`가 나서 재확인해 보니, "통과"한 첫 파일도 실제로는 본문 중간에 손상이 있었다 — "**엔진엑스(Nginx) 리버스 프록시**를 결합하여 ... 제시한다"가 "**엔진엑스핐(Nginx) 리버스 프록시스템을 전용하여 ... 구글하다"로, "구글 클라우드 프리티어 VM 생성"이 "구글폼원성"으로, "백그라운드"가 "백그띴욤드"로 바뀌어 있었다. 디코드된 바이트 길이도 Drive가 보고한 원본 `fileSize`(5556)보다 21바이트 적었다(작은 차이라 무해해 보였지만, 실은 이미 손상이 있었다는 신호였다).
  3. 같은 방식으로 옮긴 T5va0A7wvHk·y9u1IdDYHZQ·8XVVzAgn3g8 세 편도 재시도할 때마다 다른 지점에서 깨졌다(같은 파일을 다시 받아도 매번 다른 곳이 깨짐 — 소스 문제가 아니라 옮겨 적는 쪽의 문제).
- 수정: 손상 가능성이 있는 산출물은 전부 폐기(`videos/notes/`에 아무것도 커밋하지 않음, `manifest.json` 원복). 이번 회차는 **0건 발행**으로 마감하고, Drive 원본 스파크 파일은 그대로 두어(삭제·이동 금지 원칙 준수) 다음 회차가 자동으로 재시도하게 했다. 방지 규칙 체크리스트에 항목 추가(위 참조).
- 다음 방지 규칙:
  - [ ] **"게이트 통과 = 손상 없음"이 아니다.** 구조 게이트(챕터/인용/TL;DR 개수)와 UTF-8 디코드 성공은 "그럴듯한 다른 글자로 치환된 손상"을 못 잡는다. 원본 `fileSize`와 디코드 결과 바이트 길이 대조를 필수 1차 관문으로 삼는다.
  - [ ] 대용량 base64 페이로드를 세션이 직접 재입력해야 하는 구조 자체가 손상 위험원이다 — 가능하면 원본 바이트를 그대로 파이프할 수 있는 경로를 우선 찾고, 불가피하게 재입력해야 한다면 이번처럼 다단계 검증(바이트 길이 대조 + 전문 통독)을 거친다.
  - [ ] 검증에 자신이 없으면 억지로 살리지 말고 그 회차는 건너뛴다 — Drive 원본이 남아 있으므로 다음 회차가 다시 시도할 수 있다(1차이자 유일한 dedup은 `videos/notes/<id>.md` 존재 여부이므로 안전하게 재시도 가능).

## 2026-08-19 · 스파크 영상노트 인제스트 (2회 대조를 통과한 손상 — 코퍼스 대조로 검출)
- 결함유형: 콘텐츠 손상(무결성 실패), 검증 절차 자체의 사각지대
- 무엇이 관찰됐나: 런북대로 파일마다 `download_file_content`를 2회 독립 실행하고 두 전사본을 바이트 대조했다. 이 절차는 실제로 작동했다 — zBRb4bm4dyc에서 `2026년`→`2026륄`(base64 `4W`→`6W`), zkc7iDXjHUA 4곳, -NZX_CzrFzw 5곳, Lk44MMqpmkY 5곳을 잡아냈고, 매번 `fileSize`와 정확히 일치하는 쪽이 정답이었다.
  1. **그런데 2회 대조를 통과한 손상이 따로 있었다.** A·B 두 전사본이 **같은 자리에서 똑같이 틀린** 경우다: `키미 코드`→`키미 코듘`(`c`→`Y`), `짧게 작성`→`쇧게 작성`(`K`→`I`), `40줄 안팎`→`40줄 안팍`(`O`→`N`). 전부 base64 **한 글자** 치환이고 결과가 정상 한글 음절이라 `check_encoding.py`의 사설영역·제어문자 검사도 통과했다.
  2. 즉 오독이 **확률적이 아니라 결정적**일 수 있다. 같은 입력을 같은 방식으로 두 번 읽으면 같은 자리에서 같이 틀린다. 2026-08-17 기록("매번 다른 곳이 깨짐")과 **양쪽 다 발생**하며, 2회 대조는 확률적 손상만 걸러낸다.
  3. 이걸 잡은 건 **기존 발행 노트 86편의 한글 음절 코퍼스와 대조**하는 검사였다. 손상 음절(듘·쇧·팍)은 코퍼스에 0회, 정상 희귀어(뺄·뺀·닛=init)는 오탐으로 같이 뜬다.
  4. `fileSize` 대조는 이 유형을 **원리적으로 못 잡는다** — 한글↔한글 치환은 3바이트로 길이가 같다.
- 수정: 손상이 확인된 -NZX_CzrFzw·Lk44MMqpmkY 2건은 폐기(다음 회차 재시도). A·B 불일치가 22곳이라 원본을 확정할 수 없던 XSsKjS1z7Yg도 폐기. 코퍼스 대조 검사를 `tools/check_encoding.py`에 `rare_syllables()`로 추가하되, 오탐이 있으므로 **차단이 아니라 '검토 필요' 자문 출력**으로 넣었다(leave-one-out — 검사 대상 자신을 코퍼스에서 빼지 않으면 검사가 통째로 무력해진다).
- 다음 방지 규칙:
  - [ ] **"2회 다운로드 대조 통과 = 무손상"이 아니다.** 결정적 오독은 두 전사본이 똑같이 틀리므로 대조를 통과한다. 반드시 `python3 tools/check_encoding.py <파일>`의 **'코퍼스에 없는 음절' 자문 출력까지 눈으로 확인**하고, 각 건이 정상 희귀어인지 손상인지 문맥으로 가른다.
  - [ ] 손상 판별 단서: **base64 한 글자 치환인지 확인**한다. 정상 단어와 손상 단어를 각각 인코딩해 1글자만 다르면 전사 오류(스파크 원본 오타가 아니다). 이 판별이 "고칠까 폐기할까"를 가른다.
  - [ ] 한 파일에서 손상이 1건이라도 확정되면 **그 파일은 폐기**한다. 같은 유형의 다른 손상이 흔한 음절로 떨어져 검출을 피했을 수 있으므로, 보이는 것만 고쳐 발행하면 안 된다.

## 2026-08-20 · 미적분의 발견 투트랙 (자가검수에서 잡은 것들)
- 결함유형: 인용-주장 주제 불일치 / 문서 내 수치 불일치 / 출처 없는 단정
- 무엇이 관찰됐나:
  1. **또 인용-주장 불일치가 나왔다(4번째).** 확장편에서 "실수의 엄밀한 구성이 1872년"이라는 주장에 네이버 「극한」 항목을 달아 놨다 — 그 항목은 데데킨트·칸토어를 다루지 않는다. 리만 적분·르베그 적분 연표 행도 무출처였다. → SEP Dedekind, MacTutor Riemann, Britannica Lebesgue로 각각 보강.
  2. **자기 문서가 자기 체크리스트를 어겼다.** 확장편 §10 체크리스트에 "연도 인용 시 완성과 출판을 구분할 것"이라 써 놓고, 정작 §6-3 연표에서 리만 적분을 "1854"로만 적었다(1854 발표 / 1868 출판). 규칙을 쓰는 것과 지키는 것은 별개다.
  3. **문서 내 수치 불일치**: TL;DR은 로빈슨을 "200년 이른 것", §7은 "300년 뒤에 나온 것"이라 적었다(1675→1966은 약 290년). 같은 사실을 두 곳에 쓰면 반드시 대조할 것.
  4. **출처 없는 단정 3건**(이해편): 콜린스를 "왕립학회의"로 단정, 뉴턴-후크 광학 논쟁, 라이프니츠 장례식 묘사. 각각 소속 표현 삭제·SEP 인용·삭제로 처리.
  5. 마크다운 출처 목록 중간에 빈 줄이 들어가 `<ul>`이 둘로 쪼개졌다(추가 항목을 이어 붙일 때 발생).
- 무엇이 잘 작동했나: **본문 수식을 전부 sympy로 재유도해 대조**하는 절차(배로 a/e·뉴턴 유율방정식·몫의 미분법·리만합 극한·넓이 미분). 전 항목 일치했고, 이 절차 덕에 "역사적 인물의 방법이 현대 결과와 정말 같은가"를 주장이 아니라 계산으로 확인할 수 있었다. 수학사·수식 포함 보고서에는 기본 절차로 삼을 것.
- 다음 방지 규칙:
  - [ ] **인용을 달 때 출처의 URL/제목이 그 주장을 실제로 다루는지 한 건씩 대조한다.** 특히 연표·표의 각 행은 행 단위로 확인 — 표는 인용 누락과 오배치가 가장 잘 숨는 곳이다.
  - [ ] **본문에 체크리스트·규칙을 쓴 문서는 그 규칙으로 자기 본문을 한 번 더 훑는다.** (이번엔 "완성 vs 출판" 규칙을 자기가 어겼다.)
  - [ ] **같은 수치·연수가 두 곳 이상에 나오면 grep으로 대조**한 뒤 커밋한다(TL;DR ↔ 본문이 특히 자주 어긋난다).
  - [ ] 출처 목록에 항목을 추가할 때 **빈 줄이 끼지 않았는지** 확인한다(리스트가 쪼개짐).
  - [ ] 수식이 있는 보고서는 **커밋 전 sympy로 전 수식 재유도·대조**를 돌린다.

## 2026-08-20 · 스파크 영상노트 (평문 경로 첫 실전 — 폐기 3건이 그대로 통과)
- 결함유형: 없음(무손상 발행) / 게이트 사각지대 1건 발견
- 무엇이 관찰됐나:
  1. **08-19에 손상으로 폐기했던 정확히 그 3건**(XSsKjS1z7Yg·-NZX_CzrFzw·Lk44MMqpmkY)을 `read_file_content`(평문) 경로로 다시 처리했더니 **손상 0건**으로 통과했다. 같은 파일·같은 세션 구조에서 base64 경로는 3/3 손상, 평문 경로는 0/3 손상 — 원인이 파일이 아니라 **전송 표현(base64)**에 있었음이 통제된 비교로 확인됐다.
  2. 파일당 2회 독립 호출 대조에서 불일치 0곳. base64 시절(-NZX_CzrFzw 5곳, Lk44MMqpmkY 5곳)과 대비된다.
  3. `check_encoding.py`의 코퍼스 자문은 이번에도 떴지만 전부 **기록된 오탐**이었다 — `뺄`·`뺀`(빼다), `닛`(init 음차). 손상 음절(듘·쇧·팍) 계열은 0건. 오탐 목록이 실제로 판별에 쓰였다.
  4. **독립 리뷰어가 게이트 사각지대를 찾았다.** `-NZX_CzrFzw`는 115분 영상에 챕터 5개뿐이고 3→4장 39.7분, 5장→끝 43.5분이 비어 누적 미커버가 약 75%다. 그런데 `verify_video.py`의 챕터 공백 검사는 **단일 최대 공백만** 보므로 37.6% < 60%로 통과했다. 희소성이 두 구간에 나뉘면 게이트를 빠져나간다.
- 수정: 3건 모두 발행. `-NZX_CzrFzw`의 커버리지 문제는 노트가 없는 구간을 지어내지 않은 결과이므로 내용은 손대지 않고 **스파크 재분석 대기 항목**으로 보고에 남겼다.
- 다음 방지 규칙:
  - [ ] **Drive 본문은 `read_file_content`(평문)로 받는다. `download_file_content`(base64)를 쓰지 않는다.** 08-19 대비 08-20 통제 비교로 효과가 확인됐다(3/3 손상 → 0/3). 평문은 중복성이 커서 같은 오류가 곧바로 헛소리로 드러난다.
  - [ ] **"최대 공백 통과 = 커버리지 충분"이 아니다.** 공백이 여러 구간에 나뉘면 단일 최대 공백 검사를 빠져나간다. 긴 영상(1시간+)은 **챕터당 분/장**(코퍼스 중앙값 3.5분, p90 10분)을 함께 보고, 20분/장을 넘으면 발행하되 재분석 대기로 남긴다.

## 2026-08-21 · 스파크 영상노트 3편 (평문 경로 2회차 — 전 라인 기계 diff 도입)
- 결함유형: 없음(무손상 발행) / 검수 방법 개선 1건 · 판정 기준 공백 1건
- 무엇이 관찰됐나:
  1. **평문 경로 2회차도 손상 0건.** HP8YODI7HhY(6,227B)·iwDB0C9LLoU(7,110B)·XgWJWuz6Dmw(6,327B) 전부 2회 독립 호출 대조에서 불일치 0곳, 재읽기 추가 0회. base64 시절 최악이었던 7KB급 파일(7,110B)도 무손상 — 08-20의 통제 비교 결론(원인은 파일 크기가 아니라 전송 표현)이 재확인됐다.
  2. **독립 리뷰어가 전 라인 기계 diff를 돌린 것이 결정적이었다.** 타임스탬프 표기(`@M:SS` ↔ `· [MM:SS](…?t=N)`)만 정규화하고 `difflib`로 노트 ↔ 스파크 원본을 통째로 대조하니, 3개 파일 전체를 통틀어 차이가 **신고한 오타 수정 1행뿐**임이 나왔다. "최소 수정이었다"는 작성자의 신고를 리뷰어가 **눈이 아니라 계산으로** 검증한 것이다. 눈으로 통독하는 방식은 조용한 손질을 놓칠 수 있다.
  3. **어색한 한글 = 손상이 아니다.** `회의롱` `제나이` `기요관이` `생상하겠다` `파하지` `웹로` `파크는 GL에서`(=스파크는 지메일에서) 7종이 걸렸지만 전수 대조 결과 **예외 없이 원본과 축자 일치** — 영상 자동자막(ASR) 오인식을 스파크가 그대로 옮긴 것이다. 판별 기준은 "읽어서 이상한가"가 아니라 **"원본과 다른가"**다.
  4. `check_encoding.py` 코퍼스 자문 3건(`얕`·`켬`·`텝`)은 전부 정상 한국어. 오탐 목록에 추가할 유형: **활용형**(얕아지다), **용언 명사형**(켜다→켬), **외래어 음차**(multi-step→멀티스텝).
  5. **`내 팁` 슬롯의 외부 지식 허용 범위에 판정 기준이 없었다.** 리뷰어가 2건을 애매로 올렸다 — "프리미어, 캡컷 등"(영상에 없는 제품명), "문서가 10개 이상 쌓이면"(영상엔 14개 사례뿐인데 임계값을 만듦). 통과시켰으나 다음 회차 판정이 흔들릴 자리다.
- 수정: 3편 전부 발행. 스파크 원본 오타 1건(`재성성`→`재생성`)만 최소 수정 — `핵심 인용`이 아니라 `내 질문`(작성자 자신의 문장)이라 인용 무손질 원칙과 무관하다.
- 다음 방지 규칙:
  - [ ] **검수는 눈이 아니라 기계 diff로 시작한다.** 리뷰어는 노트 ↔ 스파크 원본을 타임스탬프 표기만 정규화해 전 라인 `difflib` 대조하고, **차이 집합이 신고된 수정 목록과 정확히 일치하는지** 확인한다. 일치하지 않으면 신고되지 않은 손질이 있다는 뜻이다.
  - [ ] **인용문 안의 어색한 한글을 손상으로 오판하지 않는다.** ASR 오인식과 인코딩 손상의 판별 기준은 가독성이 아니라 **원본과의 일치 여부**다. 원본과 같으면 무환각 원칙상 고치지 않고 그대로 보존한다(고치면 그것이 창작이다).
  - [ ] **`내 팁`/`내 질문`은 1인칭 휴리스틱 슬롯이므로 영상에 없는 제품명·수치 임계값을 담아도 통과시킨다** — 단 그것이 영상·제품에 대한 **사실 단정**이면 불합격이다. 경계는 "내가 이렇게 하겠다"(허용) vs "그 도구는 이렇다"(불허). `3줄 요약`·`핵심 인용`에는 외부 지식을 일절 넣지 않는다.
  - [ ] **인용의 실제 발화 여부는 이 환경에서 검증 불가하다는 점을 매 회차 명시한다.** `get_transcripts`가 클라우드 IP 차단으로 실패하므로 보증 범위는 "노트 = 스파크 원본"까지다. "스파크 원본 = 실제 발화"는 미검증이며, 이는 개별 편의 결함이 아니라 파이프라인의 구조적 한계다.

## 2026-08-21 · 수학사 섹션 신설 (math/) — 렌더러 부재로 죽어 있던 링크 690회 복구

- 결함유형: 기존 갤러리 렌더 누락 2종(위키링크·인용) / 게이트 측정 지점 불일치 1건 / 파서 결함 3건
- 무엇이 관찰됐나:
  1. **본문 `[[위키링크]]` 690회가 전부 깨진 채 노출되고 있었다.** 62편에 이미 위키링크가 쓰여 있는데
     `index.html`의 JS는 `[[` 를 전혀 처리하지 않았다. `marked`가 그대로 흘려보내 **옵시디언에서는 링크,
     웹에서는 대괄호 문자열**이었다. 데이터는 갖춰져 있었고 없는 것은 렌더러뿐이었다.
  2. **인용 `[n]` 3,281개가 클릭 불가였다.** 각주 `[^라벨]`만 구현돼 있었고 출처 인용은 평문이었다.
     "출처는 밝혀져 있으나 확인이 불편한" 상태가 오래 유지됐다.
  3. **CRLF가 JS 정규식을 깼다.** 리포트 파일이 CRLF인데, JS 정규식에서 마침표는 캐리지리턴을
     **매칭하지 않는다**(줄 종결자로 취급). 그래서 `(.+)$` 가 캐리지리턴 앞에서 멈추고 `$` 에
     실패해 출처 항목이 한 줄도 파싱되지 않았다. 증상이 "출처 0건"이라 헤딩 매칭 실패로
     오진하기 쉬웠다.
  4. **`m` 플래그의 `$`는 모든 줄 끝에 매칭된다.** 출처 섹션을 "헤딩 다음부터 다음 헤딩
     앞까지"로 잡는 정규식을 `m` 플래그로 한 방에 쓰려 했더니 **첫 줄만** 잡혔다.
     게으른 수량자가 첫 줄 끝에서 `$` 를 만나 곧바로 멈추기 때문이다.
     **섹션 추출을 정규식 한 방으로 하지 말 것** — 줄 순회가 짧고 확실하다.
  5. **출처 형식이 세 가지로 섞여 있었다.** `- [1] 제목 — URL` / `- [K1] …`(vector-advanced) /
     `1. (국내) 제목 … URL`(hesse-demian·claude-plugins-guide). 게다가 `**국내**` `**해외**`
     소제목이 목록을 두 개의 `<ol>`로 쪼개, 첫 목록만 훑으면 절반을 놓친다.
  6. **게이트 측정 지점이 스키마와 어긋났다.** CLAUDE.md 정본은 파일 전체 `LC_ALL=C.UTF-8 wc -m` 8,000자다.
     실측하니 기존 리포트 frontmatter 중앙값은 **327자**(최소 228·최대 402)여서 실질 본문 하한은 7,673자였다.
     수학사 노트는 발전단계·출처를 구조화해 담아 frontmatter가 **3,553자**다. 여기에 파일 전체 8,000자를
     그대로 적용하면 실질 본문 하한이 4,447자로 떨어져 **게이트가 3,226자(42%)만큼 몰래 헐거워진다.**
  7. **frontmatter 값이 따옴표로 시작하면 YAML이 깨진다.** `제목: "Barrow and …" (arXiv:1111.6145) — …`
     처럼 인용 스칼라 뒤에 내용이 더 오는 형태. 기존 리포트에도 같은 유형이 1편 있었다
     (`vector-advanced`: `주제: 벡터(Vector) — 확장편: 벡터공간에서…` — 값 안의 콜론).
     **옵시디언도 이 파일의 메타데이터를 못 읽는다.**
- 수정: `index.html`·`math/index.html`에 위키링크·인용 렌더러를 이식했다(마크다운 원본은 미수정).
  62/62편 출처 파싱, 인용 3,267/3,281(100%) 링크화, 위키링크 150/361(42%) 해결.
  나머지 211회는 전부 개념어(MCP·업무 자동화·미분형식…)로 문서가 없는 것이 정상이며 콘텐츠 백로그가 된다.
  자수 게이트는 **본문 기준 7,673자**로 옮겼다(환산 근거는 `tools/verify_math.py` 머리말).
- 다음 방지 규칙:
  - [ ] **문법을 도입하면 렌더러도 같이 넣는다.** `[[위키링크]]`처럼 원본에 쓰이기 시작한 표기는
        렌더 경로가 없으면 조용히 깨진 채 쌓인다. 새 표기를 쓰기 전에 "이게 웹에서 어떻게 보이나"를 확인한다.
  - [ ] **본문 텍스트를 정규식으로 다룰 때 먼저 CRLF를 LF로 정규화한다.**
        JS 정규식에서 마침표는 캐리지리턴을 매칭하지 않는다.
  - [ ] **`m` 플래그와 `$`로 섹션 범위를 잡지 않는다.** 시작 줄만 정규식으로 찾고 끝은 줄 순회로 판정한다.
  - [ ] **게이트 수치를 다른 스키마에 옮길 때는 측정 지점을 함께 환산한다.** 같은 숫자를 쓰는 것이
        같은 높이를 뜻하지 않는다. 환산 근거(중앙값·차이)를 코드 주석과 출력에 남긴다.
  - [ ] **링크 해석에 추측 매칭(부분일치·유사도)을 쓰지 않는다. 틀린 링크가 미해결 링크보다 나쁘다.**
        자동 정규화로 안 잡히면 `link-aliases.json`에 손으로 적고, 무엇을 적을지는
        `verify_math.py --backlog`가 참조 횟수순으로 알려준다.
  - [ ] **frontmatter 값이 `"` 로 시작하면 통째로 작은따옴표로 감싼다.** 커밋 전
        `python3 -c "import yaml,io; yaml.safe_load(...)"` 로 파싱을 확인한다.

## 2026-08-21 — 구글 드라이브 안의 git 클론은 작업 저장소로 쓰지 않는다

허브·다크모드·인용 링크화를 머지한 뒤 로컬을 맞추다가 저장소가 반쯤 부서졌다.

**무슨 일이 있었나.** `git pull` 이 드라이브 잠금으로 중간에 죽었고, 이어 돌린
`git reset --hard` 가 옛 커밋 기준으로 파일을 **지우는 데까지는 성공한 뒤**
새 파일을 **쓰지 못하고** 멈췄다. 결과적으로 `math/` 전체, `research/index.html`,
`tools/*.py` 여섯 개가 작업본에서 사라졌다. 원격과 배포본은 무사했으므로
`git checkout -- <경로>` 를 여러 번 나눠 돌려 전부 되살렸고,
SVG 세 점은 git 블롭과 바이트 단위로 대조해 동일함을 확인했다.

**진짜 원인은 잠금이 아니라 열거 차단이다.** 드라이브는 `math/notes` 와
`math/assets/figures` 를 `UnauthorizedAccessException` 으로 막았는데,
정확한 경로로 파일을 여는 것은 되고 **디렉터리를 나열하는 것만** 안 된다.
그래서 증상이 헷갈린다 — `git status` 는 멀쩡한 파일을 "삭제됨"으로 보고하고,
`git checkout` 은 "File exists" 와 "unable to stat just-written file" 을 동시에 뱉는다.

**가장 위험했던 순간.** 이 상태에서 `build_link_index.py` 를 돌렸더니 math 노트를
하나도 못 읽고 `link-index.json` 을 **math 항목 없이** 다시 썼다. 빌드가 실패로
끝나지 않고 '내용이 줄어든 정상 산출물' 을 낸 것이다. 되돌려서 173건
(research 62 · videos 94 · math 15 · guides 2) 을 복구했다.

- 다음 방지 규칙:
  - [ ] **구글 드라이브 동기화 폴더 안에서 `git reset --hard` 를 쓰지 않는다.**
        지우기는 성공하고 쓰기는 실패해 저장소가 반쯤 부서진 채 남는다.
        되돌릴 때도 파괴적 명령을 다시 쓰지 말고 `git checkout -- <경로>` 로 경로를 나눠 복구한다.
  - [ ] **드라이브 오류가 나면 먼저 "파일이 없는 것" 과 "열거가 막힌 것" 을 가른다.**
        `Test-Path` 가 참인데 `Get-ChildItem` 이 거부되면 내용은 멀쩡하다.
        이때 `git status` 의 "삭제됨" 을 믿고 손대면 진짜로 지우게 된다.
  - [ ] **입력을 디렉터리 순회로 모으는 빌드 스크립트는 0건을 정상으로 취급하지 않는다.**
        `build_link_index.py` 처럼 산출물을 통째로 덮어쓰는 도구는 섹션별 최소 건수를
        확인하고, 이전 산출물보다 크게 줄면 쓰지 말고 실패해야 한다.
  - [ ] **작업용 클론은 드라이브 밖에 둔다.** 같은 원인으로 `.git/index` 손상이
        이미 두 번 있었고 이번이 세 번째다. 원고와 산출물만 드라이브에 두고
        저장소는 로컬 디스크에 두는 편이 맞다.

## 2026-08-22 · 리포 구조 변경을 확인하지 않고 예전 방식으로 작업
- 결함유형: 절차 미확인(빌드 파이프라인 우회) / 산출물 누락
- 무엇이 관찰됐나:
  1. #85~#93 사이에 리포가 크게 재편됐다 — 메인 허브 `index.html`(이룬 서재) 신설, 리서치 갤러리가 `research/`로 이동, 이해편·확장편을 **한 카드로 묶는 짝카드** 도입, `math/` 수학사 아카이브 신설, `tools/build_*.py` 빌드 스크립트 도입.
  2. 그런데 #94에서 나는 **예전 방식대로 `reports/manifest.json`을 손으로 편집**했다. 결과: 새 보고서 2편에 `cat`·`pair`·`track`·`chars`가 빠져 갤러리에서 분류 안 된 낱장 카드로 나열됐고, `link-index.json`이 2026-08-20에 멈춰 **메인 허브에서는 아예 보이지 않았다.**
  3. 절차는 이미 문서화돼 있었다 — `build_reports_meta.py` 독스트링("새 보고서를 추가하면 CATS 에 한 줄 넣고 이 스크립트를 다시 돌린다")과 `MATH_PIPELINE.md` M6("`build_link_index.py` 는 반드시 manifest 뒤에 돌린다"). **읽지 않아서 놓친 것이지 몰라서가 아니다.**
  4. 사용자가 "달라진 부분을 점검하라"고 한 것을 나는 **콘텐츠 정합성**(사실 충돌)으로 읽었으나, 실제 의도는 **리포 구조·양식의 변경**이었다. 세션이 여러 날에 걸쳐 이어질 때 사용자는 그 사이에 리포를 직접 고친다.
- 수정: CATS 등록 후 `build_reports_meta.py` → `build_link_index.py` 순서로 재빌드. cat 없는 보고서 0건, link-index 182→184(비파괴).
- 다음 방지 규칙:
  - [ ] **세션 재개·장기 세션에서는 작업 전에 `git fetch origin main -q && git log origin/main --oneline -15`와 최상위 `ls`를 먼저 본다.** 내가 마지막으로 본 뒤 리포가 바뀌었을 수 있다 — 특히 CLAUDE.md·`*_PIPELINE.md`·`tools/`의 수정 시각을 확인할 것.
    ⚠️ **`fetch` 를 빠뜨리면 이 점검은 통째로 무의미하다**(2026-08-25 실증). `git log origin/main` 은 원격을 보지 않고 **로컬에 캐시된 ref** 를 읽는다. 실제로 2일 묵은 `b63e73d` 를 최신으로 오인해 24커밋(7서가 개편 포함) 뒤에서 작업했고, 커밋 직전에야 충돌로 드러났다. **`git reflog show origin/main --date=...` 로 내 ref 가 언제 갱신됐는지 확인하는 습관을 들일 것.**
  - [ ] **`tools/build_*.py`가 있으면 그 산출물(`manifest.json`·`link-index.json`)을 절대 손으로 편집하지 않는다.** 빌더가 채우는 필드(cat/pair/track/chars)를 사람이 재현할 수 없다.
  - [ ] **보고서 추가 후 반드시 `build_reports_meta.py` → `build_link_index.py` 순서로 돌리고**, `cat` 없는 보고서 0건과 link-index `generated` 날짜가 오늘인지 확인한다.
  - [ ] **"점검"이라는 지시는 범위가 모호하다.** 콘텐츠 정합성만 볼지 구조·양식 변경까지 볼지 갈리면, 둘 다 훑고 어느 쪽을 봤는지 명시해 보고한다.

## 2026-08-22 · 카드 안에 버튼을 넣었더니 파서가 카드 밖으로 빼냈다
- 결함유형: HTML 파싱 규칙 무지 / 눈으로 보기 전에는 안 보이는 결함
- 무엇이 관찰됐나:
  1. 서가를 넘나드는 담기함(`assets/basket.js`)을 붙이며 카드마다 `담기` 버튼을 넣었다.
     블로그·카드뉴스·개념노트의 카드는 모두 `<button class="pc">…</button>` 형태였고,
     그 안에 `<div class="cardfoot"><button class="bskt-add">` 를 넣었다.
  2. 화면은 멀쩡해 보였고 `document.querySelectorAll('.bskt-add').length` 도 20으로 맞았다.
     그런데 `.pc .bskt-add` 는 **한 건도 잡히지 않았다.** HTML 파서가 중첩 `<button>` 을
     만나면 바깥 버튼을 그 자리에서 닫아 버려, 담기 버튼과 `.cardfoot` 이 카드 밖
     그리드 직계 자식으로 튕겨 나가 있었다(`#grid` 자식이 20이 아니라 40).
  3. 개수만 세는 검증은 이 결함을 통과시킨다. 부모–자식 관계를 확인해야 잡힌다.
- 수정: 카드를 `div[role="button"] tabindex="0"` 으로 바꾸고, 잃어버린 Enter/Space 동작을
  `Basket.keyable()` 로 되돌렸다. 포커스 링은 `basket.css` 에서 다시 그린다.
  영상노트·가이드는 카드가 `<a>` 라 파서가 버튼을 그대로 두므로 앵커 안에 두되,
  `preventDefault`+`stopPropagation` 으로 이동과 분리했다.
- 다음 방지 규칙:
  - [ ] **`<button>`·`<a>` 안에 다른 상호작용 요소를 넣지 않는다.** 카드 전체가 눌리는
        UI 에 버튼을 더해야 하면 카드를 `div[role=button] tabindex=0` 으로 바꾸고
        키보드 동작을 직접 붙인다.
  - [ ] **DOM 검증은 개수가 아니라 관계로 한다.** `querySelectorAll('.x').length` 대신
        `document.querySelector('.x').closest('.card')` 또는 `#grid` 직계 자식 구성을 본다.
  - [ ] **`role=button` 으로 바꾼 요소는 포커스 링과 Enter/Space 를 반드시 되살린다.**
        브라우저 기본값이 사라진다.

## 2026-08-22 · 상단바가 두 벌이라 새 서가 셋이 메뉴에서 빠져 있었다
- 결함유형: 같은 목록이 두 곳에 있음(단일 출처 위반)
- 무엇이 관찰됐나: `assets/topbar.js` 의 `TABS` 는 5개(홈·리서치·영상노트·가이드·수학사)에
  멈춰 있었고 허브·리서치·영상노트·가이드·수학사가 이것을 쓴다. 반면 블로그·카드뉴스·
  개념노트는 각자 `<nav>` 를 직접 적어 두었다. 그래서 **허브에서는 새 서가 셋으로 갈 수
  없었고**, 블로그 상단바에는 카드뉴스·개념노트가 빠져 있었다(카드뉴스·개념노트만 7개 정상).
  서가는 늘렸는데 이동 경로를 늘리지 않은 것이다.
- 수정: `topbar.js` TABS 를 8개로. 블로그 `<nav>` 에 두 줄 추가. 파일 머리에 "서가를 새로
  만들면 TABS 와 세 페이지의 `<nav>` 를 함께 고친다" 를 경고로 남김.
- 다음 방지 규칙:
  - [ ] **서가를 추가하면 `index.html` 의 ORDER/LABEL/SHELF, `topbar.js` 의 TABS,
        blog·cardnews·concept 의 `<nav>`, `build_link_index.py` 의 섹션을 한 묶음으로 고친다.**
  - [ ] **상단바 두 벌은 언젠가 합친다.** 합치기 전까지는 위 목록이 유일한 방어선이다.

## 2026-08-22 · 루트에 둔 인쇄 페이지에서 서가 본문의 상대 경로가 어긋났다
- 결함유형: 기준 경로 가정
- 무엇이 관찰됐나: `print.html` 은 루트에 있는데 블로그 본문은 `assets/<슬러그>/thumb.png`
  처럼 **서가 폴더 기준**의 상대 경로를 쓴다. 갤러리(`/blog/`)에서는 맞지만 루트에서는
  한 칸 어긋나 그림이 통째로 깨졌다. `loading="lazy"` 때문에 화면 밖 컷은 인쇄 시
  빈칸으로 나갈 위험도 있었다.
- 수정: 담을 때 기준 폴더(`b`)를 함께 저장하고 `print.html` 이 `rebase()` 로 다시 붙인다
  (없으면 본문 경로의 디렉터리). 위키링크 `a.wl` 은 link-index 가 준 루트 기준이라 제외.
  인쇄본 이미지는 `loading="eager"`.
- 다음 방지 규칙:
  - [ ] **다른 폴더의 본문을 끌어와 렌더하면 기준 경로를 함께 넘긴다.** 본문만 옮기면 깨진다.
  - [ ] **인쇄용 페이지에서는 `loading="lazy"` 를 쓰지 않는다.**
  - [ ] **경로 변환기는 예외 목록을 먼저 정한다** — 절대 URL, `data:`, `#`, 그리고
        이미 루트 기준으로 만들어진 위키링크.

## 2026-08-22 · 개념 한 장 - 워드마크 자산 경로가 끊겨 있었다
- 결함유형: 자산 이동 후 참조 미갱신
- 무엇이 관찰됐나: `engine/brand_signature.py` 가 워드마크 SVG 를 `with_name(...)` 으로
  형제 파일에서 찾는데, 자산은 `engine/assets/` 로 옮겨져 있었다. 새 그림을 렌더하자
  `FileNotFoundError` 로 **낙관 단계에서 통째로 멈췄다.** 기존 두 장은 파일이 옮겨지기 전에
  렌더돼 있어 아무도 몰랐다 - 산출물이 남아 있으면 깨진 파이프라인이 정상처럼 보인다.
- 수정: 두 자리(`assets/`, 형제)를 다 보고, 없으면 찾아본 경로를 밝히며 멈추게 했다.
- 다음 방지 규칙:
  - [ ] **자산을 폴더로 옮기면 참조하는 코드를 같은 커밋에서 고친다.** 옮긴 뒤 한 번 이상
        실제로 실행해 볼 것. 산출물이 이미 있으면 실행 없이는 절대 드러나지 않는다.
  - [ ] **필수 자산은 `exists()` 로 먼저 확인하고 "무엇을 어디서 찾았는지"까지 출력한다.**
        `FileNotFoundError` 스택만으로는 자산이 옮겨진 건지 지워진 건지 알 수 없다.

## 2026-08-22 · 카드 내 균형은 눈으로 잡히지 않는다 - 픽셀로 재기로 함
- 결함유형: 육안 검수에 의존한 품질 기준
- 무엇이 관찰됐나:
  1. `build_concept_sheet.py` v2 의 기록에 "v1 은 요지 한 덩어리라 **카드 아래 절반이 비었다**"가
     남아 있었다. v2 에서 2단 설명을 넣어 완화했지만 **기준이 글로만 있고 검사가 없었다.**
  2. 새로 만든 첫 장을 렌더하니 네 단계 카드가 각각 69·89·69·69px 씩 비어 있었다.
     하단 블록(.box/.calc/.bars)이 `margin-top:auto` 로 바닥에 붙는 구조라 본문이 짧으면
     **카드 가운데가 텅 빈다.** 넘침은 이미 잡고 있었지만 **모자람은 아무도 안 보고 있었다.**
  3. '세 줄 정리' 칸은 더 심해서 **채움이 38%** 였다. 21px 짜리 한 줄이 130px 상자에 있었다.
- 수정: 렌더 직후 DOM 에서 (a) 단계 카드마다 본문 끝과 하단 블록 사이 여백,
  (b) 세 줄 정리 칸의 채움 비율을 재서 보고하도록 했다. 상한을 넘으면 **어느 카드를
  몇 줄 늘려야 하는지까지** 찍는다. 세 장 모두 여백 10~32px · 채움 70% 로 맞췄다.
- 다음 방지 규칙:
  - [ ] **"넘치면 실패"만으로는 부족하다. 모자람도 잰다.** 레이아웃 품질 기준은 상한과 하한을
        함께 갖는다. 넘침은 자동 실패, 모자람은 경고 + 처방(어느 블록을 몇 줄).
  - [ ] **`margin-top:auto` 로 바닥에 붙는 요소가 있으면 그 위 여백을 반드시 잰다.**
        그 구조는 내용이 짧을 때 조용히 빈 공간을 만든다.
  - [ ] **디자인 규칙을 문서에만 적지 않는다.** 지킬 수 있게 만들려면 빌더가 재고 말해 줘야 한다.

## 2026-08-22 · 스펙이 대화에만 있으면 다음 편을 못 만든다
- 결함유형: 재현 불가능한 산출물
- 무엇이 관찰됐나: 개념 한 장 두 장(심슨·최적정지)의 PNG 는 리포에 있는데 **그것을 만든
  스펙은 어디에도 없었다.** `build_concept_sheet.py` 는 있지만 호출하는 코드가 0건이었다.
  그래서 같은 양식으로 한 장 더 만들려면 레이아웃 값을 처음부터 다시 정해야 했다.
- 수정: `studio/concepts/<슬러그>.py` 에 `SPEC` 하나씩, `studio/concept_sheet.py` 가
  list/build/check 를 맡는 구조로 바꿨다. 렌더 전에 스키마(단계 4 · 정리 3 · 흐름 4 ·
  표 칸 수)를 먼저 검사해 렌더 도중이 아니라 입력 단계에서 멈춘다.
- 다음 방지 규칙:
  - [ ] **산출물을 커밋하면 그것을 만든 입력도 같은 커밋에 넣는다.** PNG 만 남기면
        다음 편에서 양식이 갈린다.
  - [ ] **엔진이 있으면 진입점도 만든다.** 호출부 0건인 엔진은 있어도 없는 것과 같다.

## 2026-08-22 · 같은 이름이 두 서가에 있을 때 링크가 엉뚱한 데로 갔다
- 결함유형: 별칭 우선순위
- 무엇이 관찰됐나: 카드뉴스 '생존자 편향'(그림 10장)과 개념노트 '생존자 편향'(이해편 5,800자)이
  이름이 같다. `build_link_index.py` 는 **먼저 등록된 쪽을 유지**하는데 카드뉴스가 앞에 있어,
  글 속의 `[[생존자 편향]]` 이 **설명 없는 그림 묶음으로** 연결됐다.
- 수정: concept 섹션을 cardnews 앞으로 옮겼다. 카드뉴스는 폴더명 별칭
  (`숫자노트_02_생존자편향`)으로 여전히 닿는다.
- 다음 방지 규칙:
  - [ ] **위키링크는 '설명이 있는 쪽'을 가리켜야 한다.** 같은 이름이 여러 서가에 생기면
        등록 순서를 의도적으로 정하고 그 이유를 코드에 적는다.
  - [ ] **빌더가 찍는 "별칭 충돌" 목록을 매번 읽는다.** 조용히 한쪽이 이기고 지나간다.

## 2026-08-22 · 새 페이지가 공용 모듈과 클래스 이름이 겹쳤다
- 결함유형: 전역 이름공간 충돌
- 무엇이 관찰됐나: `backlog.html` 이 탭에 `.t`, 숫자에 `.n`, 서가 배지에 `.sec` 를 썼는데,
  같은 페이지에 얹힌 담기함(`assets/basket.css`)이 이미 `.bskt-row .t` · `.bskt-fab .n` ·
  `.bskt-row .sec` 를 쓰고 있었다. 결과로 `document.querySelectorAll('.t')` 가
  **담기함 패널의 제목 칸까지 잡았고**, 내 CSS 가 담기함 배지 모양을 덮었다.
  담기함이 뜨기 전에는 멀쩡해 보여서 눈으로는 안 잡힌다.
- 수정: `.kt` · `.cnum` · `.shelf` 로 갈랐고, 선택자도 `#tabs .kt` 로 범위를 좁혔다.
- 다음 방지 규칙:
  - [ ] **공용 모듈(`assets/*.css`)을 얹는 페이지에서 한 글자짜리·일반 명사 클래스를 쓰지 않는다.**
        `.t` `.n` `.b` `.sec` `.row` 같은 이름은 언젠가 반드시 겹친다.
  - [ ] **`querySelectorAll` 은 되도록 컨테이너부터 좁힌다.** 전역 선택자는 남의 DOM 을 잡는다.
  - [ ] **얹은 모듈의 UI 를 띄운 상태로 확인한다.** 담기함을 열기 전에는 충돌이 안 보였다.

## 2026-08-22 · 산출물을 따로 돌려야 하면 반드시 잊는다
- 결함유형: 갱신 누락 구조
- 무엇이 관찰됐나: `backlog.json` 은 `link-index.json` 의 별칭이 정해진 뒤라야 뜻이 생긴다.
  그런데 별도 명령으로 두면 글을 쓸 때마다 두 개를 다 기억해야 한다 — 이 리포는 이미
  같은 이유로 사고가 있었다(2026-08-22 · 빌드 파이프라인 우회).
- 수정: `build_link_index.py` 끝에서 `build_backlog.py` 를 함께 돌린다(`--no-backlog` 로 해제).
  백로그가 깨져도 링크 인덱스는 이미 나온 뒤라 서로를 막지 않는다.
  자동화 워크플로 두 곳(`cardnews-render.yml`, Blog 의 `sync-to-seojae.yml`)의
  커밋 대상에도 `backlog.json` 을 넣었다 — 넣지 않으면 자동 커밋마다 백로그만 옛날 상태로 남는다.
- 다음 방지 규칙:
  - [ ] **선행 산출물에 딸린 산출물은 같은 명령에 묶는다.** 순서를 사람이 기억하게 두지 않는다.
  - [ ] **새 산출물을 만들면 그것을 커밋하는 워크플로를 전부 찾아 함께 고친다.**
        `grep -rn "<선행 산출물>" .github/workflows/` 로 시작할 것.

## 2026-08-22 · 폰트가 없는데 "있음" 이라 보고했다 — 카드가 통째로 다르게 그려졌다
- 결함유형: 검사 자체가 틀림 / 조용한 폴백
- 무엇이 관찰됐나:
  1. `cardnews.py doctor` 가 `Noto Sans CJK KR 있음` 이라고 찍었다. 그 말을 믿고
     숫자 노트 1편을 재렌더해 발행본과 대조했더니 **열 장 전부 픽셀의 5~6.6%가 달랐다.**
     차이 영역은 글자 영역 전체였고, 눈으로 보니 원본은 굵고 재렌더는 가늘었다.
  2. 원인은 `document.fonts.check()` 였다. 이 API 는 **없는 패밀리에도 true 를 준다**
     (폴백으로 그릴 수 있으니 "쓸 수 있다"고 답한다). 실제로 이 PC 에는
     Noto Sans CJK KR 이 없고 맑은 고딕으로 그려지고 있었다.
  3. 확인 방법 — 캔버스로 같은 문자열의 폭을 재서 **존재하지 않는 패밀리와 비교**한다.
     같으면 없는 것이다. 이 PC: CJK 635px = 없는이름 635px = 맑은고딕 635px.
- 수정:
  · `doctor` 의 폰트 검사를 폭 비교로 교체(없는 패밀리를 기준선으로 삼는다).
  · `cardnews.py render` 앞에 **폰트 게이트** — 없으면 렌더를 거부한다(`--force-fallback` 로 우회).
  · `tools/cardnews_parity.py` — 해시가 아니라 **픽셀**로 대조하고 같음/미세/다름을 판정한다.
  · 워크플로에 두 겹 확인 — `fc-list` 결과 검사 + 러너에서 `doctor` 실행.
  · `engine/build.py` 의 `/tmp/...` 경로를 `tempfile.gettempdir()` 로. 윈도우에서 as_uri() 가 터졌다.
- 다음 방지 규칙:
  - [ ] **`document.fonts.check()` 로 폰트 존재를 판정하지 않는다.** 없는 패밀리에도 true 다.
        폭을 재서 '존재하지 않는 이름' 과 비교할 것.
  - [ ] **폴백은 조용하다.** 산출물이 그럴듯하게 나오므로 눈으로는 안 잡힌다.
        환경 의존 렌더는 **산출 전에 환경을 검사해 막는다.**
  - [ ] **재렌더로 발행본을 갈아 끼우기 전에 반드시 픽셀 대조한다.**
        해시가 다른 것은 압축 차이일 수도 있어 판단 근거가 못 된다.
  - [ ] **문서에만 적힌 규칙("렌더는 러너가 한다")은 지켜지지 않는다.** 코드로 막아야 한다.

## 2026-08-22 · 상단바가 두 벌이라 서가를 늘릴 때마다 한쪽이 빠졌다
- 결함유형: 같은 목록이 두 곳에 (단일 출처 위반) — 두 번째 발생
- 무엇이 관찰됐나: 허브·리서치·영상노트·가이드·수학사는 `assets/topbar.js` 를 쓰고,
  블로그·카드뉴스·개념노트는 `<nav>` 를 각자 적어 두었다. 서가를 셋 늘렸을 때
  `topbar.js` 쪽이 5개에 멈춰 **허브에서 새 서가로 갈 수 없었다.** 그때는 목록만
  맞추고 구조는 그대로 뒀는데, 그러면 다음번에 똑같이 재발한다.
- 왜 바로 못 합쳤나: 세 페이지는 상단바 안에 **테마 버튼**을 갖고 있었고,
  테마 구현이 페이지마다 다르다(영상노트는 `body` 에, 나머지는 `documentElement` 에
  `data-theme` 를 건다. 버튼 클래스·글리프도 제각각). 여덟 페이지의 테마를 한꺼번에
  통일하는 것은 이 작업의 범위를 넘는다.
- 수정: `topbar.js` 에 **슬롯**(`[data-itb-slot]`)을 두어, 탭 목록만 한곳에서 관리하고
  페이지 고유 컨트롤은 페이지가 갖되 자리는 상단바가 준다. 세 페이지가 직접 적은
  `<div class="bar">` 와 관련 CSS 를 걷어냈다.
- 다음 방지 규칙:
  - [ ] **"목록만 맞추고 구조는 나중에" 는 재발을 예약하는 것이다.** 같은 목록이 두 곳에
        있다는 것을 발견하면 그 자리에서 한 곳으로 만든다.
  - [ ] **한꺼번에 통일할 수 없으면 슬롯을 판다.** 공통부(탭)만 모으고 다른 부분은
        페이지에 남겨 두면, 전면 개편 없이도 단일 출처가 된다.

## 2026-08-22 · 환경 의존 산출물은 "있는가" 가 아니라 "그때와 같은가" 로 잠근다
- 결함유형: 되돌릴 수 없는 환경 의존
- 무엇이 관찰됐나: 개념 한 장 다섯 장이 모두 **Noto CJK 가 없는 윈도우 PC 에서 폴백
  폰트로** 그려졌다. 서로는 일치하니 화면상 문제는 없지만, 나중에 러너에서 한 장을
  새로 그리면 기존 다섯과 갈린다. 게다가 기존 두 장(심슨·최적정지)은 스펙이 남아 있지
  않아 **다시 그릴 수가 없다** — 갈리면 되돌릴 방법이 없다.
- 결정(2026-08-22): 그대로 둔다. 대신 환경을 바꾸지 못하게 기계로 잠근다.
- 수정: `build_concept_sheet.FONT_FINGERPRINT` — 탐침 문자열 네 개의 폭을 재서 기록된
  값과 다르면 렌더를 거부한다. 카드뉴스는 "CJK 가 **있어야** 한다", 개념 한 장은
  "그때와 **같아야** 한다" — 방향이 반대라 헷갈리기 쉬워 코드·CLAUDE.md 양쪽에 적었다.
  선언된 font-family 도 현실에 맞게 `'Noto Sans CJK KR','Malgun Gothic',sans-serif` 로
  고쳤다(출력은 픽셀까지 동일함을 확인).
- 다음 방지 규칙:
  - [ ] **환경 의존 산출물은 환경 지문을 코드에 박아 둔다.** "폰트가 있는가" 가 아니라
        "지난번과 같은가" 를 묻는 것이 옳다 — 조건이 반대인 파이프라인이 한 리포에 공존한다.
  - [ ] **다시 만들 수 없는 산출물이 생기면 그 사실을 즉시 잠금으로 바꾼다.**
        스펙 없는 산출물 두 장 때문에 나머지 셋의 자유도까지 묶였다.
  - [ ] **산출물을 커밋할 때 그것을 만든 스펙도 함께 커밋한다**(2026-08-22 재확인).

## 2026-08-22 · 이미 있는 절차를 두고 "위험하다"고 했다
- 결함유형: 근거 없는 이중 잣대 / 리포 문서 미확인
- 무엇이 관찰됐나: 개념노트(이해편) 집필을 루틴에 넣자는 이야기에 "웹 검증이 많이 필요해
  자동화하면 위험하다"고 답했다. 사용자가 "블로그도 딥보고서도 이미 스스로 검증해서
  쓰고 있는데 뭐가 조심스럽냐"고 물어 `PIPELINE.md` 를 읽어 보니 **내 말이 틀렸다.**
  1. 딥보고서는 트리거 한 줄로 수집(네이버+WebSearch) → fact-ledger → 합성 →
     **측정 게이트**(shell 로 글자·줄·섹션·시각화·출처를 세고 미달이면 머지 금지) →
     **별도 패스 검수**까지 이미 자동으로 돈다.
  2. 게다가 `PIPELINE.md` 에 **"이해편"이 이미 규격으로 들어 있다**(≥8,000자·출처 7).
     리서치 서가에 그렇게 쓰인 이해편이 여럿 있다.
  3. 실제 차이는 눈금뿐이었다 — concept/ 이해편은 카드 10장의 짝이라 5,400~6,400자.
- 진짜 결함은 다른 데 있었다: 개념노트에만 **게이트가 없었다**(math·guide·video 는 있다).
- 수정: `tools/verify_concept.py` 를 기존 5편 실측 최저값으로 만들고 루틴 지침(4.5절)에 넣었다.
  만들자마자 **내가 쓴 세 편의 결함을 잡았다** — 국내 출처를 목록에만 올리고 본문에서
  한 번도 인용하지 않았다(균형을 형식만 맞춘 것). 인용을 붙여 고쳤다.
- 다음 방지 규칙:
  - [ ] **"이건 위험해서 자동화 못 한다"고 말하기 전에 리포에 이미 있는 절차를 확인한다.**
        이 리포는 무환각·측정 게이트·별도 검수를 이미 갖고 있다. 없는 건 대개 게이트뿐이다.
  - [ ] **서가를 새로 만들면 `verify_<서가>.py` 도 함께 만든다.** 게이트 없는 서가는
        규칙이 문서에만 있는 서가다(reports·blog 도 아직 없다 — 남은 빚).
  - [ ] **게이트를 만들면 그 자리에서 기존 산출물에 돌려 본다.** 이번엔 5편 중 4편에서
        미인용 출처가 나왔다. 만들고 안 돌리면 만든 의미가 없다.

## 2026-08-22 · 반입 스크립트가 러너가 그린 그림을 덮을 뻔했다
- 결함유형: 산출물 주인이 둘
- 무엇이 관찰됐나: 본문을 고친 뒤 `ingest_concept.py` 를 돌렸더니 **`concept/assets/*.png`
  세 장이 함께 바뀌었다.** 반입 스크립트는 볼트의 PNG 를 서가로 복사하는데, 같은 날
  개념 한 장의 주인이 **러너**로 바뀌어 있었다(`concept-sheet-render.yml`).
  그대로 커밋했으면 방금 러너에서 그린 판이 로컬 렌더로 되돌아갔을 것이다.
  자수만 갱신하려던 작업이 그림까지 되돌리는 셈이었다.
- 수정: 스펙(`studio/concepts/<슬러그>.py`)이 있으면 그 그림은 러너 소유로 보고
  반입에서 건너뛴다. 건너뛴 사실을 출력한다 — 조용히 넘어가지 않는다.
- 다음 방지 규칙:
  - [ ] **산출물의 주인을 옮기면, 그것을 쓰던 다른 스크립트를 전부 찾아 고친다.**
        `grep -rn "<산출물 경로>" tools/ .github/` 로 시작할 것.
  - [ ] **빌드를 돌린 뒤 `git status` 를 반드시 읽는다.** 의도한 파일만 바뀌었는지
        확인하지 않으면 부수 효과를 그대로 커밋한다.

## 2026-08-22 · 볼트 동기화가 서가마다 제각각이었고, 두 곳에서 조용히 빠지고 있었다
- 결함유형: 규약 불일치 / 자동화 부재로 인한 누락
- 무엇이 관찰됐나: 볼트와 리포를 세어 보니 **리서치 4편·개념노트 3편이 볼트에 없었다.**
  이유는 서가마다 볼트로 가는 길이 달랐기 때문이다.
  · 수학사   `sync_math_obsidian.py` 가 반복 동기화 — 유일하게 안 빠졌지만 **볼트 수정을 덮었다**
  · 리서치·영상·가이드  세션이 글을 쓸 때 **손으로 복사** — 잊으면 그대로 구멍
  · 개념노트  구글드라이브 폴더가 정본 — 클라우드가 갤러리를 갱신할 수 없었다
- 수정:
  · 전부 **"리포가 정본, 볼트는 사본"** 으로 통일. `tools/sync_obsidian.py` 하나로.
  · **없을 때만 쓰고 이미 있으면 건드리지 않는다.** 토요일마다 볼트에서 노트끼리 위키링크를
    거는 작업이 있어 덮으면 매주 사라진다. 리포 노트는 발행 이후 수정된 적이 한 번도 없어
    (수학사 24편·리서치 64편 실측 0건) 잃는 것이 없다.
  · 개념노트 정본을 리포로 옮겼다 — `concept/notes/*.md` 가 프론트매터를 그대로 갖고
    `build_concept_manifest.py` 가 그것으로 manifest 를 만든다. `ingest_concept.py` 폐지.
- 중간에 크게 틀릴 뻔한 것: 처음엔 **파일 이름**으로 "볼트에 이미 있나" 를 판정했다.
  볼트 파일명은 사람이 손으로 붙여 온 것이라 manifest 제목과 다르다
  (`20260622_벡터(Vector) — 왜 배우고 어디까지 가나 (이해편).md` vs 제목 `… 고등학교에서 배우는 …`).
  그래서 60편이 이미 있는데도 "없다" 로 보고 **153편을 새로 만들려 했다.**
  `--dry-run` 이 없었으면 볼트에 중복본을 쏟아부었을 것이다.
  → **파일 안의 식별자**(`#r=슬러그`·`youtu.be/<ID>`·`#n=`)로 판정하도록 고쳤다.
- 다음 방지 규칙:
  - [ ] **사람이 손으로 붙인 이름을 기계 대조의 열쇠로 쓰지 않는다.** 파일 안의 안정된
        식별자를 쓴다. 이름은 사람이 읽으라고 있는 것이지 대조하라고 있는 게 아니다.
  - [ ] **볼트처럼 되돌리기 어려운 대상에 쓰는 스크립트는 `--dry-run` 을 먼저 만든다.**
        그리고 반드시 먼저 돌려 본다. 이번에 그게 사고를 막았다.
  - [ ] **"세션이 잊지 않고 복사한다" 는 자동화가 아니다.** 규칙에만 기대면 반드시 빠진다 —
        실제로 두 서가에서 7편이 빠져 있었다.

## 2026-08-24 · 무한급수 투트랙 — 인용을 나중에 붙였고, 12건이 출처와 어긋났다 (검수 발견)

- 결함유형: 인용-출처 불일치 / 무환각위반 / 인물 오귀속
- 무엇이 틀렸나:
  게이트·스키마·인코딩·문체·수치는 전부 통과했다. **막힌 것은 인용의 신뢰성이었다.**
  독립 검수자가 원출처 11건을 직접 내려받아 대조하자 인용이 출처와 어긋나는 건이 **12건** 나왔다.
  1. **개별 실수가 아니라 한 가지 패턴이었다.** 서술을 먼저 확정하고, 나중에 그럴듯한 URL을 붙이면서
     frontmatter 출처 `제목`에 **그 자료에 없는 내용을 있다고** 적었다. 이런 제목 단정이 4건이다.
  2. 근본 원인은 **WebSearch 응답 본문을 원문으로 착각한 것**이다. 그것은 모델이 검색 결과를
     요약한 텍스트이고, 요약이 나열한 URL이 그 문장을 담고 있다는 보장이 전혀 없다.
     실제로 "다니엘 베르누이가 부분합의 절반 논거를 냈다"는 서술은 검색 요약에서 온 것으로 보이며,
     인용한 위키 문서에는 `Daniel` 이 **0회** 나온다. 인물 이름이 통째로 틀렸다.
  3. 가장 아팠던 것: 아벨 편지 대목은 **"원문을 확인해 유명한 오역을 바로잡는다"가 논지**인데,
     근거로 단 자료에 프랑스어 원문이 없고 악마 판본만 있었다. 논지가 자기 인용에 배신당했다.
  4. 출처 충돌을 볼 수 있는 자리에 있었으면서 한쪽만 골랐다 — 멩골리 1644 vs 1650.
     인용한 PDF는 **둘 다** 싣고 있었다. 방지 규칙 1번(충돌 시 병기)을 이미 어긴 셈이다.
- 수정:
  원출처를 전부 직접 열어 대조했다. `WebFetch` 가 PDF를 못 읽어 `pypdf` 로 로컬 추출해 grep 했다.
  그 결과 (a) 아벨은 프랑스어 전문과 1826-01-16 날짜를 실제로 싣는 자료로 교체(arXiv:1312.5712),
  기존 자료는 "악마 판본을 그대로 싣는 2차 문헌의 실례"로 역할을 바꿔 오히려 논지를 강화,
  (b) 인물 귀속을 지우고 출처가 실제로 말하는 형태(무주체 평균 논거 + 오일러의 기하급수 방식)로 교정,
  (c) 멩골리는 1644·1650 병기, (d) 무근거 문장(1755년 재증명·다니엘 베르누이의 두 우려·마다바 보정항
  세 종류·야코프 베르누이 재증명)은 삭제하고 **검증된 문장으로 대체**해 자수를 오히려 늘렸다.
  덤으로 원문에서 더 좋은 재료가 나왔다 — 아벨 편지의 뒷문장("수학의 가장 본질적인 부분에 기초가 없다"),
  코시가 **1853년에 스스로 오류를 인정**했다는 사실, 오일러가 쓴 방식이 오늘날 아벨 총합법과 거의 같다는 것.
- 다음 방지 규칙:
  - [ ] **WebSearch 응답 본문은 출처가 아니다.** 그것은 모델이 만든 요약이다. `[n]` 을 달기 전에
        그 URL을 실제로 열어 **인용하려는 문장이 거기 있는지 눈으로 확인**한다. 확인 못 하면
        "한 자료에 따르면" 으로 한정하거나 쓰지 않는다.
  - [ ] **frontmatter 출처 `제목`에 내 서술을 옮겨 적지 않는다.** 제목에는 **그 자료가 실제로
        담고 있는 것**만 쓴다. 제목이 곧 검증 대상이고, 틀린 제목은 독자를 정확히 반대로 안내한다.
  - [ ] **인물 귀속(누가 무엇을 주장했나)은 검색 요약이 특히 잘 틀린다.** 귀속을 쓸 때는 원문에서
        그 이름을 grep 해 확인한다. 확인 안 되면 **귀속을 빼고 주장만 적는다** — 무주체 서술이
        틀린 귀속보다 낫다.
  - [ ] **PDF 출처는 `WebFetch` 로 검증되지 않는다**(바이너리를 못 읽는다). `pypdf` 로 로컬 추출해
        grep 한다. 이번에 결정적 대조가 전부 이 경로에서 나왔다.
  - [ ] **무근거 문장을 지울 때 자수 게이트가 깨질 수 있다.** 삭제가 아니라 **검증된 문장으로 대체**한다.
        원문을 실제로 읽으면 대체할 재료는 대개 그 안에 있다 — 이번에도 그랬다.
  - [ ] **작성 컨텍스트에서는 이 결함을 못 잡는다.** 자기가 붙인 인용을 자기가 의심하지 않기 때문이다.
        원출처 대조는 **반드시 별도 검수 패스**에서 한다.
  - [ ] **출처에 "없다"고 판정할 때는 본문만 보지 않는다.** 목차·장 제목·캡션·인용상자에 있을 수 있다.
        재검수에서 실제로 걸렸다 — 자이델·스토크스의 **1847** 은 본문이 아니라 **장 목차**에 있었고,
        본문만 보고 "연도가 없다"고 판정해 정확한 연도를 잃을 뻔했다.
  - [ ] **출처의 역할이나 번호를 바꾸면 그 번호의 사용처를 전부 grep 해 다시 본다.**
        `[14]` 를 "원문 수록" 에서 "악마 판본만 싣는 실례" 로 뒤집으면서 사용처 하나를 놓쳤다.
        역할이 반대로 바뀐 출처는 남은 사용처가 곧바로 자기모순이 된다.
  - [ ] **짝문서는 한 몸이다. 한쪽에서 고친 파생 수치·표현은 반드시 다른 쪽에서 grep 한다.**
        이해편의 "90년"→"한 세기 가까이" 가 확장편 한 곳에 오지 않았다. 연도를 병기로 바꾸면
        거기서 계산된 기간·나이·"N년 만에" 가 전부 딸려 움직인다.

  ※ 이 항목의 규칙은 **검수자 자신도 같은 회차에 어겼다** — 검색 요약을 근거로 "출처 URL을
    HandWiki 판본으로 바꾸라"고 지시했는데, 그 판본에도 해당 서술이 없었다. 규칙 1번은
    작성자만의 것이 아니라 **검수자에게도 적용된다.**
    → **검수 패스에서도 규칙 1·4를 돌린다.** 출처를 교체하라고 지시하기 전에 그 URL을 먼저 연다.

## 2026-08-24 · 확률론 투트랙 — 인용이 틀린 게 아니라 없었다 (검수 발견)

- 결함유형: 무인용 단정 / 축자 중복 / 계획 문서 미갱신
- 무엇이 틀렸나:
  1. **앞 회차의 패턴은 재발하지 않았다.** 원문 11건을 처음부터 열고 수집했고, 검수자가 전수 대조한
     결과 **인용-출처 불일치 0건**, frontmatter 출처 제목 25건이 전부 자료 실물과 일치했다.
     직전 회차의 방지 규칙이 실제로 작동했다.
  2. **대신 게이트가 잡지 못하는 결함이 드러났다 — 무인용 단정.** `verify_math.py` 는 28편 0편
     실패로 통과시켰는데, `[n]` 이 하나도 없는 문단이 **절 단위로 뭉쳐** 있었다(이해편 §1은 12줄에 0개).
     게이트는 출처 **개수**와 dangling 은 세지만 **본문의 어느 문장이 무근거인지는 못 본다.**
  3. 그중 하나는 **리포 전체에 근거가 없었다.** 라플라스를 "무지의 척도"로 요약한 문장인데,
     기존 `person-laplace` 도 같은 문장을 무인용으로 두고 있었다. **새 문서를 쓰다가 기존 노트의
     결함이 드러난 것**이고, 그때까지 아무도 그 문장의 출처를 물은 적이 없었다.
  4. 자기가 단 출처와 **어긋나는** 단정도 하나 있었다. "닫힌 꼴로 안 떨어지는 방법은 쓸 수 없었다"고
     썼는데, 인용한 바로 그 자료가 최소절대편차법이 실제로 쓰였다고(보스코비치 1757 · 라플라스 1789·1799)
     적고 있었다. 출처를 읽고 인용했는데도 **읽지 않은 문단이 반박하고 있었다.**
  5. 축자 중복 2건 — 세기 개관과 일화 노트의 문장을 글자 단위로 그대로 옮겼다. 하필 그 문장 끝에서
     독자를 그 문서로 보내, 링크를 따라간 사람이 방금 읽은 문장을 다시 읽게 돼 있었다.
- 수정:
  무근거 문장을 **지우지 않고 대체했다.** 대체 재료는 대부분 이미 인용한 출처 안에 있었고,
  없는 것은 새 출처를 열어 확인한 뒤 달았다. 그 결과 자수가 8,121 → 9,114 / 10,970 → 12,246 으로 늘었다.
  특히 이해편 §1은 근거 없는 논지에서 **출처 있는 논지**가 됐다 — 드 메레의 두 번째 문제가
  "주사위 하나를 둘로 늘리자 규칙이 깨져 보인다" 였다는 당대 기록이 내 논지의 증언이었다.
  핼리 절은 근거를 못 찾은 부분을 삭제하는 대신 **"이 자료가 말하는 것은 여기까지이고 그 이전
  관행은 말하지 않는다"** 를 본문에 적었다.
- 다음 방지 규칙:
  - [ ] **게이트 통과는 무환각을 보장하지 않는다.** `verify_math.py` 는 출처 개수·중복·dangling 만 본다.
        커밋 전에 **절마다 `[n]` 개수를 세어** 0인 절이 있는지 직접 확인한다.
        `awk '/^## /{s=$0;c=0} /\[[0-9]+\]/{c++} /^## /&&NR>1{print c, s}'` 수준이면 충분하다.
  - [ ] **`[n]` 이 0인 절은 둘 중 하나다** — 근거를 안 달았거나, 근거가 없는 주장이거나.
        전자면 달고 후자면 고친다. **"자명해서 안 달았다"는 대개 후자다.**
  - [ ] **인용할 출처는 해당 문단만 읽지 말고 그 절 전체를 읽는다.** 내 주장을 뒷받침하는 문장 옆에
        내 주장을 **반박하는 문장**이 있을 수 있다. 최소제곱 건이 정확히 그랬다.
  - [ ] **출처가 말하지 않는 것은 "말하지 않는다"고 본문에 적는다.** 근거를 못 찾았다고 문장을 지우면
        논지가 끊기고, 그냥 두면 무근거 단정이 된다. **출처의 한계를 드러내는 것이 셋째 길**이고,
        아카이브의 신뢰를 오히려 높인다.
  - [ ] **새 문서가 기존 노트의 결함을 드러내면 그 자리에서 함께 고치거나 최소한 기록한다.**
        `person-laplace` 의 같은 무인용 문장은 이번에 손대지 않았다 — 다음 인물 노트 작업 때 처리한다.
  - [ ] **다른 노트의 문장을 옮길 때는 요약하거나 링크로 넘긴다.** 축자 복사는 두 문서를 서로
        읽을 필요 없게 만든다. 특히 그 문서로 보내는 링크 옆에서는 더 그렇다.
  - [ ] **계획 문서(`math/ROADMAP.md`)를 회차마다 갱신한다.** 두 회차 연속 누락해서 지적받았다.
        진행 상황·예약 해소·새 예약·계획과 달라진 점을 `## 10. 이력` 에 적는다.
        지도가 현실과 어긋나면 다음 집필자가 틀린 지도를 읽는다.

## 2026-08-25 · fetch 없는 origin/main 확인 = 점검하지 않은 것
- 결함유형: 절차 결함(점검 자체가 무효) / 기반 브랜치 오인
- 무엇이 관찰됐나:
  1. 연속함수 보고서 preflight에서 규칙대로 `git log origin/main --oneline -5` 를 봤고 `b63e73d` 를 최신이라 판단했다. **그런데 `fetch` 를 먼저 하지 않았다.** 그 ref 는 08-22 09:20에 마지막으로 갱신된 것이었고, 실제 main 은 08-22 19:04 ~ 08-24 22:35 사이에 **24커밋**(이룬 서재 7서가 개편, 카드뉴스 스튜디오, 개념노트 파이프라인 등) 앞서 있었다.
  2. 작업을 다 끝내고 **머지 시도에서 405 conflict 로 처음 드러났다.** 충돌 파일은 `link-index.json` 하나 — 빌드 산출물이었다.
  3. 즉 **#96에서 적립한 규칙을 지켰는데도 못 잡았다.** 규칙이 `fetch` 를 포함하지 않아 로컬 캐시를 읽고 "확인했다"고 착각하게 만드는 구조였다.
- 수정: 규칙에 `git fetch origin main -q &&` 를 명시. 충돌은 손으로 병합하지 않고 **`git checkout origin/main -- link-index.json` 후 빌더 재실행**으로 해결(항목 206 → 237). 보고서·manifest 는 깨끗이 리베이스됐다.
- 다음 방지 규칙:
  - [ ] **원격 상태를 묻는 모든 명령 앞에 `fetch` 를 붙인다.** `git log origin/*`·`git diff origin/*`·`git status` 의 ahead/behind 는 전부 로컬 ref 기준이다.
  - [ ] **"확인했다"고 말하기 전에 그 확인이 실제로 원격을 봤는지 자문한다.** 캐시를 읽고 최신이라 보고하는 것은 점검이 아니라 오보다.
  - [ ] **빌드 산출물이 충돌하면 병합하지 말고 재생성한다**(`checkout origin/main -- <산출물>` → 빌더 재실행). 손으로 병합한 산출물은 빌더가 만든 것과 다를 수밖에 없다.
  - [ ] 장기 세션에서 사용자가 **다른 기기에서 작업할 수 있다**는 것을 전제한다. 커밋 간격이 벌어진 세션일수록 재개 시 fetch 는 필수다.

## 2026-08-25 · 카드뉴스 주간 루틴 — 지침이 가리키는 자리에 데이터가 없다
- 결함유형: 절차 결함(지침↔리포 불일치) / 정본 이관 뒤 지침 미갱신
- 무엇이 관찰됐나:
  1. `routines/cardnews-weekly.md` 1절과 예약 프롬프트가 **`cardnews/cardnews.yaml` 의
     `production.next_up` · `production.concept_pool`** 에서 다음 차례를 고르라고 한다.
     그런데 그 파일에는 `production` 절이 **없다**. 개념풀은 `studio/gallery/gallery_meta.py`
     (`CONCEPT_POOL`·`NEXT_UP`·`LAST_PUBLISHED`)에 있는데 그 파일은 **리포에 없다** —
     `studio/cardnews.py` 의 `cmd_list` 가 `import gallery_meta` 를 하지만 로컬 전용이다.
     즉 클라우드 루틴은 **매 회차 지침의 1단계를 수행할 수 없다.**
  2. 이번 회차는 지침이 함께 적어 둔 보조 규칙만으로 정했다 — 시리즈는
     `episodes[].date` 의 시리즈별 최댓값이 가장 오래된 쪽(숫자노트 2026-08-11),
     레이아웃은 A→B→C 회전의 다음 차례(EP3=C → EP4=A). 이건 우연히 규칙이 둘 다
     yaml 안에서 계산되기 때문에 가능했다. **주제만은 규칙이 없어 세션이 골랐다.**
  3. 같은 종류의 어긋남이 하나 더 있다. 예약 프롬프트는 이해편 원고를
     **구글드라이브 '학습자료' 폴더**에 쓰라고 하는데, 2026-08-22에 정본이 리포
     (`concept/notes/*.md`, 프론트매터 포함)로 옮겨졌고 `ingest_concept.py` 는 폐지됐다.
     `routines/cardnews-weekly.md` 4.5절은 이미 갱신돼 있어서 **두 지침이 서로 다른 말을 한다.**
     이번엔 리포(=CLAUDE.md·루틴 문서, 더 최신)를 따랐다.
- 수정: 이번 회차 산출물에는 손대지 않았다(주제 선정 근거를 커밋 메시지에 남김).
  개념풀 자체를 리포로 옮기는 것은 이 루틴의 권한 밖이라 기록만 남긴다.
- 다음 방지 규칙:
  - [ ] **지침이 "A 파일의 B 절을 보라"고 하면 그 절이 실제로 있는지부터 확인한다.**
        없으면 지어내지 말고, 무엇으로 대신 정했는지를 커밋·보고에 남긴다.
        "다음 차례가 없어서 안 만들었다"와 "지침이 가리키는 데이터가 리포에 없다"는 다른 사고다.
  - [ ] **정본을 옮기면 그것을 가리키던 지침을 전부 찾아 같이 고친다.** 리포 안의 문서만
        고치고 예약 프롬프트(claude.ai Routines)를 두면, 지침이 둘로 갈려 회차마다
        어느 쪽을 따를지 판단해야 한다. 2026-08-17 사고의 뿌리("진실의 사본은 하나만")와 같다.
  - [ ] **로컬 전용 파일에 의존하는 절차는 클라우드 루틴의 지침에 넣지 않는다.**
        `gallery_meta.py` 처럼 리포에 없는 파일을 전제하면 루틴은 조용히 절차를 건너뛴다.

## 2026-08-25 · 카드뉴스 렌더 파이프라인 — 결함 넷이 겹쳐 실제 편을 한 번도 못 그렸다
- 결함유형: 파이프라인 무증상 실패 / 검사를 검사 대상보다 먼저 실행 / 오류를 빈 값으로 삼킴 / 한글 경로 이스케이프
- 무엇이 관찰됐나:
  1. `cardnews-render.yml` 이 **크로미움을 설치하기 전에** `cardnews.py doctor` 를 돌린다.
     doctor 는 크로미움을 실제로 띄워 한글 4자 폭을 재는 검사라 브라우저가 없으면
     `Executable doesn't exist ... chrome-headless-shell` 로 죽는다. `bash -e` 라 거기서 끊긴다.
     #110("폰트 없이 렌더하는 길을 막았다")에서 검사를 추가하며 순서를 뒤집었다.
  2. 그 뒤 **3회 연속 실패**했다 — 32568229306(08-22) · 32791931044(08-25) · 32793565614(08-25).
     실패해도 PR 이 안 열릴 뿐 아무 알림이 없어서, 이번 회차에 로그를 열어 보기 전까지 몰랐다.
     같은 리포의 `concept-sheet-render.yml` 은 install 을 `의존성` 단계 안에 두어 정상이다.
  3. 즉 **"조용한 실패를 시끄러운 실패로"** 라던 #102 의 설계 의도가 렌더 워크플로에는
     적용돼 있지 않았다. 루틴은 push 까지만 보고 성공으로 끝내고, 워크플로는 조용히 죽는다.
  4. 순서를 고치자 **두 번째 결함이 드러났다.** `actions/checkout@v4` 가 기본값(fetch-depth 1)
     얕은 클론이라 `git diff ${{ github.event.before }}` 가 `fatal: bad object` 로 죽는다.
     그 실패가 `$( )` 안에서 **빈 문자열로 삼켜져** `skip=1` 이 되고, 이후 렌더·반입·커밋·PR
     단계가 전부 skipped 인데 잡은 **success 로 끝났다**(32793901152). 첫 결함보다 나쁘다 —
     실패조차 아니라서 로그를 열어 볼 이유가 없다.
  5. 그것도 고치고 로컬에서 재현하자 **세 번째 결함**이 나왔다. git 은 기본(`core.quotepath=true`)
     으로 비ASCII 경로를 `"studio/contents/contents_\354\210\253…"` 으로 이스케이프해 돌려준다.
     contents 파일은 **전부 한글 이름**이라 `basename`·`grep` 이 파일을 못 찾고 시리즈는
     `\354\210\253…py"`, EP 는 빈 문자열이 된다. 즉 첫 결함을 고쳐도 **실제 편은 한 번도
     렌더된 적이 없었을 것**이다. 앞선 두 번의 success(run 1·2)는 편을 건드리지 않은 push 였다.
  6. 셋을 다 고치자 편 결정은 성공했는데(`대상: 숫자노트 EP4`) 이번엔 **렌더러가 EP4 를 못 찾았다**.
     `_load_ep` 는 `EPISODES` 원소와 모듈 변수를 **동일성(`v is ep`)으로 대조해** 이름을 얻는다.
     부모·일머리·발견은 `EPISODES = [EP11, EP12]` 라 이름이 잡히지만, **숫자노트만
     `EPISODES` 가 래퍼 dict**(`{"n":4, …, "content": EP4}`)여서 어떤 `EP*` 변수와도 같지 않다.
     그래서 var 가 `#4` 로 떨어지고 키 집합에 `ep4` 가 없다. 파일 안 주석이 이 관례 차이를
     이미 적어 뒀는데, `studio/CLAUDE.md` 와 워크플로는 둘 다 **EPn 으로 부른다.**
     문서와 코드가 다른 말을 하고 있었고, 숫자노트에서만 드러난다.
- 수정: `크로미움 설치` 단계를 분리해 `환경 점검` 앞에 두었다. 중복이던 doctor 호출 두 개도
  하나로 합쳤다. `fetch-depth: 0` 을 주고, before 가 없는 경우(새 브랜치·강제 푸시) 마지막
  커밋만 보는 폴백을 넣었다. git 호출에 `-c core.quotepath=false` 를 붙였고, 시리즈·EP 가
  비면 `::error::` 로 끊게 했다. `_load_ep` 는 `n` 에서 `EPn` 키를 만들어 함께 받게 했다. 로컬에서 실제 커밋 두 개로 두 경로를 모두 재현해
  `시리즈=숫자노트 · 편=EP4` 가 나오는 것을 확인한 뒤 올렸다.
- 다음 방지 규칙:
  - [ ] **환경 점검은 그 환경을 다 갖춘 뒤에 돌린다.** doctor·verify 류를 설치 단계 앞에 두면
        "점검이 통과 못 해서 설치를 못 하는" 순환이 생긴다. 순서를 바꿀 때 이걸 먼저 묻는다.
  - [ ] **`$(...)` 는 실패를 빈 문자열로 바꾼다.** 그 빈 값으로 "할 일이 없다"를 판정하면
        오류가 정상 종료로 둔갑한다. 명령 실패와 "결과가 없음"을 **다른 분기로** 가른다
        (여기서는 `git cat-file -e` 로 before 존재를 먼저 확인).
  - [ ] **한글 경로를 쓰는 리포에서 git 출력을 파싱하면 `-c core.quotepath=false` 를 붙인다.**
        이 리포는 콘텐츠 파일명·폴더명이 전부 한글이라 어디서든 재발한다.
  - [ ] **워크플로를 고쳤으면 로컬에서 그 셸 로직을 실제 커밋으로 재현해 본 뒤 올린다.**
        YAML 파싱 통과는 로직이 맞다는 뜻이 아니다. 세 번째 결함은 재현에서만 나왔다.
  - [ ] **같은 것을 가리키는 이름이 파일마다 다르면 조회 쪽에서 흡수한다.** `_load_ep` 가
        폴더·prefix·제목·번호를 모두 받으면서 정작 지침이 쓰는 `EPn` 만 못 받고 있었다.
        관례가 둘로 갈린 자료구조에서는 **조회 키를 좁히지 말고 넓힌다.**
  - [ ] **파일 안 주석이 "관례가 둘 섞여 있다"고 말하면, 그 둘을 모두 통과하는 시험을 먼저 짠다.**
        `_episodes` 의 docstring 이 이미 경고하고 있었는데 네 시리즈 전부로 확인한 적이 없었다.
  - [ ] **push 로 끝나는 루틴은 그 push 가 부른 워크플로의 결론까지 확인하고 보고한다.**
        브랜치에 올렸다는 사실은 산출물이 나왔다는 뜻이 아니다. 이번엔 확인해서 잡았고,
        확인하지 않았다면 "PR 곧 열림"이라고 보고한 뒤 아무 일도 안 일어났을 것이다.
  - [ ] **워크플로가 실패해도 알림이 없으면 실패는 없는 일이 된다.** 렌더 실패 시
        사람에게 닿는 경로(PR 코멘트·이슈·알림)를 붙이는 것을 다음 과제로 남긴다.

## 2026-08-25 · 스파크 영상노트 — 평문 경로 첫 실측 + 게이트가 못 잡는 태그 결함
- 결함유형: (a) 절차 검증(개선 확인) / (b) 데이터 정합(기능 결함, 무측정 사각)
- 무엇이 관찰됐나:
  1. **평문 경로가 처음으로 바이트 수준 근거를 냈다.** 08-19 회차에서 base64를 옮기다 5건 중 3건을
     손상으로 폐기한 뒤 런북을 `read_file_content`(평문)로 바꿨는데, 이번 회차(xxUbF6EPyfU, 8,756B —
     08-19 최악이었던 7,831B보다 큰 파일)에서 **복원본이 8,757B로 원본과 1바이트(말미 개행) 차**였다.
     2회 독립 호출 전사도 완전 일치, 재읽기 0회. 길이까지 맞았으므로 "한글↔한글 한 글자 치환"
     (길이를 보존해 `fileSize` 대조를 원리적으로 통과하던 유형)도 이번엔 배제된다.
     코퍼스 자문은 `뱃`(보상 뱃지) 1건뿐이었고 badge의 통용 표기라 정상어로 판별했다.
     → **표본 1건이므로 아직 3건 상한을 풀지 않는다.** 회차가 쌓여 같은 결과가 반복되면
     런북 "이 루틴이 없어져야 할 조건"에 따라 상한을 5건으로 되돌린다.
  2. **어떤 게이트도 못 잡는 결함이 독립 검수에서 나왔다.** 스파크가 태그를 `Claude Code`(공백 포함
     영문)로 붙였는데, 갤러리 33편은 `클로드코드`를 쓴다. `verify_video.py`는 태그 **개수**(7~14)만
     세므로 통과했고, 노트는 발행돼도 **태그 필터에서 나머지 33편과 영원히 묶이지 않는다.**
     공백 포함 태그는 95편 중 3개뿐이라 표기 이탈이 명백했다.
  3. 나머지 지적은 전부 스파크 원본에서 상속된 것이었다(리뷰어가 노트↔원본 diff로 먼저 확정).
     4장이 인용 범위를 넘어 "실시간으로 확인"·"작업 처리 상태"라 적어 1·3장의 "컨텍스트 완전 분리·
     최종 결과만 보고"와 정면 충돌했고, 6장은 팀 에이전트 로드맵을 **화자 귀속 없이 확정 사실로**
     단정했다(그 장의 인용은 의존성 판단 기준만 다뤄 전혀 뒷받침하지 않는다).
- 수정: 태그를 `클로드코드`로 정규화(노트+manifest 재생성). 4장·팁4를 인용이 뒷받침하는 정적 구조도
  범위로 축소. 6장·TL;DR 5를 "영상은 …전망한다"로 귀속하고 미확인 영문 병기 `(Agent Team)` 삭제.
  팁5의 "키 입력 → 병합 충돌" 인과 단정 완화. **미수정 2건은 근거를 남겼다** — 3장↔6장 결과 전달의
  긴장은 해소하려면 원본에 없는 "메인 경유" 기제를 지어내야 하고(단방향 전달과 상호 대화는 통상
  양립하므로 진짜 모순으로도 보지 않는다), "분임 처리"는 실재 단어라 문체 흠으로만 뒀다.
- 다음 방지 규칙:
  - [ ] **태그는 개수가 아니라 표기를 봐야 한다.** 인제스트 후 새 태그가 기존 manifest에 없는 표기면
        (특히 같은 개념의 영문/공백/붙여쓰기 변형) 갤러리 다수 표기로 정규화한다. 세는 게이트는
        "필터에서 혼자 떨어져 나가는" 결함을 원리적으로 못 잡는다 — 개수는 정상이기 때문이다.
  - [ ] **제3자 분석본의 결함은 고치기 전에 "노트↔원본 diff"로 전사 결함인지 원본 결함인지 먼저 가른다.**
        원본 결함이면 최소 수정하고 무엇을 왜 고쳤는지 남긴다(그 유형이 반복되면 프롬프트를 고칠 근거).
  - [ ] **미래·로드맵 주장은 화자에게 귀속시킨다.** "…가능해진다"가 아니라 "영상은 …전망한다".
        그 장의 인용이 뒷받침하지 않는 주장일수록 그렇다. 확인 안 된 영문 제품명 병기는 붙이지 않는다.
  - [ ] **모순을 없애려고 원본에 없는 기제를 만들어 넣지 않는다.** 고칠 수 없으면 고치지 말고 보고에 남긴다.

## 2026-08-25 · 해석학의 엄밀화 투트랙 — 1차 사료가 요약본에 없는 논지를 셋 냈다

- 결함유형: (a) 절차 개선(1차 사료 우선의 실효 확인) / (b) 출처가 말하지 않은 것을 옮김 / (c) 도구 전제 무효화
- 무엇이 관찰됐나:
  1. **2차 요약만 봤으면 문서가 훨씬 약했다.** MacTutor 로 골격을 잡은 뒤 데데킨트의 1872년 논문
     영역본(Beman, Open Court 1901 · Project Gutenberg)을 실제로 열었더니, 요약본에 **없는** 것이 셋 나왔다.
     (ㄱ) 그가 **14년을 묵힌 이유를 본인이 밝힌다** — "서술이 단순해 보이지 않았고 **이론 자체가 별
     전망이 없었다**". (ㄴ) 연속성 원리가 **공리임을 그가 명시**한다 — "나는 그것이 옳다는 어떤 증명도
     제시할 수 없고, 누구에게도 그럴 힘이 없다". (ㄷ) 그가 **교육에서는 기하 직관이 불가결하다고
     옹호**한다. 확장편의 중심 논지(§5)와 마무리(§15)가 전부 (ㄴ)(ㄷ)에서 나왔다.
  2. **클라인의 1895년 강연도 같았다.** BAMS 영역본 PDF 를 `pypdf` 로 뽑아 읽으니 산술화에 대한
     **"긍정과 부정 양면" 자기 평가**와 나무 비유가 있었고, 무엇보다 **"배우는 사람은 학문 자체가 크게
     밟아 온 것과 같은 발달 과정을 작게 밟아야 한다"** — 이 아카이브의 전제가 1895년에 이미 문장으로
     있었다. 검색 요약으로는 절대 나오지 않을 재료다.
  3. **자가검수에서 과잉 서술 1건을 잡았다.** 클라인 강연을 "바이어슈트라스 80세 생일 기념 자리"라고
     썼는데 원문은 `whose eightieth birthday we have lately celebrated` — **얼마 전 기념했다**는 회고이지
     이 강연이 그 자리라는 말이 아니다. 번역하며 한 칸 더 나간 것이고, 08-24 미분방정식 회차의
     "출처가 유보한 자리를 단정으로 옮김"과 같은 유형이다.
  4. **출처 둘이 같은 사실에 다른 연도를 준다.** 바이어슈트라스가 실수 이론을 강의에서 시작한 해가
     MacTutor 전기는 **1863/64**, 같은 사이트의 실수 주제 문서는 **1865년 베를린 강의**다. 출판물이
     없어 확정할 근거도 없다.
  5. **`sync_math_obsidian.py` 의 설계 전제가 이번에 깨졌다.** 이 스크립트는 볼트에 이미 있는 노트를
     덮지 않는다 — 사용자가 매주 옵시디언에서 거는 위키링크가 사라지기 때문이고, 주석은 그 근거로
     **"리포 노트는 발행 이후 수정된 적이 없으므로(24편 실측 0건) 잃는 것이 없다"**를 든다.
     그런데 이번 회차는 §8 부수작업으로 **기존 4편**(`century-18c`·`concept-function`·
     `concept-differentiation`·`concept-complex-number-advanced`)에 위키링크를 심었다.
     즉 **전제가 참이 아니게 됐고, 볼트의 그 4편은 리포와 어긋난 채 남는다.**
- 수정:
  (3) 은 "클라인은 얼마 전 그의 80세 생일을 기념했다고 덧붙인다"로 고쳤다.
  (4) 는 한쪽을 고르지 않고 **표에 `1863/64 또는 1865`로 적고 본문에 출처가 갈린다는 것을 밝혔다**
  (규칙 1번 그대로). (5) 는 **`--force` 를 쓰지 않았다** — 사용자의 볼트 위키링크를 지우는 것이
  링크 4개가 사본에서 늦게 반영되는 것보다 나쁘다. 대신 여기에 기록한다.
- 다음 방지 규칙:
  - [ ] **개념 문서의 중심 인물이 논문·강연을 남겼으면 그 원문을 먼저 연다.** 2차 요약은 *무슨 일이
        있었는지*는 주지만 *그 사람이 왜 그랬는지*는 대개 빼먹는다. 이번에 논지 셋이 전부 거기서 나왔다.
        `Project Gutenberg`·`Project Euclid`·`archive.org` 에 19세기 수학 원전 영역본이 많다.
  - [ ] **번역은 인용이다.** 영어 문장을 한국어로 옮길 때 한 칸 더 나가면 그것이 곧 오인용이다.
        `lately celebrated`(얼마 전 기념했다) → "기념하는 자리였다"처럼 **시제·화행이 바뀌는 자리**를
        특히 본다. 번역한 인용문은 커밋 전에 원문과 나란히 놓고 한 번 더 읽는다.
  - [ ] **같은 사이트의 두 문서도 서로 어긋난다.** 출처 충돌은 국내 vs 해외에서만 나는 것이 아니다.
        MacTutor 전기와 주제 문서가 연도를 다르게 적었다. **한 도메인이라고 교차검증을 건너뛰지 않는다.**
  - [ ] **도구의 주석이 근거로 든 실측값에는 유효기간이 있다.** "발행 이후 수정된 적 없다(24편 실측)"는
        그때는 참이었다. 부수작업으로 기존 노트를 고치는 회차가 생기면 그 전제가 깨진다.
        **기존 노트를 고쳤으면 그 사실이 어떤 도구의 가정을 무효화하는지 확인한다.**
  - [ ] **이 환경에서 `curl` 이 막혔다**(SSL exit 35, 2026-08-25). 원문은 PowerShell `Invoke-WebRequest`
        로 받는다. **위키미디어는 그 UA 를 429 로 거부**하므로 위키백과만 `WebFetch` 를 쓴다.
        ROADMAP §11 「착수 전 필수」에도 적어 두었다.

## 2026-08-25 · 해석학의 엄밀화 — 검수 결과: 번역은 0건, "출처가 안 말하는 것"이 5건

- 결함유형: 무인용 단정 / 인용-출처 주제 불일치 / 짝문서 불일치 / 문서 내부 모순
- 무엇이 관찰됐나:
  1. **작성자가 가장 걱정한 자리는 깨끗했다.** 영어 1차 사료 번역 35건과 frontmatter 출처 `제목`
     21건을 검수자가 전수 대조했는데 **왜곡 0건 · 불일치 0건**이었다. 08-24 무한급수 회차의 주
     결함 유형(제목에 자료에 없는 내용을 적음)이 **재발하지 않았다.** 축자 중복도 0건이다.
  2. **대신 다른 축에서 걸렸다 — "출처 목록에 근거가 없는 사실".** 이해편이 "1872년에
     바이어슈트라스가 어디서도 미분되지 않는 연속함수를 내놓았다"를 무인용으로 단정했는데,
     그 사실은 **확장편의 출처 [7]에만 있고 이해편 출처 9개 어디에도 없었다.** 절 단위 `[n]`
     카운트는 7이라 통과한다. 확률론 회차가 만든 그 점검이 **"절에 인용이 있는가"만 보고
     "이 문장의 근거가 이 문서 안에 있는가"는 못 본다.**
  3. **짝문서가 같은 사실을 다르게 처리했다.** 확장편은 바이어슈트라스 강의 연도를
     `1863/64 또는 1865`로 **병기**하고 그 이유까지 적었는데, 이해편 「한눈 요약」은 `1865`로
     **단정**했다. 병기 판단을 내린 사람이 짝문서에 그것을 옮기지 않은 것이다.
  4. **압축이 인과를 앞당겼다.** 본문 §7 의 표는 "3월 14일 하이네 → 결심을 굳혔다 /
     3월 20일 칸토어 → 서문 쓰던 중 도착"을 정확히 구분하는데, **TL;DR 만** "둘이 도착하자
     출판을 결심한다"로 뭉갰다. 원문은 `already half determined … confirmed me in my decision`.
  5. **같은 낱말로 반대 판정을 냈다.** §7 "동시 발표는 **경쟁의 산물**" ↔ §9 "두 사람은
     **경쟁한 것이 아니라** 같은 존재 증명을 두 번 한 것". 둘 다 맞는 말인데 낱말이 겹쳐 모순으로 읽힌다.
  6. **표의 한 칸이 출처에 없는 값을 갖고 있었다.** §16 표의 '출판' 열이 바이어슈트라스 1872년
     함수에 "됨"을 적었는데 [7]은 `discover` 까지만 말한다. 하필 표 바로 아래가
     **"출판 열이 이 표의 논지다"**라고 못박아 그 칸을 하중점으로 만들어 두었다.
  7. **새 문서가 기존 노트의 오류를 드러냈다.** `concept-differentiation-advanced` §7 은 볼차노가
     "교직에서 **해임**"됐다고 적는데, 원문은 **정직(suspended) 후 본인이 사임(resigned)** 이다.
- 수정: 16건 적용. (2)는 MacTutor Weierstrass 를 이해편 출처 10 으로 신설해 달았고, 같은 방식으로
  「실수의 완비성」을 출처 11 로 추가해 √2 반례의 출처를 바로잡았다(그 예는 [4] 「실수계」가 아니라
  [11]에 있다). (3)은 이해편 표를 병기로 맞췄다. (6)은 "됨" → **"미상"** 으로 낮추고 표 아래에
  *무엇을 왜 비워 뒀는지*를 적었다 — 08-24 확률론 회차의 "출처가 말하지 않는 것은 말하지 않는다고
  적는다" 규칙 그대로다. (7)은 기존 노트를 그 자리에서 고쳤다.
- 다음 방지 규칙:
  - [ ] **절별 `[n]` 카운트는 "이 문서에 근거가 있는가"를 묻지 않는다.** 짝문서를 쓸 때는
        한쪽에서 쓴 사실이 **그쪽 출처 목록에도 있는지** 따로 본다. 확장편에서 [7]로 쓴 사실을
        이해편에 옮기면 이해편에는 [7]이 없다. **출처 목록은 문서마다 독립이다.**
  - [ ] **표의 셀도 인용 대상이다.** 특히 그 열이 논지를 지탱한다고 본문이 선언한 경우
        (여기서는 '출판' 열), 셀 하나하나에 근거가 있는지 본다. 없으면 **"미상"으로 낮추고
        왜 비었는지 적는다** — 표는 빈칸을 못 견디게 생겨서 없는 값을 채우게 만든다.
  - [ ] **판단을 내렸으면 짝문서에 옮긴다.** 병기·유보·범위 축소 같은 **판단**은 한쪽에서만 하면
        짝문서가 그것을 배반한다. 08-24 의 "짝문서는 한 몸이다"는 파생 수치뿐 아니라 **판단**에도 적용된다.
  - [ ] **TL;DR 과 요약표는 본문을 압축하다 인과를 앞당긴다.** 본문이 정확히 구분한 것을 요약이
        뭉개는 방향이므로, **요약은 본문을 다 쓴 뒤 본문에서 역으로 검산한다.**
  - [ ] **같은 낱말로 다른 층위를 말하지 않는다.** "경쟁"처럼 폭이 넓은 낱말은 한 문서 안에서
        긍정·부정이 다 가능하다. 한쪽을 좁은 말로 바꾼다(출판 선취 압력 / 수학적 경쟁).
  - [ ] **원문 대조는 별도 패스에서만 잡힌다는 것이 다시 확인됐다.** 작성자가 자가검수로 2건을
        잡았지만(클라인 생일·연도 충돌), 위 5건은 전부 **검수자가 원출처를 다시 열어야** 나왔다.
        자가검수는 검수를 대체하지 않는다.

## 2026-08-25 · 인물 2편(데데킨트·칸토어) — 직전 회차의 결함이 같은 형태로 재발했다

- 결함유형: 출처 목록 밖의 사실 단정(재발) / 나이 계산 오류 / 표 셀의 양태 변경(재발) / 축자 중복
- 무엇이 관찰됐나:
  1. **바로 전 회차에 적립한 규칙을 같은 날 어겼다.** 08-25 엄밀화 회차의 교훈이
     **"출처 목록은 문서마다 독립이다"**였는데, `person-dedekind` 가 리만의 1854년 강연이
     1868년에 출판됐다는 사실을 무인용으로 적었다. 그 근거는 `concept-analytic-geometry-advanced`
     의 출처에만 있고 이 문서에는 없다. **같은 유형이 하나 더 있었다** — 칸토어와의 서신이
     1882년에 끊기고 1897년에 재회했다는 서술은 MacTutor **칸토어** 전기에만 있는데, 그 문서는
     person-dedekind 출처가 아니었다.
     → 규칙을 알고 있어도, **여러 노트를 오가며 쓸 때 사실이 어느 출처 목록에 속하는지는 자동으로 지켜지지 않는다.**
  2. **나이를 계산하지 않고 어림했다.** 도입 인용문에 "이 사람이 **30대에** 실수의 바닥을 찾아냈다"고
     썼는데 1831년생이 1858년에 한 일이므로 **만 27세**다. 문서의 첫 화면이고, 게이트도 검수 전
     자가점검도 산술을 확인하지 않는다.
  3. **표 셀에서 양태가 또 바뀌었다.** 국내 수학백과는 "이 날을 집합론의 진정한 생일로 **볼 수 있다**"
     인데 두 노트의 표가 모두 "이 날이 **집합론의 진정한 생일이다**"로 단정했다. 정작 본문 도입부는
     "국내 수학백과는 …라 **부른다**"로 올바르게 귀속하고 있었다 — **같은 문서 안에서 처리가 갈렸다.**
  4. **어떤 출처에도 없는 문장이 하나 있었다** — "아리스토텔레스 이래 수학이 허용한 것은 앞의
     것뿐이었다". 원문 10편을 전수 grep 한 결과 `아리스토텔레스|Aristotle` 0건이었다.
     배경 지식이 사실 확인 없이 문장으로 새어 들어온 경우다.
  5. **개념 노트와 48자가 글자 단위로 겹쳤다.** "수업에서 쓸 만한 대목"의 한 줄이 강조 표시까지
     같았다. 반면 **같은 1차 사료를 같게 옮긴 번역 인용문의 중복은 결함이 아니다** — 검수자도
     그렇게 판정했다. 중복 판정은 **인용인가 서술인가**로 갈라야 한다.
  6. **좋았던 것도 기록해 둔다.** 번역 24건 전수 대조에서 **왜곡 0건**이었고, frontmatter 출처
     `제목` 17건도 불일치 0건이었다. 직전 회차의 두 주요 유형(번역 왜곡·제목 과장)은 재발하지 않았다.
     국내 인명사전이 MacTutor 와 어긋나는 두 대목(유대계 출신 주장·베를린 초빙 무산)도
     각각 **쓰지 않기**와 **귀속하기**로 정확히 갈랐다.
- 수정: 20건 적용. (1)은 MacTutor **리만** 전기와 **칸토어** 전기를 person-dedekind 출처 10·11 로
  신설해 달았다. 리만 건은 원문을 열어 보니 내가 쓰려던 문장(데데킨트가 1868년에 그 원고를 냈다)은
  **어느 출처도 말하지 않아서**, 출처가 실제로 말하는 두 사실(세 사람에게만 한 강의를 사후 출판한 것 ·
  1854년 원고가 1868년까지 출판되지 않은 것)로 **다시 썼다.** (4)는 삭제했다.
  (5)는 다른 각도로 고쳐 썼다. 이데알 도입 시점이 [1]과 [7]에서 갈리는 것도 검수가 잡아 **병기**했다.
- 다음 방지 규칙:
  - [ ] **다른 노트에서 배운 사실을 옮겨 쓸 때, 그 사실의 출처를 함께 옮겼는지 확인한다.**
        규칙을 아는 것과 지키는 것이 다르다는 것이 하루 만에 증명됐다. 실무적으로는 —
        **새 노트를 쓰다 다른 노트를 참고했으면, 그 문단의 `[n]` 이 이 문서의 출처 목록을
        가리키는지 커밋 전에 한 번 훑는다.**
  - [ ] **나이·기간·"N년 만에" 는 반드시 뺄셈을 한다.** 어림하지 않는다. 인물 노트는 생몰년이
        frontmatter 에 있으므로 본문의 모든 나이 표현을 그 값으로 검산할 수 있다.
  - [ ] **표 셀의 양태를 본문과 맞춘다.** 본문에서 "…라고 적는다"로 귀속했으면 표에서도 귀속한다.
        표는 짧게 쓰려는 압력 때문에 유보가 가장 먼저 떨어져 나가는 자리다.
  - [ ] **배경 지식으로 아는 문장은 출처를 달거나 지운다.** "아리스토텔레스 이래…"처럼 널리
        알려진 것일수록 확인 없이 통과한다. **널리 알려진 것이 이 문서의 출처에 있다는 뜻은 아니다.**
  - [ ] **축자 중복 점검은 서술만 대상으로 한다.** 같은 1차 사료의 번역이 두 노트에서 같은 것은
        일관성이지 결함이 아니다. 검수 지시에 이 구분을 명시할 것.

## 2026-08-26 · 인물 3편(코시·바이어슈트라스·볼차노) — 뺄셈 오류가 이틀 연속 나왔다

- 결함유형: 나이·기간 산술(재발) / 귀속 오류 / 상충 출처 미병기 / 출처가 말하지 않는 인과
- 무엇이 관찰됐나:
  1. **직전 회차의 결함이 같은 형태로 재발했다.** 08-25 에 "30대에"가 만 27세였던 것을 잡고
     방지 규칙까지 적었는데, 이번에는 코시 노트가 **"한 표도 못 받은 사람이 30여 년 뒤 해석학의
     표준을 쓴다"**고 썼다. 1814년 0표 → 1821년 『해석학 강의』는 **7년**이다.
     **바로 다음 절 제목이 "1821년"이고 연보에도 두 해가 나란히 있는데** 놓쳤다.
     같은 문서 §12 가 "평가와 실력의 시차"를 논지로 삼고 있어 숫자가 논지를 망치고 있었다.
     바이어슈트라스 노트에도 같은 유형이 있었다 — 1838년 본 이탈 → 1856년 베를린은 18년인데 "스무 해".
  2. **한 문서 안에서 귀속 처리가 갈렸다.** 코시 노트 **첫 문장**이 "MacTutor 는 …라고 적는다"로
     인용했는데, 그 문장은 MacTutor 가 **벨오스트의 전기에서 인용한 것**이었다. 정작 같은 문서 §7 은
     같은 자료를 "**벨오스트의 서술이 이어진다**"로 정확히 귀속하고 있었다.
     08-25 의 결함 유형 3(같은 문서 안에서 처리가 갈림)과 같다.
  3. **자기 출처 목록 안의 충돌을 병기하지 않았다.** 바이어슈트라스의 1872년 병리적 함수를
     **이 노트의 출처 [4](네이버 「해석학」)가 1874년으로 적는다.** 그런데 같은 노트 §6 은
     실수 이론 시작 연도(1863/64 vs 1865)를 정확히 병기하고 있었다 — **병기할 줄 아는데
     다른 자리에서 안 했다.** 충돌은 국내 vs 해외에서만 나는 것이 아니라 **한 문서의 출처 목록
     안에서도** 난다.
  4. **출처가 말하지 않는 인과를 세웠다.** 볼차노 노트가 "먼저 했는데 이름이 안 남은" 이유를
     **출판 금지**로 지목했는데, 금지는 **1819년 12월**이고 **1817년 논문은 그전에 이미 나왔다.**
     게다가 금지는 뒤에 부분적으로 풀려 **종교·정치가 아닌 것은 낼 수 있었다** — 수학은 막힌 적이 없다.
     MacTutor 가 드는 이유는 **저작 대부분이 원고로 남았다**는 것이다.
     ※ 이 틀은 기존 `concept-analysis-rigor` §5-1 에서 승계한 것이라 **신규 결함이라기보다 전파된 것**이고,
     새 노트가 그것을 `요약` frontmatter 까지 끌어올렸다.
  5. **링크 대칭은 검수 전에 작성자가 잡았다.** 2차 검수가 중단 직전 "링크 대칭 문제로 보이는 것"만
     남기고 끊겼는데, 그 단서로 frontmatter 상호참조를 전수 대조해 **단방향 6건**을 찾았다
     (개념 노트 `기여인물` 에 person-bolzano 누락 2건 + `관련인물` 4쌍). 3차 검수는 비대칭 0건으로 확인했다.
  6. **좋았던 것도 적어 둔다.** 직접 인용 21건 전수 대조에서 **번역 왜곡 0건**, frontmatter 출처
     `제목` 23건 불일치 0건, 표 셀 양태 훼손 0건. 국내 인명사전의 **'소르본 사망' 오류**(MacTutor 는
     Sceaux)도 쓰지 않았다. 08-25 의 세 결함 유형 중 둘은 재발하지 않았다.
- 수정: 34건 적용. (1)은 "일곱 해 뒤"·"열여덟 해 만에"로. (2)는 벨오스트 인용으로 다시 쓰고 원문을
  더 길게 옮겼다. (3)은 §7 에 병기 한 줄을 넣었다. (4)는 **출처가 말하는 이유(원고로 남음)로 바꾸되,
  학계 바깥으로 밀려난 것은 사실이므로 그것과 분리해** 두 층으로 다시 썼다.
  그 밖에 연도 근거 없는 표 셀 4건, 출처에 없는 수식어 5건, 축자 중복 2건을 고쳤다.
- 다음 방지 규칙:
  - [ ] **연도 두 개가 한 문장 안에 있으면 그 자리에서 뺄셈을 적어 본다.** 방지 규칙을 적어 두는
        것만으로는 두 번 다 못 막았다. 실무적으로 — **"N년 뒤/만에/동안"이라고 쓸 때마다 괄호에
        두 연도를 임시로 적고, 커밋 전에 그 괄호를 지운다.** 연보가 있는 인물 노트는 특히
        본문 숫자와 연보를 나란히 놓고 한 번 훑는다.
  - [ ] **2차 문헌이 인용한 3차 인용을 그 2차 문헌의 말로 옮기지 않는다.** MacTutor 처럼
        `is summed up in [4] as follows:-` 로 남의 말을 끌어오는 자료가 많다. **인용 블록 앞의
        한 줄을 반드시 읽고**, 그것이 누구의 말인지 확인한 뒤 귀속한다.
  - [ ] **병기는 문서 단위가 아니라 사실 단위로 판단한다.** 한 자리에서 병기했다고 그 문서가
        안전한 것이 아니다. **자기 출처 목록의 모든 자료가 같은 사실에 다른 값을 주는지**를
        사실마다 확인한다 — 특히 국내 개론 자료는 연도를 다르게 적는 일이 잦다.
  - [ ] **기존 노트에서 가져온 서사 틀도 검증 대상이다.** 볼차노 건은 이미 발행된 개념 노트의
        문장을 이어받은 것이라 의심하지 않았다. **승계한 틀일수록 원문을 다시 열어 시점과 인과를
        확인한다** — 한 번 틀리면 노트를 건널 때마다 증폭된다.
  - [ ] **검수가 중단돼도 그 부분 정보는 쓸 수 있다.** 2차 검수가 "링크 대칭 문제"라는 한 줄만
        남기고 끊겼는데, 그 단서만으로 기계적 전수 대조를 돌려 6건을 찾았다.
        **끊긴 검수를 무효로 치지 말고 남긴 조각을 단서로 쓴다.**

## 2026-08-26 · 하위헌스·야코프 + 일화 2편 — 아카이브가 이미 정한 연도와 충돌했다

- 결함유형: 저장소 내부 사실 충돌 / 나이 산술(3연속 재발) / 상충 출처 미병기 / 승계된 인과
- 무엇이 관찰됐나:
  1. **가장 무거운 것은 출처가 아니라 저장소였다.** `person-jakob-bernoulli` 가 바젤 문제 해결을
     **1737년**이라 적었다. 근거는 MacTutor 의 야코프 전기이고 그 자료는 실제로 1737 이라 쓴다.
     **그런데 같은 아카이브의 5편이 이미 1735 로 확정해 두었고**(`person-euler` §3 제목이
     "바젤 문제 — 1735", `concept-series` 연표, `century-18c` …), 그 근거는 **같은 사이트의
     오일러 전기**와 Wikipedia(1734 해결·1735-12-05 낭독)다. 게다가 `concept-series-advanced` 는
     **1737 을 다른 결과에 이미 배정**해 두었다. 출처 대조만으로는 이 결함이 잡히지 않는다 —
     인용이 출처와 일치했기 때문이다.
  2. **나이 산술이 세 회차 연속 나왔다.** 08-25 "30대에"(실제 27), 08-26 "30여 년"(실제 7)·
     "스무 해"(실제 18), 그리고 이번 `person-huygens` — **요약은 "열여덟 살", 본문은 "스무 살",
     둘 다 "마흔세 해 뒤 예순둘"과 맞지 않는다.** 1691−43 = 1648 이고 그해 그는 만 19세다.
     **출처가 나이를 주지 않아 전부 파생값인데 세 자리가 서로 달랐다.**
  3. **같은 회차 안에서 처리가 갈렸다.** 사슬곡선 1691 을 `person-huygens` 는 "답이 나오는 것은
     1691년"으로 정확히 적었는데, `person-jakob-bernoulli` 표는 **"1691 사슬 곡선을 과제로 걸다"**로
     적었다. 출처는 **풀이가 실린 해**만 말하고 과제를 낸 해는 말하지 않는다.
  4. **승계된 인과가 또 나왔다.** `person-huygens` 가 사이클로이드 진자의 이유를 "원호는 진폭에
     따라 주기가 달라져서"라고 적었는데, 출처가 대는 이유는 **"큰 호를 그리는 진자가 바다에서 더
     쓸모 있으리라 여겨서"**다. 이 틀은 기존 `concept-calculus-of-variations` §4-2(무인용)에서
     넘어온 것이다. 08-26 에 볼차노 건으로 같은 교훈을 적었는데 같은 날 또 걸렸다.
  5. **좋았던 것.** 비문 13행·아나그램 62자를 검수자가 글자 단위로 대조해 **완전 일치**를 확인했고,
     직접 인용 35건에 왜곡이 없었다. 2차 문헌이 인용한 3차 인용(호프만)의 귀속도 정확했다 —
     08-26 에 적립한 규칙이 작동했다.
- 수정: 33건 적용. (1)은 MacTutor 오일러 전기를 출처로 신설해 **병기**하고 파생 수치도 고쳤다.
  (2)는 "열아홉 살 무렵"으로 세 자리를 통일했다. (3)(4)는 출처가 말하는 범위로 되돌렸다.
  그 밖에 극좌표 "최초" vs "이른 사용자" 병기, 출처 제목 오기(학습백과→천문학백과),
  라틴어 철자 정정 사실 명시, 연도 없는 표 셀 2건, 무인용 배경("수학자 여덟 명"), 축자 중복 2건,
  `관련인물` 단방향 8건(+ 선재 `person-barrow` 건까지)을 고쳤다.
- 다음 방지 규칙:
  - [ ] **새 노트에 연도·수치를 적기 전에 그것이 저장소 안에 이미 있는지 grep 한다.**
        `grep -rn "바젤 문제" math/` 한 줄이면 됐다. 출처 대조는 **이 문서가 맞는가**만 보고
        **아카이브가 한목소리인가**는 보지 않는다. 둘은 다른 점검이다.
  - [ ] **출처가 나이를 주지 않으면 나이를 쓰지 않거나, 쓰려면 한 곳에서만 계산해 나머지를 거기서 끌어온다.**
        요약·도입·본문·연보가 각자 계산하면 반드시 어긋난다. 세 회차 연속 걸린 유형이므로
        **"N살"·"N해 뒤"가 한 문서에 둘 이상 있으면 커밋 전에 그것들끼리 검산한다.**
  - [ ] **한 회차에 여러 편을 쓰면 같은 사실의 처리를 편들끼리 대조한다.** 같은 날 쓴 두 노트가
        같은 연도를 다르게 귀속했다. 회차 끝에 **공통 사실 목록을 만들어 한 번 훑는 것**이 싸다.
  - [ ] **일화 유형은 `사실성근거`가 본체다.** 전언("…고 한다")과 단정을 출처별로 갈라 적으면
        판정이 저절로 서고, 독자도 어디까지 믿을지 안다. `episode-bernoulli-tomb` 을 본으로 삼는다.

## 2026-08-26 · 19세기 세기 개관 — 저장소가 이미 "병기한다"고 정한 것을 단정으로 되돌렸다

- 결함유형: 저장소 처리 불일치(재발) / 상충 미병기 / 무근거 연도 / frontmatter 무근거
- 무엇이 관찰됐나:
  1. **08-26 오전에 적립한 규칙을 오후에 어겼다.** `person-weierstrass` 는 병리적 함수의 연도를
     **"출처가 갈린다 — 국내 수학백과는 1874년으로 적는다. 한쪽을 고르지 않고 병기한다"**로 처리해 두었는데,
     세기 개관이 같은 사실을 **1872 단정**으로 되돌렸다(네 자리). 같은 유형이 하나 더 있었다 —
     `person-weierstrass` 가 **"언제까지였는지는 출처가 못박지 않는다"**며 "십수 년"으로 쓴 교직 기간을
     개관이 **"13년"**으로 못박았다.
     → **세기 개관은 20편을 요약하는 문서라 이 위험이 구조적이다.** 요약하면서 원 노트의 *유보*가 떨어져 나간다.
  2. **13개 출처 어디에도 없는 연도가 네 자리에 실렸다** — "크렐레 학술지 **1826년** 창간"(시대이슈·
     세계사사건·본문·연표). 역사적으로 맞는 값이지만 **이 문서의 출처가 말하지 않는다.**
     연표 행이 `[5]`를 달고 있어 **오귀속**까지 됐다. 같은 유형으로 "힐베르트 **23개** 문제"도 무근거였다.
  3. **자기 출처 둘이 갈리는데 병기하지 않았다** — 『익산』을 [11]은 1868, [10]은 1869 로 적는다.
  4. **`시대이슈`·`세계사사건` 은 본문 `[n]` 이 붙지 않는 자리라 점검을 빠져나간다.** 검수가 13개 항목을
     전수 판정해 셋을 걸렀다(1826 크렐레 · 1866 프로이센-오스트리아 전쟁의 인과 해석 · 1900 "23개").
     절별 `[n]` 카운트로는 이 필드가 보이지 않는다.
  5. **승계한 틀이 또 문제였다.** 볼차노 1817 서술을 `concept-analysis-rigor` 에서 이어받으면서
     인용 번호만 [7]→[1]로 바꿨는데, **이 문서의 [1]에는 저작명도 중간값 정리도 없었다.**
     08-26 오전에 볼차노 건으로 같은 교훈을 적었는데 같은 인물에서 또 걸렸다.
  6. **좋았던 것.** 직접 인용 18건 전수 대조에서 왜곡 0건이었고, 조선 재료 [10]~[13]의 모든 항목이
     원문과 일치했다. 국내 수학백과의 "…볼 수 있다"는 양태도 두 자리 모두 지켰다.
- 수정: 47건 적용. MacTutor 볼차노 전기와 함수 개념 주제 문서를 **출처 14·15 로 신설**해 (5)와 1837 건을
  근거 있게 만들었고, (1)은 `person-weierstrass` 와 같은 처리로 통일했다. (2)는 연도를 빼고 서술을
  출처 범위로 줄였다. 조선 부분에서는 『산학정의』가 동서 비교가 아니라 **오류 교정**이라는 것,
  『산학습유』가 **필사본**이라는 것, "혈연"이 아니라 **혼맥**이라는 것을 출처대로 고쳤다.
- 다음 방지 규칙:
  - [ ] **요약 문서를 쓸 때는 원 노트의 유보까지 옮긴다.** 세기 개관·TL;DR·한눈 요약처럼 압축하는 자리에서
        가장 먼저 떨어져 나가는 것이 **"출처가 갈린다"·"출처가 못박지 않는다"** 같은 유보다.
        **원 노트에서 문장을 가져올 때 그 문단에 유보가 붙어 있는지 확인한다.**
  - [ ] **`시대이슈`·`세계사사건` 은 본문과 따로 점검한다.** 절별 `[n]` 카운트가 닿지 않는 필드다.
        커밋 전에 **각 항목이 이 문서의 출처 중 무엇으로 뒷받침되는지** 한 줄씩 자문한다.
        뒷받침이 없는 배경 사실은 실어도 되지만 **인과 해석까지 얹지 않는다.**
  - [ ] **다른 노트에서 문장을 가져올 때 인용 번호를 갈아 끼우지 말고 출처 자체를 옮긴다.**
        번호만 바꾸면 그 번호가 가리키는 자료가 그 사실을 말하지 않을 수 있다. 두 회차 연속 같은 유형이다.
  - [ ] **역사적으로 맞는 값이라도 이 문서의 출처가 말하지 않으면 쓰지 않는다.** 크렐레 1826 은 사실이지만
        무근거였다. **"내가 아는 것"과 "이 문서가 댈 수 있는 것"은 다르다.**

## 2026-08-26 · 20세기 세기 개관 — WebFetch 요약문을 원문 인용처럼 썼다

- 결함유형: **출처 날조(도구 오용)** / 저장소 유보 되돌림(재발) / 자료명 오귀속 / 양태 훼손 / URL 오기
- 무엇이 관찰됐나:
  1. **가장 심각한 것: 출처에 없는 서술을 지어내 인용했다.** §4 에 "MacTutor는 **강연에서 발표된 것과
     출판된 목록이 다르다**고 적는다[2]"라고 썼는데, MacTutor 힐베르트 전기에는 그런 문장이 없다.
     그 페이지는 "23개"를 그냥 단정한다. **그 문장은 `WebFetch` 의 요약 모델이 덧붙인 헤지였다** —
     도구가 돌려준 요약문에 "notes the published list differs from the lecture presentation"이 있었고,
     나는 그것을 원문 서술로 착각했다. 게다가 없는 유보를 근거로 **숫자를 병기하지 않는** 결정까지 내렸다.
     → **`WebFetch` 가 돌려주는 것은 원문이 아니라 소형 모델의 요약이다.** 요약문에 있는 표현을
     "출처가 …라고 적는다"로 옮기면 날조가 된다. **인용할 문장은 반드시 원문 텍스트에서 확인한다.**
     (이번 회차에 실제로 두 번 더 이 검사를 했고, 한 번은 "24 th"처럼 태그가 벗겨져 띄어진 탓에
      처음 검색이 놓쳤다. 없는 것으로 결론 내기 전에 표기 변형을 확인할 것.)
  2. **저장소가 단 유보를 또 되돌렸다(2회 연속).** `concept-analysis-rigor-advanced` 와 `century-19c` 가
     힐베르트 1900년 18개 공리에 대해 **"존재성 증명이 없다는 문제가 남았다"**를 명시해 두었는데,
     개관이 §11 에서 "문제를 지웠다"는 성공담으로 단정했다. **이 문서의 출처 12 원문이 바로 그 대목에서
     정반대를 적는다** — "there was no proof that any such system actually existed."
     게다가 `century-19c` 가 "네 물음 모두 [[20세기]]로 넘어간다"며 이 문서를 가리키는데,
     **네 번째 물음이 이 문서에서 한 번도 다뤄지지 않았다.**
     → **받는 문서를 쓸 때는 넘긴 문서가 무엇을 넘겼는지 목록으로 확인하고 하나씩 지운다.**
  3. **출처 6을 네 곳에서 자료명을 틀리게 불렀다.** 네이버 지식백과 **수학산책**(글 김병한, 과학창의재단)인데
     "국내 **수학백과**"라고 썼다. 같은 문서가 출처 7·9(대한수학회 수학백과)를 "수학백과"라고 부르므로
     독자는 둘을 같은 자료로 읽게 된다. **네이버 지식백과는 하위 콘텐츠명이 여럿이다.**
  4. **원문이 스스로 단 유보를 지웠다.** 수학산책이 "**조금 과장되게 표현하면** … 시기**라 말할 수 있다**"로
     이중으로 물러선 문장을, 개관이 "…시기였다는 것이다"로 단정화했다.
  5. **신설한 출처의 URL 이 404 였다.** 검수가 "적분법 확장편의 출처 5에서 URL 을 그대로 옮겨 오라"고
     했는데 옮겨 적으면서 `Lebesgue_extra` 를 `Lebesgue_scientist` 로 잘못 썼다.
     → **출처를 신설·이관하면 URL 을 실제로 열어 본다.**
  6. **덤으로 기존 노트의 오류를 찾았다.** 그 URL 을 열어 보니 해당 글은 **당주아·펠릭스·몽텔이 함께 쓴
     1957년 논문**이고 동전 비유는 르베그의 말을 인용한 것인데, `concept-integration-advanced` 는
     출처 설명과 본문 두 곳에서 **"몽텔에게 보낸 편지"**라고 적고 있었다. 몽텔은 공저자이지 수신인이 아니다.
     같이 고쳤다. **원문 대조는 새 문서만이 아니라 그 출처를 쓰는 옛 문서도 검증한다.**
  7. **좋았던 것.** 착수 전에 `person-cantor` 를 먼저 읽어, 출처 6이 담은
     "정신병동에서 쓸쓸히 죽었다고 전해진다"는 **저장소가 이미 반박해 둔 통설**임을 확인하고 쓰지 않았다.
     §8 한국 부분은 검수에서 결함 0건이었다 — 여권 건("없애 버렸다"[8] / "압수하였다"[7])과
     명예의 전당(2006[8] / 2017[7])을 실제로 병기했다.
     `연대` 규약 위반(1900~1974 → 1901~2000), "그로부터 20년 뒤"(실제 16년), 다른 노트에서 가져온
     "280년 가까이"는 검수 전에 스스로 걸렀다.
- 수정: 검수 18건에 대해 **29곳 + 후속 5곳**을 고쳤다. MacTutor 「Hilbert's Problems」와
  「Lebesgue, the Scientist…」를 **출처 14·15로 신설**해 (1)과 동전 비유를 근거 있게 만들었고,
  §11·§12 에 존재성 증명 결여를 되살렸다. 출처 15 원문에서 **다르부의 적대, 부시네스크의 "함수는 도함수를
  가질 때에만 흥미롭다", 피카르만이 옹호했다**는 재료를 얻어 §7 을 보강했다.
- 다음 방지 규칙:
  - [ ] **`WebFetch` 결과를 원문으로 취급하지 않는다.** 그것은 소형 모델의 요약이다. 따옴표에 넣거나
        "출처가 …라고 적는다"로 쓸 문장은 **PowerShell 페처로 원문을 받아 그 안에서 찾는다.**
        요약문에만 있는 헤지·수식어는 **출처의 것이 아니라 도구의 것일 수 있다.**
  - [ ] **없는 것으로 결론 내기 전에 표기 변형을 확인한다.** 태그가 벗겨진 텍스트에서는
        `24th` → `24 th`, `x²` → `x 2` 처럼 띄어진다.
  - [ ] **받는 문서는 넘긴 문서의 숙제 목록을 하나씩 지운다.** `century-19c` §12 같은 "다음 세기로" 표가
        있으면 그 항목들이 새 문서에서 실제로 처리됐는지 표로 대조한다.
  - [ ] **네이버 지식백과는 하위 콘텐츠명(수학백과 / 수학산책 / 과학인물백과 / 한국민족문화대백과)을
        본문에서 정확히 부른다.** 한 문서가 둘 이상을 쓰면 독자가 구별할 수 있어야 한다.
  - [ ] **출처를 신설하거나 다른 노트에서 옮겨 올 때 URL 을 반드시 열어 본다.** 그리고 열어 본 김에
        **그 출처를 쓰는 기존 노트의 서술도 맞는지 본다** — 이번에 옛 오류 하나가 그렇게 잡혔다.

## 2026-08-26 · 18세기 도해 3장 — 계산은 스크립트로 했는데 옮겨 적다가 깨졌다

- 결함유형: **좌표 부분추출 오류** / 텍스트-도형 겹침 / 축척 미고지 / 시각 누락 / 서체 규약 분열
- 무엇이 관찰됐나:
  1. **치명적인 것 하나가 "옮겨 적기"에서 나왔다.** 정규곡선 폴리라인을 121점으로 계산해 놓고
     SVG 에는 절반만 실으려고 손으로 걸러 적었는데, 한 점에서 **이웃 점의 y 를 가져다 썼다**
     (`264.36,201.71` 이어야 할 자리에 `266.37` 의 y 인 `202.92`). 1.21px 차이인데
     **하필 "끊긴 데 없는 곡선"이라고 말하는 바로 그 곡선에 눈에 보이는 꺾임을 만들었다.**
     → **계산을 스크립트로 했더라도 사람이 옮겨 적는 단계가 남으면 거기서 깨진다.**
     같은 회차에 사인 곡선 폴리라인도 같은 이유로 한 번 틀렸고, 그때는 스크립트로 재생성해 고쳤다.
     **폴리라인은 손으로 부분추출하지 말고 스크립트가 최종 문자열까지 만들게 한다.**
  2. **게이트도 XML 검사도 렌더링 결함을 하나도 못 잡는다.** `verify_math.py` 는 파일 존재만 보고
     XML 파서는 문법만 본다. 헤드리스 크롬으로 PNG 를 만들어 눈으로 보고서야 셋이 나왔다 —
     부제가 그래프에 겹침, 오른쪽 라벨 두 개가 붙어 "오차 10짜리 하나오차 5짜리 둘"로 읽힘,
     본문이 viewBox 밖으로 잘림. 검수는 여기에 더해 **0.5px·2px 단위의 근접**까지 픽셀 탐침으로 잡았다.
  3. **세리프 서체의 옛날식 숫자(old-style figures)가 함정이다.** `u(x, 0)` 이 Georgia 에서
     **`u(x, o)` 로 읽혔다.** 0 이 x-높이로 내려앉기 때문이다. 숫자만 산세리프 `<tspan>` 으로 떼어냈다.
  4. **정직한 데이터가 그림을 망칠 수 있다.** 첫 관측의 잔차가 0.062 라 정사각형 한 변이 2.48px 이었고,
     반지름 4 인 관측점 마커에 완전히 가려 **정사각형이 6개가 아니라 5개로 보였다.**
     관측값 하나를 바꿔(2.1 → 2.5) 최소제곱을 다시 풀었고, 가장 작은 변이 8.76px 이 됐다.
     **예시 데이터는 논지를 보이도록 고를 자유가 있다 — 다만 고친 뒤 반드시 다시 푼다.**
  5. **두 패널을 나란히 놓으면 독자는 높이를 비교한다.** n=8 과 n=32 의 최고 확률은 0.273 대 0.140 으로
     두 배 가까이 다른데 둘 다 128px 로 그렸다(세로 축척 1.95배 차이). 고지가 없으면
     **"n 이 커져도 최고 확률은 그대로"**라는 정반대 결론을 읽게 된다. 그림 안에 한 줄로 밝혔다.
  6. **좋았던 것.** 좌표 약 200개를 검수가 재계산했는데 **어긋난 것이 (1) 하나뿐**이었다.
     막대 26개가 `C(n,k)/2^n` 과 0.03px 이내, 정규곡선이 막대와 **같은 축척**(914.6 px/확률)이라
     꼭짓점이 최대막대보다 정확히 1.00px 위에 있었다(밀도/확률질량 비 1.00784 그 자체).
     최소제곱 직선은 **기울기를 ±0.05·±0.15 틀어 잔차제곱합이 커지는 것까지 확인**해 정말 최소임을 검산했다.
  7. **§3 의 유보를 그림에 옮긴 것이 검수에서 확인됐다.** `concept-differential-equation-advanced` 는
     인용 자료가 **"누가 어느 쪽을 들었는지는 말하지 않는다"**고 적어 두었다. SVG 안에 인물 이름이
     **하나도 없고**, 마지막 줄이 판정이 아니라 물음이며, 캡션이 유보를 명시한다.
     → **그림은 문장보다 단정하기 쉽다.** 두 모양을 나란히 놓기만 해도 곧바로 두 진영으로 읽힌다.
  8. 캡션이 본문보다 덜 조심스러웠던 자리가 하나 있었다 — "줄은 실제로 왼쪽처럼 튕겨진다"라는
     **물리적 사실 주장에까지 [10]이 걸린 것처럼** 읽혔다. 문장을 끊어 근거 범위를 분명히 했다.
- 수정: 검수 8건 전부 반영. 그중 [4]는 검수가 준 좌표를 그대로 쓰지 않고 **직접 재계산해 대조한 뒤**
  적용했다(전부 일치). frontmatter `이미지:` 키 순서가 저장소에서 3 대 3으로 갈려 있던 것도
  **파일 > 설명 > 출처 > 라이선스**로 통일했다.
- 다음 방지 규칙:
  - [ ] **폴리라인·좌표열은 스크립트가 최종 SVG 문자열까지 출력하게 한다.** 사람이 걸러 옮기지 않는다.
        이번에 같은 유형이 한 회차에 두 번 났다.
  - [ ] **도해를 커밋하기 전에 헤드리스 크롬으로 PNG 를 만들어 눈으로 본다.**
        `chrome.exe --headless --disable-gpu --screenshot=<out>.png --window-size=1240,680 file:///<svg>`
        게이트와 XML 검사는 렌더링 결함을 하나도 잡지 못한다.
  - [ ] **세리프 서체에 숫자를 넣지 않는다.** 넣어야 하면 숫자만 산세리프 `<tspan>` 으로 뺀다.
        `0` 이 `o` 로 읽힌다.
  - [ ] **패널을 둘 이상 나란히 놓으면 축척이 같은지 밝힌다.** 다르면 그림 안에 한 줄로 적는다.
  - [ ] **예시 데이터는 그림에서 보이는지 확인한 뒤 고정한다.** 가장 작은 요소가 마커에 가려지지 않는지 본다.
  - [ ] **원 노트에 유보가 달린 대목을 그리게 되면 그 유보를 그림과 캡션에 옮긴다.**
        인물 이름을 양쪽에 배치하는 것만으로 출처에 없는 대립 구도가 만들어진다.

## 2026-08-27 · 카드뉴스 주간 루틴 — 렌더는 다 됐는데 산출물 하나가 사라지고 PR 이 안 열렸다
- 결함유형: 동시 push 경합(커밋 유실) / 권한 설정으로 인한 파이프라인 정지
- 무엇이 관찰됐나:
  1. 발견노트 EP3(브라에스의 역설) 카피·이해편·개념 스펙을 한 커밋으로 브랜치에 올렸더니
     **두 워크플로가 같은 push 에 동시에 깨어났다** — `cardnews-render`(카피 파일 때문)와
     `concept-sheet-render`(개념 스펙·본문 때문). 둘 다 렌더에 성공했고 둘 다 **같은 브랜치에
     커밋**했다.
  2. 늦게 끝난 `concept-sheet-render` 의 push 가 `! [rejected] (fetch first)` 로 죽었다
     (run 33026612891). 로그에 `create mode 100644 concept/assets/braess-paradox.png` 가
     찍혀 있다 — **그림은 실제로 그려졌고 커밋까지 됐는데, 러너와 함께 사라졌다.**
     실패 로그를 열지 않으면 "개념 한 장이 왜 없지"만 남는다.
  3. 이긴 쪽인 `cardnews-render` 도 실패로 끝났다. 컷 10장을 커밋하는 데까지는 성공했는데
     마지막 `gh pr create` 가 **`GitHub Actions is not permitted to create or approve pull
     requests`** 로 거부됐다(run 33026612885). 리포/조직의
     `Allow GitHub Actions to create and approve pull requests` 가 꺼져 있다.
     로그에는 GraphQL 오류 한 줄만 남아 무엇을 켜야 하는지가 안 적혀 있었다.
  4. 즉 **산출물은 다 나왔는데 두 워크플로가 모두 빨간불**이었다. 루틴이 push 로 끝났다면
     "PR 곧 열림" 이라고 보고한 뒤 아무 일도 안 일어났을 것이다 — 2026-08-25 에 적립한
     "push 로 끝나는 루틴은 그 push 가 부른 워크플로의 결론까지 확인한다" 가 이번에 잡았다.
- 수정: 두 워크플로의 push 를 **rebase 재시도 3회**로 바꿨다(산출물 경로가
  `cardnews/` 와 `concept/` 로 겹치지 않아 충돌하지 않는다). `gh pr create` 실패에는
  무엇을 켜야 하는지를 `::error::` 로 적었다(성공/실패 판정은 그대로 둔다 — 조용히
  넘기면 PR 없이 success 가 된다). 고친 셸 로직은 베어 리포 두 클론으로 **실제 경합을
  재현해** 옛 코드가 지고 새 코드가 이기는 것을 확인한 뒤 올렸다.
  이번 회차분은 개념 한 장을 workflow_dispatch 로 다시 돌리고 PR 은 손으로 열었다.
- 다음 방지 규칙:
  - [ ] **한 push 가 여러 워크플로를 깨우고 그것들이 같은 브랜치에 커밋한다면 경합을 전제한다.**
        `concurrency` 그룹은 **워크플로마다 따로**라 서로 다른 워크플로 사이의 경합을 막지 못한다.
        푸시는 `git push || (git pull --rebase && git push)` 꼴로 재시도하게 둔다.
  - [ ] **"렌더 성공 = 산출물이 브랜치에 있다"가 아니다.** push 단계까지 통과했는지 보고,
        의심되면 `git ls-tree origin/<브랜치>` 로 파일이 실제로 있는지 확인한다.
        러너에만 있던 커밋은 실패 로그에 `create mode ...` 로만 남는다.
  - [ ] **권한·설정이 막은 실패는 오류 메시지에 '무엇을 켜야 하는지'를 적어 둔다.**
        GraphQL 원문 한 줄로는 다음 사람이 30분을 쓴다. 판정을 통과로 바꾸는 것은 답이 아니다.
  - [ ] **파이프라인 실패가 곧 산출물 실패는 아니다.** 어디까지 됐는지를 먼저 가른 뒤
        남은 것만 손으로 메운다(이번엔 PR 한 건과 재실행 한 번이었다).

## 2026-08-27 · 사람 게이트가 자동화를 멈춰 세웠다 — 머지까지가 루틴의 일
- 결함유형: 절차 설계(완료 정의가 짧음) / draft PR 로 인한 머지 불가
- 무엇이 관찰됐나:
  1. EP3 를 push·렌더까지 끝내고 "PR 곧 열림" 으로 보고했다. 그런데 사용자가 본 것은
     **옛 갤러리**였다. main 에 안 들어갔으니 당연했다 — 갤러리는 main 에서만 배포된다.
     Pages 는 멀쩡했고(main 최신 커밋을 정상 배포) main 이 옛 상태였을 뿐이다.
     **"배포가 안 됐다"와 "배포할 것이 main 에 없다"는 다른 사고다.**
  2. 게다가 시스템 지침대로 PR 을 **draft** 로 열어 두어 **머지 버튼이 아예 비활성**이었다.
     사용자가 누르러 왔어도 못 눌렀다. 자동화를 막은 것은 정책이 아니라 이 한 플래그였다.
  3. 사용자 지적 — **"화요일 것은 자동으로 머지했는데? 내가 머지해야 한다면 자동화 의미가 없다."**
     이력이 맞았다. #124(화요일 카드뉴스)를 포함해 최근 PR 들은 생성 후 10~30초 만에
     squash 머지돼 있었다. 즉 **다른 회차들은 이미 스스로 머지하고 있었는데**
     이 루틴만 사람 게이트를 지키다 혼자 멈춰 섰다. 규칙이 회차마다 갈려 있었던 것이다.
  4. 머지하려 하자 **충돌**이 났다. `link-index.json`·`backlog.json` 둘이었고 전부 빌드
     산출물이라 2026-08-25 규칙(main 판본 받고 빌더 재실행)으로 그대로 풀렸다.
     **PR 을 오래 열어 둘수록 이 충돌은 반드시 생긴다** — 늦은 머지의 비용이다.
- 수정: `routines/cardnews-weekly.md` 에 §5.5 를 넣어 **머지·라이브 확인까지 루틴의 일**로
  못 박았다(컷 육안 확인 → non-draft PR → main 병합·산출물 재생성 → 게이트 → squash 머지 →
  **라이브 manifest 를 실제로 받아 확인**). §6 성공 문구, §7 금지 목록, §8 흐름도도 함께 고쳤다.
  이번 건은 그 절차대로 머지해 라이브(개념노트 7편·`discovery03_*`)까지 확인했다.
- 다음 방지 규칙:
  - [ ] **"완료"를 push·PR 로 정의하지 않는다.** 사용자가 보는 곳(라이브 갤러리)에 떴는지를
        **실제로 받아서** 확인해야 완료다. `main 반영 = 라이브 반영` 도 아니다(배포가 남아 있다).
  - [ ] **사람이 눌러야 진행되는 게이트를 두려면 그 사람이 실제로 누를 것인지 먼저 확인한다.**
        안 누르면 게이트가 아니라 **정지점**이다. 무인 루틴에서는 특히 그렇다.
  - [ ] **draft PR 로 열면 머지가 원천 봉쇄된다.** 무인 파이프라인에서는 절대 draft 로 열지 않는다.
        일반 지침("PR 은 draft 로")보다 리포 런북이 우선한다.
  - [ ] **같은 리포 안에서 회차마다 머지 정책이 다르면 그것부터 통일한다.** "이 갈래는 사람이,
        저 갈래는 자동" 은 사용자에게 일관성 없는 시스템으로 보인다 — 실제로 그렇게 보였다.
  - [ ] **머지를 미루면 산출물 충돌이 자란다.** `link-index.json` 류는 main 이 움직일 때마다
        갈라지므로, 만든 날 머지하는 것이 가장 싸다.

## 2026-08-27 · 라이브를 "실제로 받아" 확인할 수 없는 환경 — 대체 증거를 정해 둔다
- 결함유형: 절차 실행 불가(환경 제약) / 검사기 출력 절단
- 무엇이 관찰됐나 (스파크 영상 큐레이터 회차, `6BGIfYbzWz4` 1편 발행):
  1. 같은 날 아침 main 에 들어온 규칙이 **"라이브 갤러리에 떴는지 실제로 받아서 확인해야 완료"**
     라고 못 박았다. 그런데 클라우드 세션에서 `https://irun20000-eng.github.io/...` 를 받으면
     프록시가 **CONNECT 403** 으로 끊는다(`__agentproxy/status` 의 `recentRelayFailures` 에
     `irun20000-eng.github.io:443 connect_rejected` 로 그대로 찍힌다). 5회 재시도 전부 실패.
     즉 그 규칙은 **로컬에서는 맞고 무인 클라우드 회차에서는 실행 자체가 불가능하다.**
  2. 규칙을 못 지키니 "확인 못 했다" 로 끝낼 뻔했다. 그건 완료 정의를 지키는 게 아니라
     **완료 판정을 포기하는 것**이다. 대신 GitHub Actions 의 `pages build and deployment`
     실행 목록에서 **머지 커밋 SHA 로 성공 여부를 대조**했다(`dc5fb209 completed success`).
     이건 "배포가 끝났다" 를 Pages 쪽 기록으로 확인하는 것이라 규칙의 의도를 만족한다.
  3. 부수 관찰 — `pages-build-deployment` 는 `.github/workflows/` 에 파일이 없어
     이름으로 조회하면 404 다. `path: dynamic/pages/pages-build-deployment` 인 동적 워크플로라
     **수치 workflow_id(299661363)로 조회**해야 한다.
  4. 부수 관찰 2 — `actions_list` 응답이 74KB 라 컨텍스트 한도를 넘겨 파일로 떨어진다.
     매번 통째로 읽지 말고 저장된 파일을 `json.loads` 해서 `head_sha/conclusion` 만 뽑을 것.
  5. 부수 관찰 3 — `tools/check_encoding.py` 의 '코퍼스에 없는 음절' 자문은 **8건까지만 찍고
     자른다.** 이번엔 "16개" 라고 했지만 실제 서로 다른 음절은 3종(잼13·쌤1·넉2)이었다.
     횟수와 종수를 헷갈리면 멀쩡한 노트를 폐기하게 된다 — 한 줄 스크립트로 **distinct** 를
     세어 확인했다.
  6. 부수 관찰 4 — `tools/render_parity.py` 는 지금 **항상 실패한다.** 이관 전 1MB HTML 을
     기준으로 ID 집합을 대조하는데, 그 HTML 에는 이관 이후 추가된 23편이 없기 때문이다.
     이번 변경 때문이 아니다. 갤러리 구조를 바꿀 때만 쓰는 이관 대조기이므로 발행 게이트로
     오해하지 말 것.
- 다음 방지 규칙:
  - [ ] **"실제로 받아 확인" 이 막힌 환경이면 대체 증거를 명시하고 그것으로 판정한다.**
        무인 클라우드 회차의 라이브 증거 = **머지 커밋 SHA 의 `pages build and deployment`
        success**. 확인을 건너뛰거나, 확인한 척하지 않는다 — 둘 다 같은 사고로 이어진다.
  - [ ] **보고에는 무엇으로 확인했는지까지 쓴다.** "라이브 확인함" 과 "배포 기록으로 확인함"
        은 다른 강도의 주장이다. 사용자가 강도를 알아야 다시 볼지 판단한다.
  - [ ] **검사기의 "N개" 가 종수인지 횟수인지 확인한다.** 출력이 잘리는 검사기는 특히 그렇다.
        폐기는 되돌릴 수 없으니 세는 단위부터 맞춘다.
  - [ ] **항상 실패하는 검사기는 게이트가 아니다.** `render_parity.py` 처럼 용도가 끝난 대조기는
        실패를 신호로 읽지 말고, 어떤 조건에서만 의미 있는지 알고 돌린다.

## 2026-08-27 · 일화 3편 — 한 출처만 확인하고 "근거 없다"고 판단했다

- 결함유형: **확인 범위 축소** / 출처 오귀속 / 저장소 결정 되돌림(재발) / 인과 역전(재발) / 원문과 반대되는 해석
- 무엇이 관찰됐나:
  1. **스스로 지운 문장이 실은 근거가 있었다.** 초고에 "칸토어는 평면이 직선보다 크다고 믿고 오래
     증명하려 했다"고 썼다가, MacTutor 원문에 없어서 **무근거로 판단하고 삭제**했다.
     그런데 이 문서의 **출처 [1](네이버 수학백과 「무한집합」)에는 그 문장이 그대로 있었다** —
     "평면에는 직선보다 더 많은 점들이 있을 것이라는 생각을 증명하려고 오래 동안 시도하였으나
     성공하지 못하였다." **출처 [2] 만 확인하고 내린 판단이었다.**
     → 무근거로 판정하려면 **그 문서의 출처 전부**를 확인해야 한다. 한 출처에 없다는 것은
     "그 출처에 없다"일 뿐이다.
  2. **같은 문서를 끝까지 안 읽어서 결함이 셋 더 나왔다.** 페아노 1890 을 [2]에 달았는데 [2]에는
     페아노가 없고 [1]에 있었다. "평면과 직선"이 1874년 물음의 형태라고 단정했는데 [1]은 그것을
     1877년 편지의 내용으로 적는다(병기했어야 했다). 셋 다 **같은 한 문서를 통독하지 않아** 생겼다.
  3. **저장소가 명시적으로 교정해 둔 것을 또 되돌렸다(3회 연속).** `person-bolzano` §8 은
     **"출판 금지 때문이라고 말하면 시점이 맞지 않는다 — 금지는 1819년 12월 이후이고 1817년 논문은
     그전에 이미 나왔다"**고 한 절을 통째로 써서 이 통설을 교정해 두었는데, 일화가 다시
     "볼차노의 것은 출판을 금지당했다"로 적었다.
  4. **코시 1830 인과 역전이 재발했다.** 2026-08-26 회차에 `century-19c` 에서 고친 것과 **같은 결함**이다 —
     출처는 ① 파리를 떠났고 ② 그 뒤 선서 요구가 생겼고 ③ 돌아가지 않아 자리를 잃었다고 적는데,
     "선서를 거부하고 떠났다"로 순서를 뒤집었다. `person-cauchy` §8 에도 올바른 순서가 적혀 있었다.
  5. **1차 사료를 인용해 놓고 반대로 해석했다.** 데데킨트 1872년 서문을 인용하며
     "연구할 때는 넘어갈 수 있는데 가르치려니 넘어갈 수 없었다"고 적었는데, 원문은 정반대다 —
     기하 직관에 기대는 것을 **"교육적으로 대단히 유용하고 없어서는 안 된다"**고 하고,
     못 견딘 것은 **그것이 과학적일 수 없다는 점**이라고 적는다.
     **인용문은 맞았고 그 앞뒤 문장을 안 읽었다.**
  6. **좋았던 것.** 인용 전수 대조에서 왜곡 0건이었다(데데킨트 서문·디리클레 추천서·왕비의 말·
     프로이덴탈의 양태·「Did Cauchy plagiarise Bolzano」 논문 실재까지 전부 확인). 연도 계산 4건 모두 정확.
     `person-cantor` §7 이 반박해 둔 "크로네커 박해 → 광기" 틀을 되살리지 않고 학술지 차원의 사실에만
     머문 것도 확인됐다.
- 수정: 검수 9건에 대해 **19곳**을 고쳤다. 삭제했던 문장을 [1] 근거로 복원했고, 1874년 편지 인용의
  잘린 뒷부분("답이 '아니오'라는 것은 너무 분명해서 증명조차 거의 필요 없어 보인다")을 되살렸으며,
  MacTutor 가 강조한 **"그 대응은 연속사상이 아니다"**를 추가했다.
- 다음 방지 규칙:
  - [ ] **"이 문서의 출처에 없다"고 판정하기 전에 그 문서의 출처를 전부 확인한다.**
        한 출처에 없다는 것은 그 출처에 없다는 뜻일 뿐이다. 이번에 그 착각으로 맞는 문장을 지웠다.
  - [ ] **출처를 새로 넣을 때 그 문서를 통독하고 `출처.제목` 필드를 먼저 확정한다.**
        그 다음에 본문 각주를 단다. 이번 결함 셋이 "같은 문서를 끝까지 안 읽어서" 생겼다.
  - [ ] **인물이 겹치는 노트를 쓸 때 그 인물 노트의 "교정" 절을 먼저 읽는다.**
        `person-bolzano` §8, `person-cantor` §7 처럼 **통설을 반박하려고 따로 쓴 절**이 있다.
        3회 연속 같은 유형으로 걸렸다.
  - [ ] **1차 사료를 인용할 때 인용 구간의 앞뒤 문단까지 읽는다.** 인용문 자체는 맞는데 해석이
        원문과 반대가 될 수 있다. 이번 데데킨트 건이 그 사례다.

## 2026-08-27 · 허브 검색 — 서가를 합쳐 놓고도 교차점을 못 찾고 있었다

- 결함유형: 통합 배선 미완 / 코드가 세는 숫자와 사람이 적은 문구의 노후 불일치
- 무엇이 관찰됐나 (사용자 질문 "여러 작업을 하나의 서재로 합치는 데 혼돈은 없는가"에서 출발):
  1. **합치기는 잘 돼 있었다.** 슬러그·제목 충돌 0건, 최신 12줄이 일곱 서가에 고르게
     퍼져(영상이 96편인데도 잠식하지 않음), 모든 줄에 서가 배지가 붙는다.
     서가 개수·편수도 코드가 센다. 여기까지는 문제가 없었다.
  2. **그런데 찾기가 합쳐지지 않았다.** 허브 `hits()` 가 **제목 문자열만** 봤다.
     `수학` 으로 찾으면 96편이 아니라 **블로그 7편**만 나왔다 — 수학사 노트 제목이
     `적분법`·`무한급수` 라 '수학'이라는 글자가 없기 때문이다.
     `미적분` 도 6편뿐이었다(실제로는 32편).
     **서가를 늘릴수록 이 구멍이 커진다** — 서가마다 제목 짓는 관례가 다르기 때문이다.
  3. **드러난 자리는 '묶어 읽기'였다.** 「수학 공부법」 묶음이 *"개념 보고서와 블로그 글,
     카드뉴스가 한 주제에서 만나는 지점"* 이라고 소개해 놓고 실제로는 **블로그만** 내놓았다.
     소개 문구가 거짓이 된 것을 아무도 못 봤다 — **편수는 코드가 세서 늘 맞았기 때문에
     오히려 안심하고 있었다.** 자동으로 맞는 숫자 옆에 손으로 적은 문구가 있으면
     숫자가 문구의 노후를 가려 준다.
  4. 고친 뒤 확인은 **`index.html` 에서 `hits()`·`countBy()`·DOCS 매핑을 그대로 떼어
     node 로 돌렸다.** 파이썬으로 같은 로직을 다시 짜서 확인했더니 그건 '내가 이해한 것'을
     검증할 뿐 배포되는 코드를 검증하지 못했다. 실제 코드를 떼어 돌리자 `LABEL` 미정의 같은
     스코프 문제가 바로 드러났다.
- 수정: `link-index.json` 각 항목에 `검색어`(갤러리 태그를 모은 공백 연결 문자열)를 싣고,
  허브 검색이 제목+검색어를 보되 **제목에 걸린 것을 앞에** 두게 했다. 낡은 소개 문구 3건 교체.
  6개 묶음 중 5개가 다서가로 바뀌었다(수학 7→96, 미적분 6→32).
- 다음 방지 규칙:
  - [ ] **서가를 늘리면 "합쳤나"가 아니라 "가로질러 찾히나"를 확인한다.** 카드가 보이고
        편수가 맞는 것은 통합의 절반이다. 나머지 절반은 **한 서가의 말로 다른 서가가
        찾히는가** 이고, 제목 검색만으로는 성립하지 않는다(서가마다 제목 관례가 다르다).
        점검법: 대표 주제어 3~4개로 검색해 **결과가 몇 개 서가에 걸치는지** 센다.
  - [ ] **코드가 세는 숫자 옆에 손으로 적은 문구를 두면 문구만 조용히 낡는다.**
        숫자가 맞으니 그 블록 전체가 맞다고 믿게 된다. 검색·필터 동작을 바꿀 때
        **그 결과를 설명하는 문구를 같은 커밋에서 다시 읽는다.**
  - [ ] **프런트엔드 동작은 같은 로직을 다시 짜서 확인하지 말고 그 파일에서 떼어 돌린다.**
        재구현 검증은 '내가 이해한 것'만 검증한다. `index.html` 에서 함수 본문을 잘라
        `new Function` 으로 실행하면 브라우저 없이도 배포본 그대로를 돌릴 수 있다.
  - [ ] **모든 페이지가 받아 가는 파일에 필드를 더할 때는 덤프 모양까지 계산한다.**
        `indent=1` 에서 리스트는 원소마다 한 줄을 먹는다. 태그를 리스트로 실었으면
        파일이 배로 부풀었을 것이다 — 부분일치 검색에는 문자열이면 충분했다.
        (실제 증가: 119KB→157KB, gzip 30KB→41KB.)

## 2026-08-28 · 20세기 일화 3편 — 인용을 한 문장 앞에서 끊어 출처를 뒤집었다

- 결함유형: **인용 절단으로 인한 의미 반전(재발)** / 선언한 유보의 누출 / 무근거 시점 특정 / 무근거 단정
- 무엇이 관찰됐나:
  1. **출처가 바로 다음 문장에서 부정하는 것을 단정으로 실었다.** 스탠퍼드 철학백과가
     "폰 노이만이 11월 20일 편지로 제2정리에 해당하는 따름정리를 알렸다"고 적은 데서 인용을 끊고
     **"듣는 쪽이 한 걸음을 더 갔다"**로 맺었는데, 원문의 **바로 다음 문장**이 이렇다 —
     *"Meanwhile Gödel himself, however, had found the same idea and had already sent the final version
     of his article, which now contained also a statement of the second theorem."*
     **괴델이 이미 찾아 최종본을 보낸 뒤였다.** 정반대다.
     → **08-27 회차의 데데킨트 서문 건과 같은 유형이다**(인용문은 맞고 앞뒤를 안 읽음). 이틀 연속이다.
  2. **선언한 유보가 두 군데서 새어 나갔다.** 이 문서는 §"얼마나 확실한 이야기인가"에서
     **"흔히 이야기되는 '연설 바로 다음 날'은 쓰지 않는다"**고 밝혀 놓고, 다른 절에서
     **"1년도 안 되어"**로 연설 시점을 특정했고(성립하려면 연설이 1930년 7월 이후여야 한다),
     배치로 선후를 암시했다.
     → **유보를 선언하는 절과 그 유보를 지켜야 하는 절은 다르다.** 선언한 뒤 문서 전체를 그 기준으로
     한 번 더 훑어야 한다. 제목·요약·표·수업 대목까지.
  3. **적대적 반응의 시점을 근거 없이 못박았다.** "1902년 학위논문이 나왔을 때 이야기다"라고 썼는데,
     회고 논문은 "학위논문에 이어진 다른 연구들"까지 묶어 적고, MacTutor 는 **1904·1906년 두 저작을
     설명한 직후에** 적대적 반응 문장을 놓는다. 게다가 `century-20c` 는 1901년 논문과 1902년 학위논문을
     일부러 갈라 두었는데 일화가 1901년을 통째로 지웠다.
  4. **"자료가 말하지 않는다"를 세 번 지키고 한 번 놓쳤다.** 남대문 편은 학술지가 왜 거기 있었는지를
     "자료가 말하지 않는다"로 정확히 처리해 놓고, **"당시 그에게는 국제 학술지에 논문을 낼 통로가
     없었다"**는 기록되지 않은 사정을 단정했다. 초른이 왜 대신 투고했는지는 두 자료 모두 침묵한다.
  5. **좋았던 것.** 통설을 쓰지 않기로 한 판단이 옳았음이 확인됐다(검수가 세 출처 전문을 훑어
     날짜가 없음을 확인). MacTutor 가 힐베르트 출생지를 "Wehlau, near Königsberg"로 적는 것을 보고
     **"그가 태어난 도시"가 아니라 "그가 태어난 지방의 도시"**로 쓴 것, 부시네스크를 전언으로 옮긴 것,
     1차 사료를 헤드리스 렌더까지 해서 확인한 것(그 과정에서 DOI 오기를 잡았다)이 함께 확인됐다.
- 수정: 검수 10건에 대해 **19곳**을 고쳤다. `century-20c` 의 같은 축약("막스 초른" → "막스 아우구스트 초른")과
  "1929년 **한** 지도"(인명 하안 Hahn 이 수사로 읽힌다)도 함께 손봤다.
- 다음 방지 규칙:
  - [ ] **인용을 끊는 자리 바로 다음 문장을 반드시 읽는다.** 이틀 연속 같은 유형으로 걸렸다.
        특히 **인용에서 결론을 끌어낼 때** — 원문이 그 결론을 곧바로 뒤집는 경우가 있다.
  - [ ] **유보를 선언했으면 문서 전체를 그 기준으로 한 번 더 훑는다.** 선언한 절만 지켜지고
        본문·표·수업 대목에서 새는 일이 있다. "쓰지 않기로 한 정보"를 검색어로 삼아 전수 확인한다.
  - [ ] **"…했을 때 이야기다"로 시점을 묶기 전에 출처가 그 시점을 묶는지 본다.**
        자료가 여러 해에 걸친 것을 한 해로 못박는 것은 무근거 단정이다.
  - [ ] **자바스크립트로 그려지는 페이지는 헤드리스 크롬으로 확인한다.** AMS 논문 페이지처럼
        raw HTML 에 서지가 없는 곳이 있다. 이번에 그 과정에서 DOI 오기를 잡았다.

## 2026-08-28 · 인물 3편(20세기) — MacTutor 의 인용은 발췌다

- 결함유형: **발췌 인용에서 결론 끌어내기** / 오귀속 / 산술 오류 / 최상급 과장 / 무근거 시점
- 무엇이 관찰됐나:
  1. **줄임표 안에 논지가 있었다.** `person-hilbert` 는 1900년 강연을 인용하며 "모든 수학 문제는 반드시
     엄밀한 해결이 가능하다"를 축으로 삼고 "31년 뒤 반증되는 것이 정확히 이 문장"이라 적었다.
     그런데 MacTutor **전기**가 옮긴 발췌는 줄임표로 두 군데를 잘라 놓았다. 강연 전문을 보면 —
     (ㄱ) 힐베르트는 그 확신에 **"모든 수학자가 공유하지만 어느 누구도 아직 증명으로 뒷받침한 바 없는"**
     이라는 괄호를 달았고, (ㄴ) "엄밀한 해결"이 **답을 주거나, 해결이 불가능하다는 증명이 나오거나 둘 중 하나**
     라는 뜻이었다. 그는 평행선 공리처럼 **"애초 의도와는 다른 뜻에서나마"** 해결된 옛 문제들을 근거로 들었다.
     → **인용에서 결론을 끌어낼 자리라면 발췌가 아니라 원 강연·원 논문을 찾는다.**
     MacTutor 전기 페이지는 대개 그 전문으로 가는 링크를 따로 달아 둔다.
  2. **출처 설명 필드에 그 출처가 말하지 않는 것을 적었다.** `person-godel` 의 출처 6(수학백과 「무한집합」)
     설명에 "유리수 집합보다 크고… 괴델과 코헨에 의해 풀린다"를 적었는데, 그 문서에는 **'괴델'도 '코헨'도
     한 번도 나오지 않는다.** 그 문구는 출처 4의 것이다. 그 문서는 "**자연수** 전체의 집합보다 크기가 크고"로 적는다.
     → 08-27 회차의 "출처 전문을 통독하고 `출처.제목` 을 먼저 확정한다"는 규칙을 **한 자리에서 어겼다.**
  3. **작은 산술 둘.** 요약에서 1931년 괴델의 나이를 "스물다섯"으로 적었는데 저장소가 이미 24로 못박아 두었고
     (같은 노트의 표도 24다), 괴델 §9에서 "여덟 살 + 일흔한 해"로 적어 79세가 됐다(향년 71세).
     **같은 문서 안에서 숫자가 어긋났다.**
  4. **최상급과 시점을 근거 없이 올렸다.** "One of Gödel's closest friends"를 "가장 가까운 친구"로,
     "over many years"를 "만년의"로 옮겼다. 원문은 시기를 적지 않는다.
  5. **인용을 다른 문맥으로 옮겼다.** 콜모고로프의 제자 회고("피곤하고 수학적 착상으로 가득한 채…")는
     1930년대 코마롭카의 **대학원생** 소풍 이야기인데, 1960년대 **영재학교 아이들** 절에 옮겨 놓아
     아이들의 회고처럼 읽혔다. → 인용은 원문에서 그것이 놓인 문단째로 옮긴다.
  6. **좋았던 것.** 지난 두 회차에 연속으로 났던 "인용 앞뒤 안 읽기"가 **핵심 두 곳에서 막혔다.**
     괴델 §5는 폰 노이만 편지 뒤의 "그사이 괴델 본인도 최종본을 이미 보낸 뒤였다"를 실었고,
     콜모고로프 §6은 MacTutor 의 조건절("If … he completely solved …")을 읽어 6번은 "확률 부분에 큰 기여,
     '완전히 풀었다'는 쓰지 않는다"로, 13번은 "완전히 풀었다"로 강도를 갈랐다.
     `episode-koenigsberg-1930` 이 정한 유보("연설 다음 날"을 쓰지 않는다)도 세 노트가 모두 지켰다.
- 수정: 검수 14건에 대해 **22곳**을 고쳤다. 강연 전문을 **출처 9로 신설**해 (1)을 근거 있게 만들었고,
  "코언"→"코헨"(저장소 표기 통일), 학위 연도(1884 구술시험/1885 학위), 7번 문제 표현, 빈 대학 입학 연도
  병기(1924[4] / 1923[1]), 콜모고로프 분야 목록의 여섯 번째 항목("동역학계"→"수학사와 방법론")도 고쳤다.
- 다음 방지 규칙:
  - [ ] **전기의 인용은 발췌다.** 줄임표가 있으면 그 안에 무엇이 있었는지 확인한다.
        인용에서 논지를 끌어낼 때는 원 강연·원 논문 전문을 찾는다.
  - [ ] **`출처.제목` 필드에 적기 전에 그 문서에서 해당 문자열을 실제로 찾는다.**
        두 출처의 내용을 섞어 적는 일이 생긴다.
  - [ ] **같은 문서 안의 숫자를 커밋 전에 맞춰 본다.** 나이·연도·간격이 표와 본문에서 어긋날 수 있다.
  - [ ] **최상급과 시점 부사를 원문과 대조한다.** "one of the closest"는 "가장"이 아니고,
        "over many years"는 "만년"이 아니다.
  - [ ] **인용은 원문에서 그것이 놓인 문단째로 옮긴다.** 문맥을 바꾸면 대상이 바뀐다.

## 2026-08-28 · 인물 3편(18세기) — 두 문장을 하나로 붙여 인용했다

- 결함유형: **합성 인용** / 발췌가 논지를 자름(재발) / 저장소 판정 되돌림(재발) / 오귀속 / 수치 오독
- 무엇이 관찰됐나:
  1. **서로 다른 두 문장을 하나의 인용문으로 만들었다.** MacTutor 는 달랑베르의 결함을 두 번 적는데
     대상이 다르다 — 바람 논문에는 **"그의 모든 작업에 전형적인 결함: 수학적으로는 매우 탄탄했으나
     빈약한 물리적 근거에 기댔다"**, 진동하는 현 논문에는 **"수학적으로 보기 좋은 단순화를 경계조건에
     적용해 관측과 어긋났다"**. 노트는 **뒤 문장의 내용을 앞 문장의 라벨에 얹어** 인용부호로 묶었고,
     그 결과 같은 문장을 두 번 세는 서술이 됐다.
     → **인용부호 안은 한 자리에서 온 것이어야 한다.** 두 곳을 합치면 그것은 인용이 아니라 요약이다.
  2. **발췌가 논지를 자르는 유형이 또 났다.** 오일러와의 다툼에서 MacTutor 는
     "한편으로는 달랑베르가 그럴 만했다"고 적은 **바로 다음에** "다른 한편 그의 글이 너무 뒤엉켜 있어
     오일러가 따라갈 수 없었다"를 붙이는데, 앞부분만 옮겨 **피해자–가해자 구도**로 만들었다.
     저장소가 베르누이 형제 건에서 지키는 "양쪽" 원칙과도 어긋났다.
  3. **"출처가 그렇게 적지 않는다"고 단정했는데 사실이 아니었다.** 푸리에의 처신을 국내 자료만 날카롭게
     적는 줄 알고 "MacTutor 는 같은 시기를 그렇게 적지 않는다"고 썼는데, MacTutor 도
     **"양쪽 모두에게 말로 환심을 얻어 냈다"**고 적는다. **한 자료가 말하지 않는다고 쓰기 전에 그 자료를 끝까지 읽는다.**
  4. **병기가 늘 옳은 것은 아니다.** 최속강하선 출제 연도를 저장소는 이미 **"1696이 맞고 1689는 오기"**로
     판정해 두었는데, 새 노트가 "두 값을 함께 둔다"로 유보를 되살렸다.
     → **갈리는 것을 병기하는 규칙은 판정이 없을 때의 규칙이다.** 저장소가 판정했으면 그 판정을 잇고,
     뒤집으려면 근거를 대야 한다.
  5. **수치 오독 하나.** "다섯 사람이 답을 보냈다"고 썼는데 원문의 다섯에는 **요한 자신이 포함**된다
     ("Newton, Jacob, Leibniz and de L'Hôpital solving the problem **in addition to Johann Bernoulli**").
     저장소의 `concept-calculus-of-variations` 는 이미 다섯에 요한을 넣어 적고 있었다.
  6. **좋았던 것.** 지난 회차의 지적(같은 문서 안 숫자 어긋남)을 **커밋 전에 스스로 둘 잡았다**
     — 푸리에의 "13년 뒤"(실제 12년)와 "11년"(1807 기준 15년/1811 기준 11년 병기).
     검수가 세 편의 모든 간격을 재검산해 어긋난 것이 없음을 확인했다.
     진동하는 현 다툼의 "누가 어느 쪽을 들었는지는 말하지 않는다"는 유보와 형제 건의 "양쪽" 처리도 지켜졌다.
- 수정: 검수 17건에 대해 **29곳**을 고쳤다. 오일러의 변분법 저작 연도 오귀속(1744 는 [2], 오일러 전기는 1740),
  요한이 진동하는 현을 연구했다는 무근거 귀속, 푸리에 귀국 연도(1801[1] / 1810[2]) 병기,
  과학원 선출 연도(1817[1] / 1816[2]) 병기, 장식적으로 붙인 출처 번호 제거도 함께 했다.
- 다음 방지 규칙:
  - [ ] **인용부호 안은 한 자리에서 온 것만 넣는다.** 두 문단의 내용을 합쳐야 한다면 인용부호를 풀고
        "…라고 적고, 다른 데서는 …라고 적는다"로 쓴다.
  - [ ] **"이 출처는 그렇게 적지 않는다"고 쓰기 전에 그 출처를 끝까지 읽는다.**
        대비를 세우는 문장이야말로 양쪽을 다 확인해야 한다.
  - [ ] **병기 전에 저장소가 이미 판정했는지 찾아본다.** `grep` 으로 그 연도·이름을 다른 노트에서 검색한다.
        판정이 있으면 잇고, 없을 때만 병기한다.
  - [ ] **"N명이 …했다"를 쓸 때 그 N에 주인공이 포함되는지 본다.** 원문의 "in addition to"를 놓치기 쉽다.

## 2026-08-28 · 인물 1편(18세기, 베이즈) — 편드는 방향을 반대로 썼다

- 결함유형: **논지 방향 역전** / 무근거 귀속(재발) / 근거번호 오귀속(재발) / 출처에 없는 최상급 /
  발췌가 논지를 자름(재발) / 저작명 표기 갈림 / 별칭 충돌 / 일괄 삽입의 공백 불일치
- 무엇이 관찰됐나:
  1. **편드는 방향이 반대였다.** 베이즈의 1736년 글은 제목부터
     "a Defence of the Mathematicians **Against** the Objections of the Author of The Analyst" 인데,
     노트는 "그는 **버클리를** 무작정 편들지 않는다"라고 썼다. 뒤따르는 인용이 보이는 것은
     그가 **자기편(수학자 쪽)** 을 무작정 편들지 않았다는 것이다.
     → **누가 누구를 상대로 쓴 글인지를 제목에서 먼저 확정하고 문장을 쓴다.**
  2. **두 줄 사이에서 같은 he 를 다른 사람으로 읽었다.** MacTutor 가 옮긴 프라이스 편지의
     "In an introduction which **he** has writ to this Essay" 에서 he 는 베이즈다.
     노트는 이 문장을 옳게 옮겨 놓고 **두 줄 뒤에서 "프라이스가 서론을 붙였다"** 로 뒤집었다.
     Wikipedia 는 "shepherded" 라고만 적고 서론 이야기가 없으며,
     저장소의 `concept-probability-advanced` 는 이미 **"주선"** 으로 판정해 두었다.
     → **대명사가 이어지는 인용을 옮길 때, 인용 밖에서 그 대명사를 다시 풀어 쓰면 반드시 대조한다.**
  3. **출처에 없는 최상급을 썼다.** "가장 이른 응답은 공격 2년 뒤에 나왔다" 고 적었는데,
     버클리에 대한 응답은 이미 **1734년 주린**과 **1735년 월턴**이 냈고 버클리가 1735년에
     둘을 지목해 되받았다. 근거로 단 MacTutor 에는 first/earliest 라는 말이 없다.
     → **"가장", "최초", "유일" 은 출처가 그 단어를 쓸 때만 쓴다.** 없으면 "이미 …에 나와 있었다" 로 적는다.
  4. **근거 번호를 그 출처가 말하지 않는 문장에 달았다(재발).** MacTutor 라플라스 전기는
     1781년 각서도, 라플라스가 베이즈적 해석을 개척·대중화했다는 말도 적지 않는다.
     그 근거는 Wikipedia Thomas Bayes 에 있고 저장소도 그렇게 달아 두었다.
     → **각주를 달기 전에 그 출처의 원문에서 해당 문장을 찾아 본다.** 인물이 겹친다고 근거가 되지 않는다.
  5. **일괄 삽입이 주변 공백을 기존 것과 다르게 만들었다.** 갤러리URL 13편을 스크립트로 채웠는데
     기존 47편은 `갤러리URL` 줄 바로 다음이 `---` 인 반면 새 13편에는 빈 줄이 하나 더 들어갔다.
     스크립트가 "본문이 바뀌지 않았다" 는 것만 검사하고 **삽입한 자리의 모양은 검사하지 않았다.**
     → **일괄 삽입 스크립트는 삽입 후 `git diff` 로 기존 사례와 모양을 대조하는 검사를 넣는다.**
  6. **별칭이 다른 노트의 이름을 가져갔다.** `person-bayes` 가 `베이즈 정리` 를 별칭으로 선언했는데
     그것은 `concept-probability-advanced` 의 이름이다. 색인이 조용히 한쪽을 버린다.
     노트 자신의 §6이 "정리는 확장편이 다룬다" 고 적으므로 선언이 스스로와도 어긋났다.
     → **별칭을 넣기 전에 `math/manifest.json` 의 별칭 색인에서 그 이름이 이미 쓰이는지 본다.**
  7. **좋았던 것.** MacTutor 인용 5곳이 원문과 글자 단위로 일치했고(**합성 인용 없음**),
     연도·간격이 전부 맞았으며, 저장소 판정을 되돌린 자리가 없었다.
     지난 세 회차에서 반복되던 두 결함이 처음으로 나지 않았다.
- 수정: 검수 13건 전부. 근거가 사라진 출처 4는 지우지 않고 **원문을 다시 읽어 실제로 쓸 대목**
  ("베이즈 규칙"이라는 이름은 여러 해 뒤 푸앵카레가 붙였다) 을 찾아 §6에 넣었다.
- 다음 방지 규칙:
  - [ ] **"A가 B를 편든다/반박한다" 를 쓰기 전에 그 글의 제목에서 대상을 확정한다.**
  - [ ] **인용문의 대명사를 인용 밖에서 이름으로 풀 때 원문에서 지시 대상을 다시 확인한다.**
  - [ ] **최상급은 출처의 단어를 그대로 옮길 때만 쓴다.**
  - [ ] **일괄 삽입 스크립트에 "기존 사례와 모양이 같은가" 검사를 넣는다.**
  - [ ] **별칭 추가 전 `math/manifest.json` 별칭 색인에서 충돌을 확인한다.**

## 2026-08-28 · 인물 1편(드무아브르) — 대구를 만들려다 근거가 말하는 시점을 놓쳤다

- 결함유형: **문장을 위해 시점을 옮김** / 같은 문서 안 산술 어긋남(재발) / 출처의 한정어 삭제 /
  요약이 본문보다 셈 / 무근거 사실 / 인용부호 안 근거 중복
- 무엇이 관찰됐나:
  1. **대구를 만들려다 근거가 말하는 시점을 잃었다.** 드무아브르가 1710년에 케임브리지 자리를
     얻으려 애쓴 대목 바로 뒤에 **"핼리 자신도 그 시절 대학 자리가 없었다[7] — 자리를 못 가진
     사람이 자리를 못 가진 사람을 도우려 한 셈이다"** 라고 썼다. 그런데 근거로 단 MacTutor 핼리
     전기가 **"핼리는 1704년 월리스가 죽은 뒤 옥스퍼드 새빌 기하학 교수로 임명됐다"** 고 적는다.
     1710년에 그는 이미 교수였다. 자리가 없던 것은 1691년 새빌 천문학 석좌에 지원했다 떨어진
     뒤부터 1704년까지이고, 두 사람이 만난 1692년이 그 안에 든다.
     → **문장이 좋아 보일 때가 가장 위험하다.** 대구·반전·아이러니를 세우기 전에
     **그 근거가 어느 해를 말하는지**를 다시 읽는다. 시점을 옮기면 대개 사실과 맞출 수 있다.
  2. **유보를 선언해 놓고 다른 절에서 정확한 연수로 새어 나갔다(재발).** 출국 시점을
     "1687~88 · 기록이 갈린다" 로 처리해 놓고, 다른 절에서 **"예순일곱 해 만에"** 라고 못박았다.
     노트가 굵게 세운 축년도는 1685년이고 1754−1685=**69** 다.
     → **유보를 선언했으면 문서 전체를 그 기준으로 다시 훑는다.** "N년 뒤/N해 만에" 는
     유보가 걸린 연도를 기준으로 삼지 않는다.
  3. **출처의 한정어를 지우고 없는 한정어를 넣었다.** [3]은 "**대략** 20만 명의 신교도
     **상공인과 기술자**" 라 적는데, 노트는 "대략" 을 지워 정밀도를 올리고 "20만 명은 **대부분**
     상공인과 기술자" 로 집합을 느슨하게 만들었다. 그 느슨해진 정의 위에서 "그도 그중 하나다"
     라고 썼는데 드무아브르는 상공인도 기술자도 아니다. 지식층까지 포함한 서술은 [4]에 있었다.
     → **"대략/약/…쯤" 을 지우지 않는다. 집합의 정의를 넓히려면 그렇게 적는 출처를 찾는다.**
  4. **"한눈에" 가 본문보다 셌다.** 본문은 위키백과를 "**적어도 이유의 일부**" 로 옳게 옮겨
     놓고, 요약 불릿에서는 "이유는 실력이 아니었다 — 프랑스인이라는 것이었다[1][2]" 로
     단정했다. **요약은 본문보다 세지면 안 된다.**
     → **요약 블록을 쓴 뒤 본문의 해당 문장과 강도를 대조한다.**
  5. **근거 없는 사실이 하나 있었다.** 베이즈가 "스털링과 드무아브르가 준 log z! 급수가
     발산한다" 고 짚은 것은 MacTutor **베이즈** 페이지에 있는데, 이 노트의 출처 7개 어디에도
     없었다. `person-bayes` 를 쓰면서 읽은 것이 그대로 넘어왔다.
     → **다른 노트를 쓰며 알게 된 사실을 옮길 때 근거도 함께 옮긴다.** 아는 것과 이 문서가
     근거를 가진 것은 다르다.
  6. **인용부호 안에 근거를 둘 달았다.** 인용문은 위키백과의 문장 그대로인데 `[1][2]` 를 달아
     두 출처가 같은 문장을 적은 것처럼 보이게 했다. 08-28 회차의 "인용부호 안은 한 자리에서
     온 것만" 이 각주 번호 쪽으로 재발한 형태다.
     → **인용부호 안의 각주는 하나다.** 다른 출처도 같은 일을 적는다면 인용 밖에서 따로 적는다.
  7. **좋았던 것.** 착수 전에 `grep` 으로 저장소의 조사위원회 연도 판정(1712)을 확인해
     MacTutor 의 1710을 따르지 않았고, **MacTutor 가 다른 해로 적는다는 것을 괄호로 밝혀** 뒤에
     오는 사람이 되돌리지 않게 했다. MacTutor 의 명백한 오기(1673년 만남, 그해 여섯 살)도
     걸러냈다. 인용부호가 쳐진 6곳이 전부 원문과 글자 단위로 일치했다 — **합성 인용 없음.**
- 수정: 검수 11건 전부와 중복 표현 1건. 출처를 8개로 늘렸다.
- 다음 방지 규칙:
  - [ ] **대구·반전을 세우기 전에 그 근거가 말하는 연도를 다시 읽는다.**
  - [ ] **유보를 선언한 연도는 "N년 뒤" 의 기준으로 쓰지 않는다.**
  - [ ] **출처의 "대략/약" 을 지우지 않는다.**
  - [ ] **요약 블록을 쓴 뒤 본문과 강도를 대조한다.**
  - [ ] **다른 노트에서 알게 된 사실을 옮길 때 근거도 함께 옮긴다.**
  - [ ] **인용부호 안의 각주는 하나로 한다.**

## 2026-08-28 · 인물 1편(디리클레) — 대비를 세우려고 사실을 지어냈다

- 결함유형: **대비를 위한 날조** / 같은 절 안에서 자기 문장 뒤집기 / 저장소 판정 되돌림(재발) /
  없는 문서로 넘김 / 출처 한정어 조작(재발) / 근거번호 오귀속(재발)
- 무엇이 관찰됐나:
  1. **대비를 세우려고 죽음을 지어냈다.** "베이즈는 끝내 대학 밖이었고 **드무아브르는
     커피하우스에서 죽었지만**, 디리클레는 가우스의 자리에 앉았다"라고 썼다. 어느 출처도
     그가 커피하우스에서 죽었다고 적지 않는다 — MacTutor는 **"그는 가난 속에 죽었다"**이고,
     커피하우스는 "그가 시간 대부분을 보낸 곳"이다. 저장소의 `person-de-moivre` 자신이
     "런던에서 죽어 **세인트마틴인더필즈에 묻혔다**"고 적어 두었다.
     → **직전 회차의 "대구를 만들려다 시점을 놓쳤다"가 한 단계 더 나갔다.** 그때는 있는
     사실의 연도를 틀렸고, 이번엔 **없는 사실을 만들었다.** 세 항을 나란히 놓는 문장은
     각 항이 그 노트에 실제로 적힌 문장인지 하나씩 대조한다.
  2. **같은 절 안에서 자기 문장을 뒤집었다.** 두 줄 위에서 "베이즈는 **비국교도라서
     옥스퍼드·케임브리지에 갈 수 없었고**"라고 옳게 적어 놓고, 요약하면서 "베이즈는 **끝내
     대학 밖이었고**"로 바꿨다. 베이즈는 1719년 **에든버러 대학**에 입학했다.
     → **요약 문장은 자기가 두 줄 위에 쓴 것과 대조한다.** 압축할 때 한정어가 사라진다.
  3. **저장소가 유보로 판정한 것을 단정했다(재발).** 1837년 함수 정의를
     `concept-differential-equation-advanced` §9가 **"전통적으로 알려져 있다"** 로 적고
     로바쳅스키 1834년 독립 발견과 라카토시의 반박("디리클레의 저작에 그런 정의는 전혀 없다")
     을 함께 실어 두었는데, 새 노트가 표에서 "함수의 현대적 정의 | 1837"로 못박았다.
     이 노트 자신의 출처(위키백과)도 "**one of the first** to give the modern formal definition"
     이라고 적는다.
     → **착수 전 `grep` 은 연도만이 아니라 "그 사실 자체에 저장소가 유보를 걸었는지"도 본다.**
     특히 **새 노트가 등록되는 `기여인물` 문서**는 반드시 열어 읽는다 — 그 문서가 이 노트를
     역링크하므로 충돌이 바로 보인다.
  4. **없는 문서로 넘겼다.** §5 배분표가 디리클레 문제를 `[[미분방정식 확장편]]` 으로
     넘겼는데 그 노트에 그 주제가 **한 줄도 없다**(저장소 전체에서 "디리클레 문제"를 적는 곳은
     이 인물 노트뿐이다).
     → **"어디서 다루나" 를 채울 때 그 문서를 열어 그 주제가 정말 있는지 `grep` 한다.**
     없으면 "여기"로 적고 이 문서가 다룬다.
  5. **출처의 한정어를 세 곳에서 조작했다(재발).** "some years later"→"몇십 년 뒤",
     "mathematicians, **including** Kummer, Dirichlet and Dedekind"→"쿠머, 디리클레,
     데데킨트다"(닫힌 목록), "lectures **tended to be** on the more advanced topics"→
     "그대로 강의했다".
     → **부사와 전치사를 옮긴다.** including·tended to·some·at least 가 사라지면 문장이 세진다.
  6. **디리클레를 한 번도 언급하지 않는 출처를 그의 행동에 달았다(재발).** 1848년 소총·궁·
     수입 상실은 전부 [2]만 적는데 [2][7]로 달았다. [7](네이버 「독일 3월 혁명」)은 배경만 적는다.
     → **배경 출처는 배경 문장에만 단다.** 같은 문단에 있다고 같은 각주를 붙이지 않는다.
  7. **좋았던 것.** 인용부호 12곳이 전부 원문 한 자리에서 온 연속된 대목이었고
     **각주도 모두 하나**였다 — 직전 회차의 두 결함이 재발하지 않았다. 착수 전에
     `person-riemann` 이 없는 노트라는 것을 스스로 확인해 `관련인물` 에서 뺐고,
     `concept-analysis-rigor` 가 그의 수렴 조건을 다루지 않는다는 것을 확인해 `기여개념` 에서 뺐다.
- 수정: 검수 17건 전부와 지시어 정리 1건.
- 다음 방지 규칙:
  - [ ] **여러 인물을 나란히 놓는 문장은 각 항을 그 인물 노트에서 찾아 대조한다.**
  - [ ] **요약 문장은 같은 절 위쪽의 원래 문장과 강도·내용을 대조한다.**
  - [ ] **`기여인물` 로 등록될 문서는 착수 전에 열어 읽는다.** 그 문서의 판정이 곧 이 문서의 제약이다.
  - [ ] **"어디서 다루나" 는 그 문서를 `grep` 해서 확인하고 채운다.**
  - [ ] **including · tended to · some · at least 를 옮긴다.**
  - [ ] **배경 출처는 배경 문장에만 단다.**

## 2026-08-28 · 인물 1편(가우스) — 요약하면서 두 편지를 하나로 합쳤다

- 결함유형: **요약이 원문의 구분을 지움** / 요약이 본문의 유보를 뒤집음(재발) /
  연도 오류 셋 / 발췌가 논지를 자름(재발) / 다른 출처가 부정하는 서술 / 닫힌 목록(재발) /
  근거번호 오귀속(재발)
- 무엇이 관찰됐나:
  1. **본문에서 구분해 놓은 것을 요약에서 합쳤다.** §5가 "가우스는 **한 친구에게** '일류의
     천재'라 썼고, **파르카시에게는** '칭찬하는 것은 나 자신을 칭찬하는 일'이라 썼다"로 두
     수신자를 정확히 갈라 놓았는데, §9 요약에서 **"같은 편지에서"**로 합쳤다. 원문의 구조
     ("wrote **to a friend** … **To Farkas Bolyai, however,** Gauss wrote")가 통째로 사라졌다.
     → **요약 문단은 자기가 앞 절에 쓴 문장과 대조한다.** 08-28 디리클레 회차의 "같은 절 안에서
     자기 문장 뒤집기"가 **절을 건너뛴 형태**로 재발했다. 압축할 때 사라지는 것은 늘 한정어와 구분이다.
  2. **본문의 유보를 요약에서 단정으로 바꿨다(재발).** §5의 표가 위키백과를 **"어느 정도나
     앞섰는지는 아주 분명하지 않다"**로 옳게 옮겨 놓고, §9가 "가우스가 먼저 알았는지는 **문서로
     확인된다**"고 적었다. 같은 각주 번호를 달고 정반대를 말한 것이다.
     → **§ 마지막의 "이 사람을 어떻게 읽을까" 는 새 주장을 하는 자리가 아니라 앞에서 세운 것을
     모으는 자리다.** 거기 쓴 문장마다 근거가 앞 절 어디에 있는지 짚어 본다.
  3. **연도 셋이 틀렸는데 원인이 서로 달랐다.** (ㄱ) 공작의 전사 — MacTutor가 결혼 문장 **뒤에**
     연도 없이 붙여 적은 것을 결혼과 같은 해로 읽었다. 위키백과가 **1806년 예나 전투**로 적는다.
     (ㄴ) 베버 — **부임한 해(1831)**와 **공동 연구를 시작한 해(1832)**를 한 문장으로 붙였다.
     같은 문서의 연표는 1832로 옳게 적고 있었다. (ㄷ) 천문대장 재직 — 1855−1807=48을 그대로
     "해"로 썼는데 1807년 **11월** 부임이라 마흔일곱 해다.
     → **연도가 없는 문장을 앞 문장의 연도로 채우지 않는다.** 다른 출처에서 연도를 찾는다.
     → **"N년에 부임" 과 "N년부터 …했다" 는 다른 문장이다.**
     → **"몇 해 있었나" 는 뺄셈이 아니라 달까지 확인한다.**
  4. **발췌가 논지를 잘랐다(재발, 다섯 번째).** "수학계에서는 야코비와 디리클레만 참석했다"
     바로 뒤에 **"but Gauss received many messages and honours"**가 있는데 앞부분만 옮겨
     "명성과 실제 자리의 거리"라는 반전을 만들었다. **반전을 세운 자리가 위험한 자리다** —
     08-28 드무아브르(대구를 위해 시점을 옮김), 디리클레(대비를 위해 사실을 날조)에 이어 셋째다.
  5. **자기 출처 하나가 부정하는 것을 다른 출처만 보고 단정했다.** 정다각형 작도를
     "필요충분조건을 찾아냈다"[4]고 적었는데, 같은 노트의 [5]가 **"당연하다고 생각한 것인지
     증명은 남기지 않았다. 가우스의 말도 의심하는 수학자들은 훗날 정말로 그렇다는 것을
     증명하였는데"**라고 적고 [2]도 충분조건 방향만 적는다.
     → **출처 일곱 개를 모아 놓고 한 개만 보고 쓰지 않는다.** 같은 사실을 다루는 다른 출처가
     내 노트 안에 이미 있는지 먼저 본다.
  6. **닫힌 목록과 근거번호 오귀속이 또 났다.** "Two of Gauss's last doctoral students were"를
     "마지막 박사 제자 **둘이** …다"로 닫았고, 연표 1852년 줄의 `[1]` 은 그 사실을 적지 않는
     출처다(그것은 `person-dedekind` 에서 온 것이다). 둘 다 08-28 회차의 재발이다.
  7. **좋았던 것.** 인용부호가 쳐진 **21곳 전부**가 원문 한 자리에서 온 연속된 대목이었고
     **각주도 모두 하나**였다 — 합성 인용이 세 회차 연속 나지 않았다. 최상급 둘("그리스 수학 이래
     가장 큰 진전", "2000년 넘게 없던 첫 진전")도 두 출처가 실제로 그 단어를 쓴다. 그리고
     **착수 전에 `기여인물` 로 등록될 네 문서를 읽는 규칙**(직전 회차에서 만든 것)이 작동해
     겹침도 되돌림도 없었다.
- 수정: 검수 18건 전부(치환 24곳).
- 다음 방지 규칙:
  - [ ] **마지막 정리 절은 앞 절에서 세운 것만 모은다.** 새 단정을 만들지 않는다.
  - [ ] **요약 문단을 쓴 뒤 그 문단의 각 항을 앞 절의 원문장과 나란히 놓고 읽는다.**
  - [ ] **연도가 없는 문장에 앞 문장의 연도를 채우지 않는다.**
  - [ ] **"몇 해 있었나" 는 달까지 확인하고 쓴다.**
  - [ ] **한 사실에 대해 내 출처 목록 안의 다른 출처가 무엇을 적는지 먼저 본다.**

## 2026-08-28 · 인물 1편(푸앵카레) — 아이러니는 사실인데 인과는 아니었다

- 결함유형: **아이러니를 인과로 승격** / 앞 절과의 모순(재발) / 없는 연도를 앞 문장에서 채움(재발) /
  내 출처가 부정하는 단정(재발) / 발췌가 논지를 자름(재발 6회) / 한정어 삭제(재발) / 링크 비대칭
- 무엇이 관찰됐나:
  1. **축을 인과로 세웠는데 출처에는 아이러니만 있었다.** 두 사실은 각각 확실하다 —
     툴루즈가 적은 **"늘 서둘렀고 고치러 되돌아가기를 싫어했다"**, MacTutor 가 적은
     **"이 오류가 오늘날 혼돈 이론의 탄생을 표시하는 것으로 여겨진다"**. 그런데 노트는
     요약 필드에 **"그 수정이 혼돈 이론의 출발점이 됐다"**, §5 끝에 **"봉우리만 보는 사람에게
     오류가 났다"**로 적어 **성급함 → 오류**와 **수정 → 혼돈 이론** 두 인과를 만들었다.
     어느 출처도 그렇게 적지 않는다.
     → **두 사실을 나란히 놓는 것과 하나가 다른 하나를 낳았다고 적는 것은 다른 문장이다.**
     아이러니는 "A인 사람에게 B가 일어났다"까지고, 인과는 "A라서 B가 났다"이다.
     축을 세울 때 **어느 쪽인지 먼저 정하고, 인과라면 그렇게 적는 출처를 댄다.**
  2. **앞 절에서 자기가 쓴 것을 뒤집었다(재발 3회).** §3이 "**그 각서에서** 혼돈적 운동을 처음
     수학적으로 서술했다"[1]고 적어 놓고, §4가 "**처음 낸 것에는 없었다**"고 적었다. 위키백과의
     "최종 인쇄본이 혼돈 이론으로 이어지는 착상들을 담았다"에서 "첫 판본에는 없었다"를 끌어낸
     것인데, 출처는 그 반대를 시사한다.
     → **"X가 담았다"에서 "Y에는 없었다"를 끌어내지 않는다.** 없었다고 쓰려면 없었다고 적는
     출처가 있어야 한다.
  3. **없는 연도를 앞 문장에서 채웠다(재발, 두 회차 연속).** 연표의 1889년 두 줄이 그렇다 —
     MacTutor 는 수상 연도도 오류 발견 연도도 적지 않고, 위키백과는 **1887년 수상**으로 적는다.
     앞 문장의 "1889년의 예순 번째 생일"에서 끌어왔다.
     → 가우스 회차에서 만든 규칙이 그대로 재발했다. **연표를 채울 때는 각 줄마다 그 연도를 적는
     출처 문장을 손으로 짚는다.**
  4. **내 출처 목록 안의 다른 출처가 부정하는 것을 단정했다(재발).** "제자가 없어"라고 적었는데
     같은 노트의 위키백과에 `==== Students ====` 절이 있고 박사 제자 둘을 이름과 연도까지 적는다.
     → 가우스 회차의 규칙("한 사실에 대해 내 출처 목록 안의 다른 출처가 무엇을 적는지 먼저
     본다")이 재발했다. **인물 노트에서 특히 "…가 없었다" 류 문장을 쓸 때 확인한다.**
  5. **발췌가 논지를 잘랐다(재발 6회).** 박사 논문 심사평을 인용하며 **바로 앞 문장**
     "**They praised the results near the beginning of the work**"를 빼고 "다소 혼란스럽다"만
     옮겨 심사평 전체가 부정적이었던 것처럼 만들었다. 지금까지는 인용 **뒤** 문장을 놓쳤는데
     이번엔 **앞** 문장이었다.
     → **인용을 끊는 자리의 앞뒤 문장을 모두 읽는다.**
  6. **좋았던 것.** 블록인용 15곳과 인라인 인용부호 전부가 원문 한 자리에서 온 연속된 대목이었고
     **각주도 모두 하나**였다 — 합성 인용이 **네 회차 연속** 나지 않았다. 저장소 판정 되돌림도
     없었고(§13 혼돈 판정·비유클리드 모형·힐베르트/괴델), 최상급 다섯 가운데 넷이 출처의 단어
     그대로이며 나머지 하나는 "…로 여겨진다"로 옳게 완화돼 있었다.
- 수정: 검수 13건 전부(치환 22곳)와 권고 둘(재귀정리·버스 계단 일화).
- 다음 방지 규칙:
  - [ ] **축을 세울 때 아이러니인지 인과인지 먼저 정한다.** 인과면 그렇게 적는 출처를 댄다.
  - [ ] **"A가 담았다"에서 "B에는 없었다"를 끌어내지 않는다.**
  - [ ] **연표는 줄마다 그 연도를 적는 출처 문장을 손으로 짚는다.**
  - [ ] **"…가 없었다" 류 문장은 내 출처 목록 전체에서 반증을 찾아본다.**
  - [ ] **인용을 끊는 자리의 앞 문장도 읽는다.**

## 2026-08-28 · 인물 1편(프레게) — 어제 검수가 지운 인과를 오늘 새 노트가 되살렸다

- 결함유형: **저장소 판정 되돌림(어제 것)** / 양보절만 떼어 방향 뒤집기 / career→생애 오역 /
  내 출처의 반전을 안 씀(재발) / 절대 부정(재발) / 근거 없이 다른 노트에서 옮김(재발) /
  인용이 필자 서술을 삼킴 / 병기 누락 / 한정어 삭제(재발) / 요약이 본문보다 셈(재발)
- 무엇이 관찰됐나:
  1. **바로 어제 검수가 지운 문장을 오늘 새 노트가 되살렸다.** 푸앵카레 회차에서
     "수정 → 혼돈 이론"이라는 인과가 출처를 넘어선다는 판정을 받아 지웠는데, 프레게 노트의
     마지막 절이 **"푸앵카레는 고쳐서 더 갔고, 그 수정본이 혼돈 이론으로 이어졌다"**로 그대로
     복원했다. 그것도 **푸앵카레를 한 글자도 언급하지 않는 출처에 각주를 달아서**.
     → **다른 인물 노트를 끌어와 대비를 세울 때, 그 노트의 현재 문장을 열어 읽는다.**
     기억 속의 그 인물이 아니라 저장소에 적힌 그 인물과 대비해야 한다. 어제 만든 규칙
     ("기여인물로 등록될 문서는 착수 전에 읽는다")을 **본문에서 언급하는 모든 인물 노트**로 넓힌다.
  2. **양보절만 떼어 내 출처의 방향을 뒤집었다.** 위키백과의 문장은
     "Frege thought Jews ought at least be deprived of certain political rights. And,
     **although** he had held friendly relations with Jews …, Frege wrote that it would be best
     if Jews would 'get lost …'" 이다. 노트는 although 절만 떼어 **"같은 출처가 균형도 적는다"**로
     제시하고 주절을 삭제했다. 발췌가 논지를 자르는 결함(7회)이 **가장 민감한 대목**에서 났다.
     → **although·despite·however 로 시작하는 절은 그 자체로 인용할 수 없다.** 주절과 함께
     옮기거나, 옮기지 않는다.
  3. **career 를 "생애"로 옮겼다.** "He taught there for the rest of his **career**"인데
     "남은 생애 내내"로 적었다. 그는 1917년 퇴임하고 1925년 바트 클라이넨에서 죽었으며,
     **같은 노트의 연표가 "1917 예나 퇴임"을 적고 있었다.**
     → **career·life·work 를 구별해 옮긴다.** 그리고 옮긴 뒤 자기 연표와 대조한다.
  4. **내 출처가 적어 둔 반전을 본문에서 쓰지 않았다(재발).** 위키백과는 "그 처방이 무가치하다는
     것이 드러났다" 바로 뒤에 **"but recent work has shown that much of the program …
     might be salvaged in other ways"**를 붙인다. 노트의 **출처 요약 필드에는 그 절이 적혀
     있는데** 본문에서 한 번도 쓰이지 않아, §5·§10·요약이 실패를 무조건으로 만들었다.
     → **출처 요약 필드를 쓸 때 적어 둔 것은 본문에서 다 쓰거나, 안 쓸 이유가 있어야 한다.**
     특히 "그런데/다만"으로 시작하는 절.
  5. **제목에 절대 부정을 썼다(재발).** §3 제목이 "아무도 읽지 않았다"였는데 아홉 줄 아래
     자기 노트가 "서평이 여섯 개 있었다"고 적고, 국내 출처 원문에도 **"읽었다 하더라도"**라는
     한정이 붙어 있었다. 그리고 1896년 정원외 명예교수가 된 것도 두 출처가 함께 적는다.
     → 푸앵카레 회차의 **"…가 없었다 류 문장은 내 출처 목록 전체에서 반증을 찾아본다"**가
     **제목**에서 재발했다. 절 제목도 문장이다.
  6. **인용부호가 필자의 서술과 감상까지 삼켰다.** 국내 자료에서 카르납의 회상으로 표시된 것은
     "그는 작고 내성적인 사람이었다"까지인데, 노트는 이어지는 필자의 서술 두 문장을 함께 묶어
     "카르납의 회상이다"로 제시했고, 감상임을 드러내는 마지막 절
     ("프레게의 모습은 무척이나 쓸쓸해 보인다")을 "수업을 했다"로 바꿔 지웠다.
     → **"…라고 회상하기를"의 범위가 어디까지인지 원문에서 확인한다.** 국내 자료는 회상과
     필자 서술이 한 문단에 섞여 있는 일이 많다.
  7. **YAML 이 깨졌다.** 출처 제목에 `산수의 기초: 논리주의` 처럼 **콜론+공백**이 들어가면
     frontmatter 파싱이 실패하고 검증기가 문서를 통째로 버린다.
     → **frontmatter 값에 `: `를 쓰지 않는다.** 원자료의 제목에 콜론이 있으면 `—`로 바꾼다.
  8. **좋았던 것.** 인용부호가 쳐진 22곳 가운데 19곳이 원문 한 자리에서 온 연속된 대목이었고
     **각주도 전부 하나**였다 — 합성 인용이 **다섯 회차 연속** 나지 않았다. 최상급 다섯이
     모두 출처의 단어 그대로였고, 케니의 콜럼버스 비유도 노트가 그것을 넘어서는 인과를 만들지
     않았다. 1924년 일기를 다룬 절은 검수가 **저장소의 다른 인물 노트들과 일관되므로 빼지 말라**고
     판정했다.
- 수정: 검수 16건 전부(치환 31곳)와 절 번호 재정렬. 「뜻과 지시체」 절과 칸토어의 서평을 넣었다.
- 다음 방지 규칙:
  - [ ] **본문에서 언급하는 다른 인물 노트는 그 문장을 열어 읽고 대비를 세운다.**
  - [ ] **although·despite·however 절만 떼어 인용하지 않는다.**
  - [ ] **career·life·work 를 구별해 옮기고, 옮긴 뒤 자기 연표와 대조한다.**
  - [ ] **출처 요약 필드에 적어 둔 "그런데/다만" 절을 본문에서 쓴다.**
  - [ ] **절 제목에도 절대 부정을 쓰지 않는다.**
  - [ ] **frontmatter 값에 `: `를 쓰지 않는다.**

## 2026-08-29 · 로그 투트랙 — 절을 끼워 넣으면서 sed 로 번호를 밀었더니 상호참조가 깨졌다

- 결함유형: 자동 치환의 연쇄 적용 / 절 참조 무효화 / 게이트가 못 잡는 결함
- 무엇이 관찰됐나:
  1. **분량 게이트를 채우려고 절을 중간에 끼워 넣었다.** 이해편 §4(세 법칙)와
     확장편 §6(표를 만든다는 노동)을 새로 넣으면서 뒤 절 번호를 하나씩 밀어야 했다.
     스크립트로 `## 7. → ## 8.` 식으로 제목을 밀고, 본문의 `(§7) → (§8)` 도 같이 밀었다.
  2. **치환이 연쇄로 걸렸다.** `(§11)→(§12)` 를 먼저 적용하고 그 다음 `(§12)→(§13)` 을
     적용하니, **방금 12가 된 것이 다시 13이 됐다.** 파이썬 `str.replace` 를 순서대로
     돌린 탓이다. 확장편 「수업에서 쓸 만한 대목」의 "양쪽 다 그럴듯한 논쟁(§12)" 이
     (§14) 가 되어 엉뚱한 절을 가리켰다.
  3. **괄호 없는 참조는 아예 안 밀렸다.** 이해편 본문의 "§6의 쌍곡선 이야기다" 는
     `(§6)` 형태가 아니라 치환 대상에서 빠졌고, 게다가 **쌍곡선은 이해편에 없는 절**이었다
     (확장편 §7이다). 원래부터 틀린 참조였는데 번호 밀기가 그것을 가려 줬다.
  4. **`verify_math.py` 는 이것을 못 잡는다.** 자수·줄수·절수·시각화·출처·인용 정합성은
     전부 통과했다. **절 참조는 세는 대상이 아니다.**
  5. 업로드 직전 볼트 사본을 눈으로 읽다가 발견했다. 한 번 더 읽지 않았으면
     세 건이 그대로 발행됐을 것이다.
- 수정: 3건. 이해편의 "§6의 쌍곡선" → "[[로그 (확장편)]] §7의 쌍곡선",
  확장편 "§12의 답" → "§14", "논쟁(§14)" → "(§12)".
  그리고 **전수 재검증 스크립트**를 돌려 두 노트의 모든 `§n` 이 그 노트에 실재하는
  절인지 확인했다(다른 문서를 가리키는 참조는 제외). 깨진 참조 0건.
- 다음 방지 규칙:
  - [ ] **번호를 미는 치환은 역순으로 하거나 한 번에 한다.** 오름차순으로 `str.replace`
        를 이어 돌리면 앞에서 바꾼 것을 뒤에서 다시 바꾼다. **내림차순(큰 번호부터)으로
        돌리거나, `re.sub` 에 함수를 넘겨 한 번의 스캔으로 끝낸다.**
  - [ ] **절을 끼워 넣었으면 그 문서의 `§n` 을 전수 대조한다.** 한 줄이면 된다 —
        본문의 `§n` 을 모두 뽑아 `^## n\.` 집합에 들어 있는지 본다.
        검사기가 세지 않는 것은 그때그때 세어야 한다.
  - [ ] **다른 문서의 절을 가리킬 때는 문서 이름을 함께 적는다.** "§6의 쌍곡선"처럼
        적으면 어느 문서의 §6인지 알 수 없고, 번호가 밀릴 때 조용히 틀린다.
        `[[로그 (확장편)]] §7` 처럼 적으면 사람이 읽을 때 바로 걸린다.
  - [ ] **분량 게이트를 채우려고 절을 끼워 넣는 것 자체를 경계한다.** 이번에는 넣은 내용이
        본문에 값을 더했지만(세 법칙·표를 만드는 노동), **구조를 건드리는 순간 참조가
        깨진다.** 게이트 미달이면 먼저 기존 절을 깊게 쓰는 쪽을 본다.

## 2026-08-30 · 블로그 서가 도해가 검게 뭉갠 건 (사용자 신고)
- 결함유형: 이관누락(스타일시트) / 검사기 사각 / 계측기 오탐
- 무엇이 틀렸나:
  1. **본문만 옮기고 색 정의를 안 옮겼다.** 블로그 노트의 SVG 는 색을 `fill="var(--green)"`
     처럼 **CSS 변수로 부른다**. 그 정의는 원천 Blog 리포의 `published/<슬러그>/brand.css`
     에 있는데 `ingest_blog.py` 는 본문·이미지만 가져왔다. 갤러리에 `--green` ·
     `--green-deep` · `--green-soft` · `--amber` · `--ink-soft` 다섯이 없었다.
  2. **미정의 `var()` 는 SVG 에서 검정으로 떨어진다.** 값이 무효면 표현속성이 초기값
     (black)이 된다. 그래서 도해가 **글자도 배경도 다 검은 덩어리**가 됐다 —
     19/22편 · 499자리 · 라이트와 다크 양쪽 모두. 두 달 넘게 그 상태로 발행돼 있었다.
  3. **조용히 죽는다.** 콘솔 오류 0, 깨진 이미지 0, PNG 129장 전부 무결, 모바일 잘림 0.
     "파일이 멀쩡한가"를 재는 검사는 전부 통과했다. 발행용 PNG export 는 원천 환경에서
     그려져 **멀쩡했기 때문에** 파일만 봐서는 끝까지 알 수 없었다.
  4. 실제로 찾은 방법은 하나뿐이었다 — **브라우저로 열어 스크린샷을 눈으로 본 것.**
  5. 곁가지 둘: 도해가 Pretendard 로 조판됐는데 갤러리가 그 폰트를 안 불러
     글자가 상자를 넘어 잘렸고(`코드로 계산`), 표지 `<img>` 가 22편 중 19편에만 있었다
     (초기 3편은 원천이 `<section class="thumb">` 였고 인제스트가 걷어내기만 했다).
- 수정: `blog/index.html` 에 brand.css 정본 팔레트를 **필라별로** 심고(openPost 가
  `.post` 에 `data-pillar` 를 붙인다), 도해에 Pretendard 를 물리고, 다크에서는 색을
  뒤집는 대신 **도해에만 제 지면을 깔았다**(math/ 수학사 도해와 같은 규약 — `--green-deep`
  이 글자 58회·배경 12회로 겸용이라 뒤집으면 읽히는 자리가 뒤집힌다). 표지는
  `ingest_blog.py` 가 걷어낸 자리에 같은 그림의 `<img>` 를 끼우도록 고쳤다 — 노트를
  손으로 고치면 다음 자동 인제스트가 되돌린다.
- 계측기도 두 번 틀렸다(기록해 둔다):
  - KaTeX 내부 SVG(`viewBox="0 0 400000 1080"`)를 "클리핑"으로 잡아 5건 오탐.
  - `rgba(255,255,255,.2)` 원 위의 흰 글자를 "대비 1.0"으로 잡아 28건 오탐 —
    **알파를 합성하지 않은 탓**이다. 둘 다 눈으로 보고서야 걸렀다.
- 다음 방지 규칙:
  - [ ] **본문을 다른 갤러리로 옮길 때 "색·폰트 정의가 따라왔는지"를 확인한다.**
        옮긴 HTML 이 `var(...)` 나 클래스로 스타일을 부르면 그 정의는 원천 스타일시트에
        있다. 본문·이미지만 옮기면 그림은 살아 있는 척하면서 죽는다.
  - [ ] **미정의 `var()` 는 한 줄로 잰다.** 속성이 `var(` 로 시작하는데
        `getComputedStyle` 값이 `rgb(0, 0, 0)` 이면 미정의다. 이관·갤러리 변경 뒤 필수.
  - [ ] **파일 무결성 ≠ 화면 정상.** PNG 디코드·깨진 img·오버플로 검사를 다 통과해도
        화면은 검을 수 있다. 새 이관·새 갤러리는 **실제 브라우저로 한 편 열어 눈으로 본다.**
  - [ ] **색 대비 계측기는 알파를 합성하고, KaTeX 내부 SVG 는 제외한다.** 안 그러면
        오탐이 진짜를 덮는다.
  - [ ] **인제스트가 다시 쓰는 파일은 손으로 고치지 않는다.** `blog/notes/*.md` 는
        Blog 리포의 워크플로가 Search 를 체크아웃해 `tools/ingest_blog.py` 로 다시 쓴다.
        고칠 자리는 노트가 아니라 그 스크립트다.
  - [ ] **인제스트 산출물은 `newline="\n"` 으로 쓴다.** 윈도우에서 손으로 돌리면
        파이썬이 CRLF 로 써서 내용이 그대로인 편들까지 전부 '수정됨' 으로 잡힌다.

## 2026-08-31 · 미분과 변화율 투트랙 (자가검수 + 기계 대조)
- 결함유형: 수치오류 / 과잉단순화 / 인용-정의 불일치 / 링크표기 이탈
- 무엇이 관찰됐나:
  1. **분량 게이트를 전부 통과한 상태에서 수치가 틀려 있었다.** 오일러 방법 표의 마지막
     행이 0.55·÷2.04 로 적혀 있었으나 실측은 0.54·÷2.07. 글자수·섹션·시각화·출처 게이트는
     **세는 게이트라 값의 진위를 보지 않는다.** 파이썬으로 전 수치를 재계산해 잡았다.
  2. **"상승률 둔화 = `f″<0`" 이 엄밀히 거짓**이었다. 상승률은 로그미분 `P′/P` 라
     `P″ < (P′)²/P` 이면 둔화하므로 `P″>0` 과 양립한다. 수업용 대비로는 유효해 본문은
     살리고 각주로 정확한 조건을 병기했다.
  3. 출처 3건이 목록에만 있고 본문에서 안 불렸고, 각주 정의 2건은 참조가 없어 **렌더에서
     빠질 상태**였다. frontmatter `소스수` 도 실제(7/7)와 다르게 6/5로 적혀 있었다.
  4. `[[변화율로 예측하기 (확장편)]]` 이 별칭구멍을 만들었다. 빌더 정본 이름은
     `변화율로 예측하기`.
  5. **검사기 자신이 틀렸다.** 위키링크 해결 여부를 보는 스크립트가 `link-index.json` 의
     `alias` 맵을 안 보고 `entries` 키만 봤고, `alias` 값이 **인덱스 0** 인 항목을
     거짓으로 읽어 "미해결" 로 찍었다. 실제로는 전부 해결됐다.
- 수정: 수치 4곳 정정, 각주 `[^상승률]` 신설, 출처 3건을 제자리에 인용, 각주 참조 2건 삽입,
  `소스수` 정정, 링크 표기를 정본 이름으로 교체(별칭구멍 0). 출처 없이 인과로 단정한 문장
  (라이프니츠 기호 설계)과 출처 없는 숫자(룽게–쿠타 차수)는 완화·삭제했다.
- 다음 방지 규칙: 위 체크리스트 4항 추가.
- 부수 관찰 하나 — **국내 웹 검색이 통째로 못 쓸 때가 있다.** "미분 실생활 활용" 웹 검색
  상위 10건이 전부 레포트 판매 사이트·프레지 발표자료였다. 전량 배제하고 지식백과와
  보도로만 국내 출처를 세웠다. 국내 4건 하한을 맞추려고 저품질 출처를 끼워 넣지 말 것.

## 2026-08-31 · 스파크 영상 노트 2편 — 원본이 실어 온 결함을 그대로 통과시킬 뻔했다
- 결함유형: 무출처 정량수치 / 제품 무오류 단정 / 미확인 기능확장 / 검사기 오판정
- 무엇이 관찰됐나:
  1. **기계 게이트를 전부 통과한 노트가 독립 검수에서 불합격했다.** 챕터·인용·팁·TL;DR·
     글자수·태그가 전부 하한을 넘었지만, `내 팁` 에 **"검토 시간을 90% 이상 단축"** 이라는
     영상에서 측정된 적 없는 수치가 있었다. 코퍼스 98편 전수 조회 결과 **정량 성과 수치를
     지어낸 팁은 이 1건이 유일**했다. 세는 게이트는 값의 진위를 보지 않는다(8-31 미분 편과 같은 형태).
  2. **AI 제품의 무오류를 자기 서술의 사실로 썼다.** TL;DR 이 "제미나이 노트북은 …
     **환각 없이** 실무 엑셀 파일을 생성한다" 였다. 근거는 화자 발언뿐인데 노트가 이를
     사실로 승격시켰다. 무환각을 규칙으로 삼는 리포에서 "AI 가 환각하지 않는다" 를
     무출처로 단정한 셈이다. 한 줄 요약·본문·팁까지 4개 층에 절대어가 누적돼 있었다.
  3. **영상이 보여주지 않은 기능으로 넓혔다.** 원본이 말한 것은 "**검색된** 유튜브 영상들"
     수집인데 팁은 "**재생목록 전체**를 긁어온다" 로 적었다.
  4. **결함은 전량 스파크 원본 유래였다.** 원본과 바이트 단위로 대조해 내 전사 문제가
     아님을 먼저 갈랐다(루틴 9번). 그래도 거르는 건 발행 전 검수의 몫이다.
  5. **리뷰어 1회차 지적 2건이 코퍼스 실측으로 무너졌다.** "인용에 구두점이 섞여 있어
     사후 편집 같다" → 코퍼스 인용 446개 중 **415개(93%)가 구두점 포함**, 오히려 표준이었다.
     "같은 상투구가 두 번 반복돼 창작 같다" → 같은 채널 기발행 노트에서 **5개 챕터 중 4개**가
     같은 상투구를 반복한다. **파일 내부 일관성이 아니라 코퍼스를 잣대로 재야 한다.**
- 수정: 정량 표현은 다른 수치로 바꾸지 않고 **제거**, 무오류 단정은 화자 주장으로 귀속,
  절대어(완벽히·극대화·모든·제한 없이) 완화, 기능 서술을 원본 범위로 축소.
  **핵심 인용은 한 글자도 손대지 않았다** — 리뷰어가 "발화체 같지 않다" 고 하니 발화체처럼
  다듬는 것은 검사가 잡으려던 창작을 검사 때문에 만드는 일이다.
- 다음 방지 규칙:
  - [ ] **`내 팁` 에 영상에서 측정되지 않은 정량 수치를 쓰지 않는다.** 고칠 때 다른 수치로
        바꾸지 말고 정량 표현 자체를 뺀다. 근사치로 바꾸는 것도 창작이다.
  - [ ] **AI 제품의 무오류(환각 없음·완벽 방지·정확도 극대화)를 자기 서술의 사실로 쓰지
        않는다.** 근거가 화자 발언뿐이면 "…라는 것이 영상의 핵심 주장" 으로 귀속한다.
        모델 보증이 아니라 **프롬프트 측 통제**("규칙을 걸어")로 서술하면 검증 가능해진다.
  - [ ] **인용은 검수 지적에 맞춰 고치지 않는다.** 인용이 의심스러우면 고치는 게 아니라
        원본 대조 → 해소되지 않으면 보류다. 다듬는 순간 축자 채록이 아니게 된다.
  - [ ] **원본 유래는 면죄부가 아니다.** 통과시킬지는 출처가 아니라 ① 검증 불가 상태에서
        위험도가 낮은가 ② 반증 가능한 형태인가로 가른다. UI 유무 진술(저위험·확인가능)은
        원본 유지 + 미검증 명시, 성과 수치(확인 방법 없음)는 삭제.
  - [ ] **검수 지적도 코퍼스로 재검증한다.** 리뷰어가 관행 이탈을 지적하면 실제 코퍼스
        분포를 세어 본다. 파일 하나 안의 일관성은 잣대가 아니다.

---

### 2026-08-31 · 자동 측정 블록 바로 위에서 손으로 적은 숫자가 낡았다 (수학사 §11)

- 무엇: `math/ROADMAP.md` §11 「지금 상태」가 **"인물 30편"(실제 37) · "세기 개관 4편"(실제 5) ·
  "갤러리URL 61편"(실제 78)** 을 적고 있었다. 바로 **아홉 줄 아래**에 `build_math_status.py` 가
  다시 쓰는 「자동 측정」 블록이 있고, 그 블록은 **정확히 이 결함을 막으려고 만든 것**이다(#173).
- 왜 안 잡혔나: 게이트도 빌더도 **산문을 검사하지 않는다.** 자동 블록을 만들면서 그 위의
  손 목록을 지우지 않았고, 그 뒤 회차마다 아래만 갱신되고 위는 그대로 남았다.
- 같은 유형 3회차다 — #144(부채표가 나흘 만에 낡음) · #173(빌드 날짜를 콘텐츠 날짜로 읽음) · 오늘.
- 함께 발견: **부채표가 둘로 갈려 서로 어긋났다.** 「갚아야 할 부채」와 「새 문서가 아닌 부채」가
  같은 3건(`person-euler`·`person-laplace`·`infinity-set-theory` §6)을 각각 "남음"으로 적고 있었는데
  **그날 아침 #179 가 셋 다 닫은 뒤**였다.
- 다음 방지 규칙:
  - [ ] **자동 산출 블록을 만들면 그 블록이 세는 것을 산문에서 전부 지운다.** 자동/수동을
        나란히 두면 맞는 숫자가 틀린 문장의 노후를 가려 준다. 산문에는 **판단만** 남긴다.
  - [ ] **같은 부채를 두 표에 적지 않는다.** 표가 둘이면 한쪽만 갱신되고 서로 어긋난다.
        나눌 근거가 정말 있으면 한쪽을 다른 쪽의 참조로만 둘 것.
  - [ ] **회차를 닫을 때 인계 문서를 실측과 대조한다.** 문서를 쓰는 것보다 지도를 갱신하는 것이
        먼저 낡는다 — 새 글은 게이트가 재지만 인계 문서는 아무도 재지 않는다.
