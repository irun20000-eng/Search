# CARDNEWS_PIPELINE.md — 카드뉴스 이관 계획 (Search 판)

> 상태: **계획, 구현 전** · 작성 2026-08-22
> 원본 설계: `카드뉴스 생성/2026-08-22-카드뉴스-스튜디오-이관-design.md` (대상 `Main`)
> 이 문서는 **대상이 `Search`로 바뀌면서 달라지는 부분만** 다룬다. 바뀌지 않는 것은 원본을 따른다.

---

## 0. 무엇이 그대로이고 무엇이 바뀌나

원본 설계의 **판단은 거의 그대로 유효하다.** 이관의 목적(프로젝트 지식 의존을 끊어 제작을 레포 위로 올린다), 정본 3분할, 10장 구조, 학습자료 3조건, 실패 시 멈추고 보고 — 전부 유지한다.

바뀌는 것은 **착지점의 관례**다. `Main`은 `scripts/build.py`가 모든 페이지를 만드는 단일 빌더 구조이고, `Search`는 **갤러리마다 `manifest.json` + `index.html` + `tools/ingest_*.py`** 로 나뉜 데이터 주도 구조다.

| | 원본(Main) | 이 문서(Search) |
|---|---|---|
| 갤러리 빌드 | `scripts/build.py` 가 HTML 통째로 생성 | `cardnews/index.html` 이 `manifest.json` 을 런타임에 읽음 |
| 메타 정본 | `data/galleries/cardnews.yaml` | `cardnews/cardnews.yaml` (Search 엔 `data/` 가 없다) |
| 이미지 | `gallery_src/cardnews/images/` | `cardnews/assets/<folder>/` |
| 학습자료 | `gallery_src/study/` + `docs/gallery/study.html` | 별도 서가 (§3) |
| 위키링크 | **벗겨서 내보낸다** | **살린다** — `link-index.json` 이 갤러리 경계를 넘게 해 준다 (§4) |
| 검색 | 없음 | 허브 통합 검색에 자동 편입 |
| 마크다운→HTML | `build.py` 에 변환 단계 추가 필요 | 불필요 — 갤러리가 이미 렌더한다 |

**Search 로 오면서 없어지는 일이 셋 있다.** 원본 설계 §9가 요구하던 마크다운 변환기, 위키링크 제거, `docs/gallery/study.html` 신규 작성이 전부 기존 배선으로 대체된다.

---

## 1. 레포 구조

블로그(`blog/`)와 **같은 양식**을 쓴다. 새로 발명하지 않는다.

```
Search/
├─ cardnews/
│   ├─ index.html            카드 → 캐러셀 뷰어 (blog/index.html 과 같은 골격)
│   ├─ manifest.json         빌드 산출물 — 직접 편집 금지
│   ├─ cardnews.yaml         ★ 메타 정본 (시리즈·편·개념풀·다음 차례)
│   └─ assets/<folder>/      620×775 webp 10장
├─ studio/                   제작 엔진 (렌더 전용, 갤러리와 분리)
│   ├─ cardnews.py           CLI 진입점
│   ├─ paths.py
│   ├─ engine/               carousel_engine(3.11 호환본) · layout_archive · selector …
│   ├─ contents/             contents_<시리즈>.py — 10장 카피 본문만
│   └─ docs/                 design-rules · content-schema · layout-archive · theme-library
└─ tools/
    ├─ ingest_cardnews.py    studio out/ + yaml → cardnews/{manifest,assets}
    └─ build_link_index.py   cardnews 섹션 추가
```

`studio/` 를 갤러리 폴더 밖에 두는 이유: `cardnews/` 는 **배포되는 정적 자산**이고 `studio/` 는 **로컬에서만 도는 렌더 도구**다. 섞으면 Pages 에 불필요한 파이썬이 실린다.

---

## 2. 블로그와 다른 점 — 캐러셀

블로그는 글 하나 = 마크다운 하나였다. 카드뉴스는 **한 편 = 이미지 10장**이라 뷰어가 다르다.

- 카드: 표지(1장) + 시리즈 배지 + 제목 + 앵글
- 열면: 좌우 넘기기, 키보드 ←/→, 인디케이터 `3 / 10`, 모바일 스와이프
- 캡션(발행용 문구)은 접어 두고 펼치기
- 학습자료가 있는 편은 카드에 **"이해편 읽기"** 를 붙여 해당 서가로 보낸다

나머지(테마 `stTheme`, 필터 칩, 딥링크 `#c=<folder>`, `hashchange`)는 블로그와 동일하게 간다. 블로그에서 실제로 물렸던 두 가지 — **슬러그 불일치**와 **뒤로가기 미동작** — 를 처음부터 막는다.

---

## 3. 학습자료 서가 — 이름 충돌을 먼저 정해야 한다

⚠️ **이룬 서재에는 이미 "학습자료"가 있다.** 바깥 서가의 `youtube-math-skill`(66편)이다. 카드뉴스 학습자료를 같은 이름으로 만들면 **서가가 둘 다 "학습자료"** 가 되어 검색·칩·내비가 전부 모호해진다.

세 가지 중 하나를 골라야 한다.

| 안 | 내용 | 평가 |
|---|---|---|
| **A** | 새 서가를 **`개념노트`** 로 (바깥 "학습자료"는 그대로) | 충돌 없음, 성격도 정확 — *권장* |
| B | 새 서가를 "학습자료", 바깥 것을 "수학 강의자료"로 개명 | 바깥 리포 이름과 어긋남 |
| C | `reports/` 의 이해편과 합침 | DEEP 게이트(8,000자)와 성격이 달라 부적합 |

**A를 권한다.** 산출물이 "개념 하나를 한 장 + 한 편으로 정리한 것"이라 이름이 내용과 맞는다.

### 3-1. 뜻밖의 이점 — 스키마가 거의 같다

학습자료 이해편의 frontmatter 를 실제로 열어 보니 `reports/` 와 사실상 같은 형식이었다.

```yaml
주제: 최적 정지(Optimal Stopping) — 이해편(입문·딥)
날짜: 2026-08-13
깊이: deep
대상수준: 이해편(입문·딥)
태그: [수학, 확률, 의사결정, 최적정지, 비서문제, 이해편]
소스수: { 국내: 2, 해외: 3 }
위키링크: ["[[최적 정지]]", "[[비서 문제]]", …]
카드뉴스: "발견 노트 2편 · 언제까지 둘러보고 언제 정할까"   ← 이것만 추가 필드
```

**변환이 거의 필요 없다.** `카드뉴스:` 한 필드만 이해하면 된다.

### 3-2. 두 가지 조정

**① 인용 표기가 다르다.** 학습자료는 `[해외1]`·`[국내1]`, `reports/` 는 `[n]` 이다. `build_link_index.py` 주석에 "기존 reports/ 는 출처 형식이 세 가지로 섞여 있어 셋 다 받는다"고 돼 있으니 **파서는 이미 관대하다.** 다만 갤러리 렌더에서 인용을 링크로 만들 때 이 형식을 인식하는지 확인이 필요하다.

**② 위키링크를 벗기지 않는다.** 원본 설계는 "볼트 밖에서는 죽은 표시가 되니 텍스트로 바꾼다"고 했다. **Search 에서는 반대로 해야 한다.** `link-index.json` 이 4개(이제 5개) 갤러리를 잇는 조회표이므로, `[[최적 정지]]` 가 다른 서가의 글로 실제 이동한다. 볼트와 웹의 동작이 오히려 같아진다.

---

## 4. 정본 지도 (Search 판)

| 정본 | 담는 것 | 산출물 |
|---|---|---|
| `cardnews/cardnews.yaml` | 시리즈 토큰 · 편 메타 · 개념풀 · 다음 차례 | `cardnews/manifest.json` |
| `studio/contents/contents_*.py` | 10장 카피 본문만 | `studio/out/` PNG |
| 볼트 `000-수집/003-카드뉴스학습자료/` | 이해편 · 개념한장 · INDEX | `개념노트/notes/`·`assets/` |

방향은 언제나 **정본 → 산출물 한 방향**이다. `manifest.json` 과 `notes/` 는 빌드 산출물이라 직접 고치지 않는다. 블로그에서 이미 이 규칙으로 돌고 있다.

---

## 5. 작업 흐름

원본 §7 과 같되 ⑤·⑥ 의 착지가 바뀐다.

```
① list        다음 차례 확인
② 카피 작성    studio/contents/ + cardnews/cardnews.yaml
③ render      studio/out/ 에 PNG 10장 + 모아보기
   ── 사람 게이트: 모아보기 육안 검수 (자동검사는 지저분한 줄바꿈을 못 잡는다) ──
④ 직전 편 s10 'next' 갱신 → 그 한 장만 재렌더
⑤ ingest      webp 변환 → cardnews/assets/ + manifest.json      ← build.py 대신
⑥ study       3조건 충족 시만 — 볼트 정본 → 개념노트/ 로 반입    ← study.html 대신
⑦ link-index  build_link_index.py 재실행                         ← 새로 추가
⑧ commit·push
```

⑦ 을 빠뜨리면 **허브 통합 검색에 안 잡힌다.** 오디세이 2편에서 실제로 겪은 사고이고, LESSONS 에 "빌더 산출물을 손으로 편집하지 않는다 / 보고서 추가 후 반드시 재빌드"로 적립돼 있다.

---

## 6. 선행 작업 — 원본 §10 에서 달라지는 것

| | 할 일 | 상태 |
|---|---|---|
| 1 | Noto Sans/Serif/Mono **CJK** KR 3종 설치 | 원본과 동일 · 미확인 |
| 2 | `pip install -r requirements.txt` · `playwright install chromium` | 원본과 동일 · 미확인 |
| 3 | ~~`Main` 클론~~ → **`Search` 클론, 드라이브 폴더 밖** | 이미 확보(`%TEMP%/searchwork`) |
| 4 | 최신 편 4개 카피 회수 (프로젝트 지식에서) | **미해결 — 사용자만 가능** |
| 5 | 클라우드 루틴 2개 정지 — 이식 완료 후에 | 원본과 동일 |

**4번이 실질적 병목이다.** 옮겨 주신 `카드뉴스 생성/cardnews-studio/contents/` 에는 `content_example.json` 만 있고 `contents_<시리즈>.py` 가 없다. 카피 원본이 아직 claude.ai 프로젝트 지식에 있다는 뜻이고, **이것 없이는 직전 편 `next` 갱신과 재렌더가 불가능하다.**

부모 1~10편 · 일머리 1편은 원본 설계대로 **카피 유실 확정**이라 재렌더하지 않는다. 추측해 다시 만들면 발행본과 문구가 어긋난다.

---

## 7. 구현 순서

되돌리기 쉬운 것부터, 파괴적인 것을 뒤로.

1. **`ingest_cardnews.py` + `cardnews/index.html`** — 기존 16편 webp 를 그대로 반입해 갤러리부터 세운다. 렌더 환경 없이도 가능하다.
2. **link-index · 허브 서가 등록** — 검색에 편입. 여기까지가 "보이는 것".
3. **`개념노트` 서가** — 이해편 2편(심슨의 역설 · 최적 정지)으로 시작.
4. **`studio/` 이식** — 폰트·크로미움 확인 후. 부모노트 12편 재렌더로 이관 검증.
5. **첫 실전** — 숫자노트 4편(검사자의 역설). 렌더 → 갤러리 → 학습자료 → 푸시 전 경로.
6. **자동화** — 블로그와 같은 방식(`sync-to-seojae.yml` 계열). 원본 §3-1 의 "이관 후 결정"이 여기서 정해진다.

1~3 은 **카피 원본 없이도 진행 가능**하다. 4번부터 선행작업 4가 필요하다.

---

## 8. 정해야 할 것

| # | 결정 | 기본안 |
|---|---|---|
| 1 | 학습자료 서가 이름 (§3) | **`개념노트`** |
| 2 | `studio/` 를 Search 에 둘 것인가, 별도 리포로 둘 것인가 | Search 안(`studio/`) — 원본 설계의 "한 레포" 판단 유지 |
| 3 | 기존 16편 캡션을 갤러리에 노출할지 | 접어 두고 펼치기 |
| 4 | 카드뉴스 자동화 주기 | 이관 후 결정(원본 §3-1) |

---

## 9. 이 문서가 대체하지 않는 것

원본 설계 문서의 다음 절은 **그대로 유효하며 여기서 다시 쓰지 않는다.**

- §1 배경 · §2 목표와 비목표 · §6 yaml 스키마
- §9 학습자료 3조건과 판별 기준 (정본은 볼트의 `_INDEX_카드뉴스학습자료.md`)
- §11 실패 처리 · §12 검증 · §13 미결

제작 규칙(10장 구조 · 절대 금지 6조 · 카피 길이 · 파이썬 3.11 제약 · zip UTF-8)은 `카드뉴스 생성/CLAUDE.md` 가 정본이다. 이식할 때 `studio/CLAUDE.md` 로 함께 옮긴다.
